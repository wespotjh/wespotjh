# 프리뷰 — 장바구니 (order/basket.html) · A안

> **라이브에 올리지 않았다.** 대표님 지시: *"완벽해질 때까지 노출은 시키지 말고 작업공간에서만."*
> 이 폴더는 로컬에서 눈으로 확인하기 위한 것이다.

## A안이 바꾼 것 / 안 바꾼 것

**바꾼 것 — CSS 한 파일뿐**
- 새 파일 `ds/css/basket.css`
- `order/basket.html` 에 **`<!--@css(/ds/css/basket.css)-->` 한 줄만 추가** (23행)

**안 바꾼 것**
- `module="Order_*"` / `{$변수}` / `onclick="{$action_*}"` — 전부 원본 그대로
- 네이버 체크아웃 `{$naver_checkout_button_id}`, 결제앱 `{$app_payment_button_box_id}` 자리
- 옵션변경 레이어(`#ec-basketOptionModifyLayer`) 구조
- 7개 배송유형 반복 영역(기본/업체기본/해외/개별/무이자 3종) 구조

되돌리려면 CSS 파일을 지우고 그 한 줄만 빼면 된다.

## 화면에서 달라지는 것

| 항목 | 카페24 기본 | A안 |
|---|---|---|
| 단계 표시 | 텍스트 나열 | 알약 칩, 현재 단계만 검정 |
| 국내/해외 탭 | 회색 박스 탭 | 알약 탭, 선택된 쪽만 검정 |
| 상품 행 | 가로 실선 구분 | **흰 카드 + 테두리 + 라운드 16px** |
| 썸네일 | 정사각 원본 | 84px 라운드 12px + 테두리 |
| 가격 | 본문과 같은 크기 | Archivo 700 · 17px · `tabular-nums` |
| 옵션 | 맨텍스트 | **알약 칩** |
| 수량 | 좌우 절대배치 −/+ | 한 덩어리 스테퍼(라운드 10px) |
| 삭제 | 회색 X (작음) | 32px 원형 히트영역, 호버 시 진해짐 |
| 합계 | 테두리 없는 목록 | **흰 카드**, 결제예정금액 26px |
| 주문 버튼 | 각진 파랑/회색 | 라운드 13px, 잉크색 채움 / 흰 배경 테두리 |
| 이용안내 | 회색 박스 | 라운드 카드 |

## 파일

| 파일 | 용도 |
|---|---|
| `compare.png` | **현재 / A안** 나란히 비교 (보고용) |
| `before_m.png` `after_m.png` | 모바일 430px 전체 캡처 |
| `before.html` / `after.html` | 실제 스킨 CSS를 그대로 링크한 프리뷰. `after` 만 `basket.css` 를 더 읽는다 |
| `_mock.py` | 프리뷰 생성기. `order/basket.html` 의 `{$변수}` 를 가짜 값으로 채워 실제 렌더 DOM 을 재현한다 |

**값은 전부 가짜다.** 상품 2개(칼륨 34,900 / 칼마비 세트 107,500)를 넣어 화면만 확인한 것이다.
템플릿을 고치면 `python3 _preview/basket/_mock.py` 를 다시 돌리면 된다.

## 작업하며 걸렸던 것 (다음 사람 참고)

1. **수량 −/+ 가 카드 모서리로 튀었다.** 스킨은 `.ec-base-qty a:before/:after` 를 `position:absolute`
   로 그리는데, 내가 `.ec-base-qty` 를 `position:static` 으로 바꾸면서 기준점이 카드로 올라갔다.
   → `.ec-base-qty` 는 `position:relative` 로 두고, 대신 그 조각들을 끄고 SVG 배경으로 교체.
2. **삭제 버튼이 X 가 아니라 + 로 보였다.** 스킨은 `+` 를 그린 뒤 `.btnDelete` 자체를 45도 돌려
   X 를 만든다. 여기서 조각을 또 45도 돌리면 90도가 되어 다시 `+` 가 된다.
   → **조각은 돌리지 말고** 위치·크기·색만 바꾼다.
3. `basketPackage.css` 규칙은 대부분 `.xans-order-basketpackage` 아래에 있다. 이 클래스는
   cafe24 가 렌더할 때 붙이므로 **프리뷰에는 없다.** 라이브에서는 그 규칙과 우선순위가 겹칠 수 있어
   실제 적용 전에 한 번 더 확인이 필요하다.

## B안 1단계 — 무료배송 진행바 (완료)

새 파일 **`ds/js/basket.js`** + `basket.html` 24행에 `<!--@js(/ds/js/basket.js)-->` 한 줄.
**HTML 마크업은 그대로**다 — 진행바 DOM 은 스크립트가 합계 카드 위에 끼워 넣는다.

| 상태 | 화면 |
|---|---|
| 미달 | `15,100원 더 담으시면 배송비가 없습니다.` + 오렌지 진행바 |
| 달성 | `50,000원을 넘겨서 배송비가 없습니다.` + 초록 100% |

**숫자를 코드에 쓰지 않는다.**
- 무료배송 기준 금액 → 화면에 이미 있는 카페24 문구에서 읽는다 (`50,000원 이상` · `5만원 이상` 둘 다 인식)
- 총 상품금액 → 합계 카드가 그린 값을 그대로 읽는다
- 둘 중 하나라도 못 읽으면 **아무것도 그리지 않는다** (틀린 숫자를 보여주느니 안 보여준다)
- 수량 변경·선택 삭제로 합계가 다시 그려지면 `MutationObserver` 로 따라 갱신한다

⚠️ **아직 확인 못 한 것**: 라이브 장바구니에 상품이 담긴 상태를 본 적이 없다(빈 장바구니만 확인 가능).
기준 금액 문구가 실제 장바구니 화면 어디에 나오는지는 프리뷰 기준으로만 맞춰 뒀다.
문구가 없으면 진행바는 그냥 안 나온다 — 화면이 깨지지는 않는다.

`freeship_short.png` 가 미달 상태 캡처다.

## 다음 (B안 나머지)

아직 안 했다. **셋 다 구매율 근거는 없다** (대표님 질문에 대한 정직한 답).
- 상품 행에 상품페이지와 같은 루틴/구성 정보 노출
- 국내/해외 탭 정리, 빈 장바구니 화면 문구·CTA
- 7개 배송유형 반복 영역을 실제로 어떤 조합까지 쓰는지 확인 후 정리
