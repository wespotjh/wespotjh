# 라이브 백업 — 2026-09-09

## `detail.css`
**교체 직전 라이브 원본.** 파일업로더에서 직접 내려받은 것.

- 경로: `ROOT > sde_design > skin10 > ds > css > detail.css`
- md5: `90120f8c500ab91adc83a8f6b92c47b2`
- 크기: **49,246 B** (파일업로더 표기 48.09KB)
- 서버 등록일: 2026-09-08 17:08:55

### 정체
git 커밋 `d589a55`(**칼륨 8차**)의 `cafe24-skin/ds/css/detail.css` 와 **바이트 단위 완전 일치**.
→ 9차·11차·12차가 서버에 한 번도 반영된 적이 없었음을 재확인한다.

### 되돌리는 법
파일업로더 같은 경로에 이 파일을 `detail.css` 이름으로 업로드하면
2026-09-09 교체 이전 상태로 완전히 복구된다.

### 왜 파일업로더로 올렸나
디자인 편집기 저장이 이 파일만 계속 실패했다(같은 폴더의 `detail-ui.js` 는 정상 저장).
스킨 내 최대 파일(52KB)이라 편집기 용량 제한이 의심된다.
파일업로더는 동일 이름 업로드를 거부하므로 **삭제 후 업로드** 순서로 교체했다.
