# 붙여넣기 파일 목록 — 2026-09-07 칼륨 상세 개편

> **이 폴더 하나만 쓰시면 됩니다.** 파일 앞 번호 = 체크리스트 순번입니다.
> 체크리스트: `team/보고/QA카페24_칼륨_4차_2026-09-07.md` §4

## ★ 이번(10차)에 다시 올리실 것 — **③ 한 개뿐입니다** (QA 확인 완료)

| 다시 올릴 것 | 어디에 | 바이트 | 맨 끝 줄 |
|---|---|--:|---|
| `3_ds-js-detail-ui.js.txt` | `ds/js/detail-ui.js` — 기존 파일 **전체 선택 후 교체** | **39,376** | `})();` |

나머지 6개(①②④⑤⑥⑦)와 ⑩ 주문서 2개는 **9차와 같은 파일**입니다 — 다시 올리지 않으셔도 됩니다.

> ### ⚠ 「적용」을 이미 누르셨다면 — **이 파일은 저장하는 순간 라이브가 바뀝니다**
> 적용 전에는 "저장해도 쇼핑몰은 1초도 영향받지 않는다"가 맞았지만, **적용 뒤에는 작업본이 곧 라이브**입니다.
> - 그래도 **안전합니다**: 바뀌는 파일은 이 1개뿐이고, 다른 파일이 이 파일을 불러 쓰는 이름·순서는 그대로입니다(QA 확인).
>   저장이 끝나는 순간부터 새 코드가 나가고, 저장 전에 페이지를 연 손님은 **원래대로 9차 화면**을 계속 씁니다(중간에 깨지는 상태가 없습니다).
> - **손님이 적은 시간에** 저장하시면 더 좋습니다. 저장 뒤 **상세 페이지를 새로고침**해 §「10차에서 볼 것」을 확인해 주십시오.
> - **되돌리기:** 문제가 있으면 같은 자리에 **9차 파일(`7044b38e`, 36,541 B)** 을 다시 붙여넣으면 즉시 원복됩니다. 필요하시면 말씀해 주십시오 — 9차 파일을 이 폴더에 `3b_` 로 넣어 드리겠습니다.

### 10차에서 볼 것 (30초)
- [ ] 구성 카드를 눌러 담고, **그 카드를 한 번 더 누르면 아래 목록에서 빠진다**
- [ ] 수량을 3으로 올린 뒤 그 카드를 다시 눌러도 **그 구성 전체가 빠진다**(총액이 함께 줄어든다)
- [ ] **추가구성상품(아래쪽 별도 목록)은 카드로 건드려지지 않는다** — 담긴 채 그대로 남는다
- [ ] 재탭할 때 **확인 팝업이 뜨지 않는다**(원문 확인: 카페24 삭제 경로에 `confirm` 없음)

---

| 순번 | 파일 | 카페24 안에서의 위치 | 바이트 | md5 | 맨 끝 줄 |
|:--:|---|---|--:|---|---|
| ① | `1_ds-js-zg-ga4.js.txt` | `ds/js/zg-ga4.js` | 41,221 | `251c6804…` | `})();` |
| ② | `2_ds-css-detail.css.txt` | `ds/css/detail.css` **(신규)** | **49,186** | **`492bbe8e…`** (11차 · 2026-09-09 추가구성상품 흰 배경) | `}` |
| ③ | `3_ds-js-detail-ui.js.txt` | `ds/js/detail-ui.js` **(신규)** | **39,376** | **`f62a2ce9…`** (10차 · 카드 재탭 해제 R-2) | `})();` |
| ④ | `4_ds-css-price.css.txt` | `ds/css/price.css` | 2,271 | `8d6f577c…` | `}` |
| ⑤ | `5_moa-layout-head.html.txt` | `moa/layout/head.html` | 3,495 | `2a64e772…` | `</head>` |
| ⑥ | `6_product-detail.html.txt` | `product/detail.html` | **45,879** | **`50f2c989…`** (9차 · 622행 실제 키 수록, T-1) | `<!--@import(/coupon/coupon_productdetail.html)-->` + 빈 줄 |
| ⑦ | `7_moa-import-product_detail-detail.html.txt` | `moa/import/product_detail/detail.html` | 2,632 | `0f392522…` | `</div>` |
| ⑩-a | `10a_orderform.html.txt` | **스마트 주문서** `orderform.html` | 13,451 | `8e691d1b…` | — |
| ⑩-b | `10b_order_result.html.txt` | **스마트 주문서** `order_result.html` | 67,003 | `9ffa746b…` | — |

⑧ 미리보기 · ⑨ 적용 은 붙여넣을 파일이 없습니다.

> ⑩ 두 파일은 **스킨이 아니라 「디자인 관리 › 스마트 주문서」** 에 있습니다. 위치가 다릅니다.
> `전달/2026-09-07_GA4_2002/` 와 `reference/주문서_GA4스니펫/` 에도 같은 내용이 있지만
> **대표님은 이 폴더만 보시면 됩니다** (내용은 md5 로 동일함을 확인했습니다).

---

## ⚠ ⑥ 카카오 공유 키 — 작업본 622행 **1줄만** 고쳐 주십시오 (2026-09-08 · QA 오세진 재작성)

**무엇이 문제인가.** 작업본(skin10)에 붙여넣은 `product/detail.html` 의 **622행**이 지금 이렇게 되어 있습니다 (미리보기 소스에서 확인):

```
<div id="kakaoKey" class="displaynone">(키 값은 스킨 원본 참조 · 저장소에는 남기지 않음)</div>
```

괄호 안 문구는 개발이 저장소에 키를 남기지 않으려고 넣어 둔 **자리표시**입니다. 이 상태로는 **「공유」 → 카카오톡 공유만** 안 됩니다.
(페이스북·네이버·URL 복사 등 다른 공유 버튼, 카카오 톡체크아웃 결제는 무관합니다.)

**조치 — 6번 파일을 다시 붙여넣지 마시고, 이 1줄만 고치십시오.**

1. 카페24 관리자 › 디자인 관리 › **`젠제네틱스 v2 작업본`** 편집 › `product/detail.html` 열기
2. 편집창에서 **Ctrl+F → `kakaoKey`** → 622행으로 이동
3. **값 복사처:** 새 탭에서 현재 적용 디자인(`바디랩 반응형`)의 같은 파일 `product/detail.html` 을 열어 같은 방법으로 `kakaoKey` 를 찾고, `>` 와 `</div>` 사이의 **32자리 영문·숫자**를 복사합니다.
   (라이브 상품 페이지에서 「페이지 소스 보기」 → `kakaoKey` 검색해도 같은 값이 그대로 보입니다. 공개 값이라 노출 걱정은 없습니다.)
4. 작업본 622행의 괄호 문구 `(키 값은 … 남기지 않음)` 을 지우고 그 자리에 붙여넣습니다. **`>` 와 `</div>` 사이에 공백·줄바꿈이 들어가면 안 됩니다.** 완성 형태:
   `<div id="kakaoKey" class="displaynone">32자리값</div>`
5. 저장.

**확인 (30초):** 미리보기 페이지에서 「페이지 소스 보기」 → `kakaoKey` 검색 → 32자리 값이 보이면 끝입니다. (QA 가 `curl` 로도 같은 것을 확인해 드립니다.)
카카오톡 공유 창이 실제로 열리는지는 **⑦ 적용 뒤 실제 몰 주소에서** 눌러 보십시오 — 미리보기 주소(`wespotjo.cafe24.com`)는 카카오 앱에 등록된 도메인이 아닐 수 있어 미리보기에서는 열리지 않아도 정상입니다.

**왜 6번 재붙여넣기가 아닌가.** 45,916 B 전체를 다시 붙이면 (a) 옛 판을 붙일 위험 (b) 붙인 뒤에도 같은 자리표시가 다시 들어가 이 단계가 또 필요합니다. 1줄 수정이 가장 작고 되돌리기도 쉽습니다(괄호 문구로 되돌리면 원상태).

> **앞으로 ⑥ `product/detail.html` 을 다시 붙여넣을 때마다 이 1줄이 다시 필요합니다** (장부 E-7). 저장소 파일에 키를 넣을지는 팀장 결정 사항이라 아직 넣지 않았습니다.
> 이 단계는 **② `detail.css` 만 다시 붙여넣는 8차와는 별개**입니다 — 8차 CSS 를 붙이신 김에 같이 해 주시면 됩니다.

> **9차(2026-09-08, 팀장 결정 T-1) 이후:** `6_product-detail.html.txt` 622행에는 **실제 키(32자리)가 들어 있습니다** (라이브 11번 페이지에서 curl 로 읽은 값 — 카카오 JavaScript 키는 공개값).
> 앞으로 ⑥ 을 이 파일로 붙여넣으면 위 1줄 수정이 필요 없습니다. **지금 미리보기만 고치시는 경우에만** 위 절차(622행 1줄)를 쓰시면 됩니다.
> 하네스 `E5` 검사가 미리보기를 매번 받아 라이브 값과 같은지 확인합니다 (고치시면 KNOWN → PASS 자동 전환).

