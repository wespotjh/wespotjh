# QA(모바일) 칼륨 8차 델타 판정서 — 대표님 실기 반려(F-1·F-2·F-3) 대응분

**작성** QA(모바일) 신아린 · 2026-09-08 · 칼륨 8차 (개발 `03H_개발_칼륨_8차_2026-09-08.md`, git `d589a55`)
**범위** 델타 — `ds/css/detail.css` 1파일 변경분 재현 + 회귀 + 하네스 C7 표본 반전. 7차 판정(5차 PASS · H-3 P2)은 그대로.

---

# 판정: **PASS**

- F-1·F-2·F-3 전부 **카페24 옵션 스크립트가 실제로 만든 두 행 유형**(`tr.option_product` 1 + `tr.add_product` 2)에서 320·390·414·768 재측정 — `− N +` 12/12 행, 겹침 0, 오른쪽 여백 20px, 세로 중심 차 1px, 넘침 0.
- 개발 원인 분석은 **라이브 시스템 번들(`optimizer.php`, 1,964,325 B) 원문으로 독립 확인** — 추가상품 모바일 빌더는 `<a class="eAddProductQuantityUpClass"><img class="up …_up">` 이 맞다.
- 회귀 0 — 구성 카드·뱃지·칩·무료배송바·합계·하단 고정바 사각형·계산값이 7차와 동일. 달라진 것은 `.productAction` 아래 내용의 **+27px 하향 이동**뿐(의도된 F-3 여백).
- 개발 수치 1건 부정확(라이브 `#totalPrice` 아래 padding 은 32px 가 아니라 **30px**) — 결론에 영향 없음(§3).

---

## 0. 해시 — 시작(07:45)·종료(07:51) 2회, 불변

| 파일 | 바이트 | md5 | 7차 대비 |
|---|--:|---|---|
| `product/detail.html` | 45,916 | `868784dd` | 불변 |
| `moa/import/product_detail/detail.html` | 2,632 | `0f392522` | 불변 |
| `moa/layout/head.html` | 3,495 | `2a64e772` | 불변 |
| **`ds/css/detail.css`** | **49,246** | **`90120f8c`** | **변경 (+3,695 B)** — 이 판정의 대상 |
| `ds/css/price.css` | 2,271 | `8d6f577c` | 불변 |
| `ds/js/detail-ui.js` | 36,541 | `7044b38e` | 불변 |
| `ds/js/zg-ga4.js` | 42,698 | `5dd8b791` | **변경 (2.003)** — 8차 작업 아님(개발 §0 표기와 일치). 대표님 결정 #9 라고 하나 **동결 파일(M6 종결) 변경은 판정 밖**, 오케스트레이터 확인 요망 |

오케스트레이터 지시문 "나머지 6파일 불변" 은 `zg-ga4.js` 에 대해 **사실이 아니다**(7차 `251c6804` → `5dd8b791`). 홈 4파일 불변(`edbc4f4d`/`c38da0b1`/`c02f8b2c`/`62268614`). 작업 트리 == HEAD(하네스 C7 포함).
정적: `!important` 주석 제외 **0** / `grep -c` 0. 새 규칙 전부 `.zg-detail` 또는 `body:not(#userStyle) .zg-detail` 구체도.

**방법:** 하네스 픽스처(`fixture-iphone-p11.html`, 라이브 p11 + 템플릿 델타) + 캐시된 시스템/사용자 JS 번들을 route 로 서빙, `detail.css` 만 **7차본/8차본/없음(라이브)** 3벌로 바꿔 끼움. 행은 `select[name=option1]`·`addproduct_option_name_63`·`_13` 에 `change` 를 쏴 **카페24 코드가 생성** — 생성된 행이 빌더 산출물인지 마커(`class="up eProductQuantityUpClass"` / `class="eAddProductQuantityUpClass"><img class="up `)로 확인했다. Chromium SwiftShader, iPhone UA.

---

## 1. F-1 · F-2 — 행 기하 (내가 다시 쟀다)

| CSS | vp | 행 | 순서 | 스테퍼 w×h | 가격 | 가격 w | 겹침 면적 | 우측 여백 | 행 안 | 세로중심 차 | 버튼 | sw/cw |
|---|:--:|---|:--:|:--:|---|--:|--:|--:|:--:|--:|---|:--:|
| **8차** | 320 | opt / add / add | `− N +` ×3 | **124×46** | 68,000 / 104,000 / 153,000원 | 72 / 76.7 | **0** | **20** | ✓ | **1** | 44×44 | 320/320 |
| **8차** | 390 | 〃 | `− N +` ×3 | **134×46** | 〃 | 〃 | **0** | **20** | ✓ | **1** | 44×44 | 390/390 |
| **8차** | 414 | 〃 | `− N +` ×3 | 134×46 | 〃 | 〃 | 0 | 20 | ✓ | 1 | 44×44 | 414/414 |
| **8차** | 768 | 〃 | `− N +` ×3 | 134×46 | 〃 | 〃 | 0 | 20 | ✓ | 1 | 44×44 | 768/768 |
| 7차(수정 전) | 390 | opt | `− N +` | **268×46** | 68,000원 | 80 | **1,716** | 16 | ✓ | **12** | | |
| 7차 | 390 | add ×2 | **`+ − N`** | **263×46** | 104,000 / 153,000원 | 80 | **1,586** | 16 | ✓ | 12 | | |
| 라이브(우리 CSS 없음) | 390 | 3행 | `− N +` | 103×30 | 〃 | 80 | 0 | 16 | ✓ | 4 | 30×30 | |

- 대표님 반려 두 결함 **7차본에서 그대로 재현**(겹침 1,716/1,586 px² · 추가상품 `+ − N`), 8차본에서 **0**. 개발 표(§4-1)와 값 일치(겹침 면적만 1,647 → 내 계측 1,586, 가격 칸 위치 차이 — 결론 무관).
- `+`/`−` 글리프: 두 행 유형 모두 `::before` 9px · `::after` 1px(잉크색) 그려짐. 7차본 추가상품 행은 `::after` 가 스킨 검정(`rgb(0,0,0)`)이었다 — `.up::after` 가 `<img>` 에 걸려 우리 규칙이 안 닿았던 흔적. 8차본은 `eAddProductQuantityUpClass::after` 로 닿는다.
- 스킨의 `<img>` 는 `opacity:.00001`(스킨 규칙)로 이미 투명 — 우리는 안 건드린다.
- 320 에서 스테퍼 124px(숫자 칸 36px), 가격 `104,000원` 76.7px → 행 안폭 208px 에 **7.3px 여유**. 개발 자진 발견(2.7px 부족) 해소 확인.
- pageerror 2건은 `Kakao`·`AuthSSLManager` 전역 미정의(교차출처 차단) — 7차본·라이브에도 동일, 우리 것 아님.

## 2. F-2 원문 — 추가상품 행의 `.up/.down` 은 `<img>` 에 있다 ✅

라이브 시스템 번들 `optimizer.php`(9/7 취득, 1,964,325 B) 원문:

- 모바일 옵션 행 `buildMobileSoldInCells()`: `<a href="#none" class="up eProductQuantityUpClass" …>` — `.up` 이 `<a>`.
- 모바일 추가상품 행(`EC_MOBILE_DEVICE === true` 분기, 오프셋 1,624,323): `<a href="#none" class="eAddProductQuantityUpClass" data-target="…_up"><img … class="up ' + sOptionBoxCode + '_up" alt="up"/></a>` — **`.up` 이 `<img>`, `<a>` 에는 동작 클래스만.**
- 데스크톱 추가상품 행(1,628,093): `<span class="quantity"><… <a class="up eAddProductQuantityUpClass">` — `<a>` 에 둘 다.
- 클릭 핸들러 `EC$(document).on('click', 'a.eAddProductQuantityUpClass, a.eAddProductQuantityDownClass', …)` — 동작 클래스가 진짜 셀렉터다. `order` 를 여기에 거는 것이 맞다.

런타임에서도 확인: 8차 픽스처 add 행 `<p>` 직계 `<a>` 클래스 = `eAddProductQuantityUpClass | eAddProductQuantityDownClass`.

## 3. F-3 — 말풍선 (강제 표시 · 경고문 표시/숨김 · 바운스 −5px)

| CSS | vp | `#totalPrice` pad-b / 높이 | `.productAction` margin-top | 경고문 | 덮는 텍스트(표시) | 숨김 간격 | 숨김+바운스 간격 | 덮는 텍스트(숨김+바운스) |
|---|:--:|---|:--:|---|---|--:|--:|---|
| **8차** | 320 | 0px / 1 (K-08 접힘) | **37px** | block | **[]** | 10 | **+5** | **[]** |
| **8차** | 390 | 〃 | 37px | block | [] | 10 | +5 | [] |
| **8차** | 768 | 〃 | 37px | block | [] | 10 | +5 | [] |
| 7차 | 390 | 0px / 1 | 0px | block | `EC-price-warning` | **−27** | **−32** | **`zg-sum__note`** ← 대표님이 본 것 |
| 라이브 CSS(우리 없음) | 390 | **30px** / 80 | 0px | block | `EC-price-warning` | | | |

- **우리 회귀 맞다**(개발 결론 동의): 라이브에서는 `#totalPrice{padding:20px 0 30px}`(사용자 CSS 번들 원문)의 **아래 30px** 이 말풍선 자리였고 K-08 접기가 그것을 0 으로 만들었다.
- **개발 수치 정정:** 라이브 여백은 **32px 가 아니라 30px** 이다(번들 원문 `padding:20px 0 30px`, 라이브 CSS 계산값 `padding-bottom:30px`). 필요 여백 = 30 + 바운스 5 = 35px, 개발이 잡은 37px 는 +2 여유 → 결과 gap +5 는 그대로 성립. 주석(`detail.css:927` "22px 0 32px")만 사실과 다르다 — P3.
- 라이브에서도 말풍선이 카페24 경고문 `.EC-price-warning` 을 덮는 것은 스킨 원래 모양 — 손대지 않는다는 판단 동의.
- 말풍선 자체(`.benefit-bubble > a{top:-28px;height:26px}`, `bubbleBounce` −5px) 는 건드리지 않았음을 CSS 번들 원문과 8차 diff 로 확인.

## 4. 회귀 — 7차 CSS vs 8차 CSS, 같은 픽스처(행 생성 후) 비교 (390 · 768)

사각형(x,y,w,h) + display/position/font-size/background/border 비교:

- **동일:** `.zg-opt` 카드 4 · `.zg-badges` · `.zg-opt__free` 3 · `.zg-opt__tag` 4 · `#freeShipGuide` · `.zg-sum` · `.zg-sum__row` 3 · `.zg-sum__note` · `.zg-bar__sum` · `#orderFixArea`(하단 고정바) · `.zg-picked-head` · `#totalPrice` · `.thumbnail__list`
- **차이(전부 `.productAction` 아래 내용의 y +27px, 크기·스타일 동일):** `.productAction`(2876→2903) · `.zg-dh` · `.zg-fold` · `.zg-more` · `.zg-guide`(칩 포함) · `.infoArea-footer` 높이 +27 · 문서 높이 7,247→7,274 (768: 6,907→6,934). 스테퍼 폭 변경이 다른 곳을 밀지 않았다.
- 하단 고정바·상단 카드는 이동 0.

## 5. 하네스 C7 — 표본 반전

`c_render.py` 를 import, `fixture.OURS['/ds/css/detail.css']` 만 바꿔 `rows390` 단독 실행:

| 반전 | 깨뜨린 것 | 결과 |
|---|---|---|
| S1 | 8차본에서 `eProduct/eAddProduct…Class` order 셀렉터만 제거(`.up/.down` 만 남김) | **`C7.seq` add 행 2건 FAIL(`+ − N`)** ✅ |
| S2 | 8차본 + `.productAction{margin-top:0}` | **`C7.bb.ov`·`bb.ovh`·`bb.gap`(−32) FAIL** ✅ |
| S3 | 7차본 통째 | **FAIL 17** — `w`(268/263)·`ov`(1716/1586)·`ri`(16)·`cy`(12) ×3행 + `seq` ×2 + 말풍선 3 ✅ (개발 "6건" 보다 많이 잡는다 — 폭·여백·중심까지) |
| S4(무효) | input 만 `flex:1 1 auto` 로 되돌림(컨테이너 134px 고정 유지) | FAIL 0 — 컨테이너가 고정돼 input 이 46px 로 줄어 **실제로 결함이 아니다**. 하네스 잘못이 아니라 내 반전이 결함을 못 만든 것. 기록만 한다 |

C7 은 거짓 통과를 막는다. 빈틈: `VP_ROWS` 에 **414 없음**(개발 표에는 있다), 데스크톱 UA 행(`span.quantity`, (B) 수정)은 픽스처가 없어 **하네스도 나도 못 쟀다** — 실기.

## 6. 정답지 대조

| 항목 | 판정 |
|---|---|
| F-1 스테퍼 134/124px 고정 · input 46/36px | 구현 · 4뷰포트 재현 |
| F-2 동작 클래스로 order · `::after` | 구현 · 원문 근거 확인 |
| 가격 칸 `right:20px; bottom:20px; line-height:44px; nowrap` (두 행 유형만) | 구현 · 옵션 없는 상품(60·61·62) 서버 렌더 행 제외 판단 동의 |
| ≤359px 124px | 구현 · 320 에서 여유 7.3px |
| F-3 `.productAction{margin-top:37px}` | 구현 · 라이브 여백 수치(32→30) 주석만 오기 |
| (B) 데스크톱 `span.quantity input` 46px | 미검(데스크톱 픽스처 없음) — 실기 |
| `#kakaoKey` 자리표시 | 오세진 소관 |

## 7. 잔여

- **P3** `detail.css:927` 주석 "22px 0 32px" → "20px 0 30px". · 하네스 `VP_ROWS` 에 414 추가 권고.
- **실기(대표님 재확인 3점, 개발 §7-2 그대로)** ① 선택 행 `− 1 +` + 가격 오른쪽 ② 추가상품 행 동일 순서 ③ 말풍선이 안내문을 안 덮음 (경고문이 숨겨진 상태에서). 추가: ④ PC 브라우저에서 선택 행 스테퍼 폭(데스크톱 빌더, (B) 수정) ⑤ 320px 급 기기(SE1·폴드 커버)에서 `104,000원` 행.

*QA(모바일) 신아린 · 2026-09-08 (칼륨 8차 델타) · 심의 판단 없음*
