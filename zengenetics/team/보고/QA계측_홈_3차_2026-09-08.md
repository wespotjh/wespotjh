# ④-B QA(계측) 홈 3차 — **전량 재검수**

**판정: PASS — 계측 회귀 0건 · 홈 계측 게이트 3회 완료 · 미해소 선행조건 없음**
**작성:** 개발팀 QA·계측 **배시우** · 2026-09-08 (홈 3차)
**시작 해시(실측 = 동결값):** git `c663592` · `index.html` `4cb10827` 17,281 · `home.css` `c88028de` 17,294 · `home-blocks.js` `b22664e2` 8,800 · `home-hero.js` `071f78c2` 36,509 · **`zg-ga4.js` `5dd8b791` 42,698 불변** · `node --check` 2/2 통과

> 1차 REJECT 없음(PASS + 선행조건) → 2차 PASS → **3차 전량 PASS.** 1차에 낸 `ZG_VER` 2.003 · N-H1 · N-H2 · N-H3 · N-H6 은 대표님 결정 #9 로 승인·구현·검증 완료(`QA계측_GA4_2003_2026-09-08.md`). **홈 계측에 남은 승인 안건이 없다.**

---

## 1. F-4 발신부 — **내 명세 그대로. 이번엔 반쪽이 아니다**

`home-hero.js:691`(내가 지목한 `dashIdx = di;` 직후):
```js
      dashIdx = di;
      /* 히어로 구간 도달을 계측 스크립트에 알린다 (A·M·E·N·S). 수신부가 없어도 무해하다. */
      try { document.dispatchEvent(new CustomEvent('zg:hero-stage', { detail: { stage: 'AMENS'.charAt(di) } })); } catch (e) {}
```
**내 §6 명세와 문자 일치**(try/catch 포함). `dashEls` 토글보다 앞에 있어 DOM 상태와 무관하게 나간다.

### 1-1. 발신부 단독 — `di` 경계를 원문 그대로 복제해 p 스윕(0.001 간격)

| 스윕 | 발신 순서 |
|---|---|
| 정방향 0→1 | `["M","E","N","S"]` — 경계 `0.100/0.360/0.570/0.830` 각 1회 |
| 역방향 1→0 | `["N","E","M","A"]` — **맨 위로 되돌아가면 `A` 가 여기서 나간다** |
| 왕복 합집합 | `["A","E","M","N","S"]` — 5구간 전부 도달 가능 |

> **2.003 검증서의 내 서술을 정밀화한다.** 나는 *"`_A` 는 시작값(0)이라 나가지 않는다"* 고 썼다. 정방향만 보면 맞지만 **역방향에는 나간다.**
> → `zg_hero_stage_A` 의 의미는 "히어로 진입"이 아니라 **"히어로 맨 위로 되돌아온 사람"** 이다. GA4 해석 시 이 정의를 쓴다. 결함이 아니라 해석 규칙이다.

### 1-2. 2.003 수신부와의 결합 — 실제로 맞물린다

| 검사 | 결과 |
|---|---|
| **no3d 경로**(3D 프레임 루프 미가동) → `zg_hero_stage_*` | **0건** ✅ (설계대로) |
| 3D 경로 모사 — 발신부 산출 순서를 정방향+되감기 **8회** 발신 | 수신 **`_M _E _N _S` 각 1건** ✅ |
| `send()` 중복 차단 | **전부 1회** ✅ (되감기 재발신 4회 전량 차단) |
| 페이로드 | `{"stage":"M","zg_ver":"2.003","send_to":"G-GZHFY596SS"}` ✅ |
| throw | **0** |

**발신부 ↔ 수신부가 실제로 물렸다.** 2.003 검증 때는 수신부만이었고, 이번에 양쪽이 붙은 상태로 확인했다.

---

## 2. F-2 임계 `<= 8` — `hero_mode` 분포에 영향, **분모 오염은 0**

`promoteIfNearTop()` 이 `pageYOffset > innerHeight*0.5` → **`> 8`** 로 좁혀졌다(개발 근거: 실측 CLS 가 스크롤 0 에서만 0.006, 그 아래는 0.4~0.8).

| 뷰포트 | 2차 임계 0.5vh | **3차 임계 8px** | 첫 마크 10 % 위치(no3d) | 임계 < 첫 마크 |
|---|---|---|---|---|
| 360×740 | 370px | **8px** | 1,005px | ✅ 126배 여유 |
| 390×844 | 422px | **8px** | 1,104px | ✅ 138배 |
| 414×896 | 448px | **8px** | 1,151px | ✅ 144배 |
| 768×1024 | 512px | **8px** | 1,278px | ✅ 160배 |
| 1280×900 | 450px | **8px** | 1,184px | ✅ 148배 |

- **분모 오염 0 — 2차보다 더 안전해졌다.** 승격은 첫 마크가 찍히기 훨씬 전(8px 지점)에만 일어나므로, 한 페이지 안에서 `hero_mode` 가 마크 사이에 바뀔 수 없다.
- **대신 `no3d` 모집단이 커진다** — 8px만 내려가도 승격을 포기한다. 팀장 지적대로다. **그것을 가르는 것이 정확히 `hero_mode`** 이고, 3값이 정상 동작함을 §4 에서 확인했다. 계측 관점 문제 없음.
- 다만 GA4 해석 시 유의: `hero_mode:'3d'` 는 **"3D 를 본 사람"** 이지 **"3D 가 가능한 기기"** 가 아니다(느린 망에서 3D 가능 기기도 no3d 로 집계된다). 판정서 §6 해석 규칙에 넣는다.

---

## 3. 접근자 스텁 3변수 — `zg-ga4.js` 전역 충돌 **0**

`index.html:60` 이 `['runMainBannerSlidePC','runMainBannerSlideMobile','runSubBannerSlide']` 3개를 접근자로 정의한다(`runSubBannerSlide` 신규).
`zg-ga4.js` 전문의 `window.*` 접근은 **`dataLayer` · `gtag` 만**이고 전역을 **내보내지 않는다**(`ZG_VER`·`ZG_ID`·`sent` 전부 IIFE 내부). 세 이름 어디에도 닿지 않는다. **충돌 0.**
차단 호출 재집계: `home-blocks.js` 0/0/0 · `home-hero.js` `stopPropagation` 0 · `stopImmediatePropagation` 0 · `preventDefault` **1**(jump 버튼, 1·2차와 동일·무해).
`home-blocks.js` 변경(`np > 0` 가드로 카운터 텍스트를 늦게 건드림)은 `zg-ga4.js` 가 읽지 않는 숫자 텍스트다 — 계측 무관.

---

## 4. dataLayer 전량 재실행 (항목 4)

| | 개편 전(라이브) | 3차 후 · no3d | 3차 후 · reduced | 3차 후 · 3d |
|---|---|---|---|---|
| `.zg-home` class | — | `zg-home zg-no3d` | `zg-home zg-reduced zg-no3d` | `zg-home` |
| `zg_scroll_depth` | **6** | **6** | 6 | 6 |
| `hero_mode` | — | `["no3d"]` | `["no3d"]` | **`["3d"]`** |
| `detail_expanded` 키 | (2.002 때 6) | **0건** ✅ | 0건 | 0건 |
| `zg_ver` | — | `["2.003"]` | `["2.003"]` | `["2.003"]` |
| **`select_item`** | 0 | **8/8** | 8/8 | 8/8 |
| `item_list_name` | — | **6종** `home_ctabar` `home_pblock_pot` `home_pblock_mag` `home_pblock_vitb` `home_thumb` `home_store_all` | 동일 | 동일 |
| jump ×2 · `.kakao_link` | +0 | **무발사** ✅ | ✅ | ✅ |
| 팝업 동결/해제 | — | +0 / +0 | +0 / +0 | +0 / +0 |
| throw | 0 | **0** | 0 | 0 |

**확정순서(home→ga4) vs 역순 완전일치 `true`.** 개편 전 6건 → 3차 후 6건 — **소실 0**, 신규는 승인분(`select_item` 8 · `zg_hero_stage` 4)뿐. `send_to` 전수, PII 0.
`reduced` 줄이 `no3d` 로 잡히는 건 2.003 스펙의 우선순위 그대로이며, 두 상태 모두 트랙 100vh 라 **분모 구분 손실 0**(2.004 백로그).

---

## 5. L-H2 — `async` 후 부팅 시각 **실측** (항목 5)

로컬 HTTP 서버로 **1,200 ms 지연된 three.js** 를 서빙하고, `zg-ga4.js` 와 같은 부팅 규약(`readyState==='loading'` → DCL 대기)의 번들을 `</body>` 앞에 두어 잰다.

| 속성 | three.js 실행 | **DCL(= `zg-ga4.js` 부팅)** | 판정 |
|---|---|---|---|
| `defer` (1·2차 이전) | +1,400 ms | **+1,400 ms** | three 를 **기다린다** |
| **`async` (현행)** | +1,230 ms | **+31 ms** | **기다리지 않는다** |

**부팅 앞당김 = 1,369 ms** (지연 1,200 ms 조건). `async` 에서 DCL 이 three 실행보다 **앞선다** ✅.
→ **W-H1 해소 확정.** 느린 망에서 조기 이탈자의 `zg_scroll_depth` 유실(N2 유형 편향)이 제거됐다.
남은 차단 자원은 `head.html` 의 Google Fonts CSS(칼륨 W-1, 40 KB)뿐 — 실기 체감은 L-H2′ 로 승계.

---

## 6. GA4 해석 규칙 (이번에 확정된 3가지 — 보고서 읽을 때 쓴다)

1. **`zg_hero_stage_A`** = 히어로 진입이 아니라 **맨 위로 되돌아온 사람**. 진입은 `page_view` + `hero_mode:'3d'` 로 본다.
2. **`hero_mode:'3d'`** = **3D 를 본 사람**(기기 성능이 아니다). 임계 8px 때문에 3D 가능 기기도 조금만 내려가면 `no3d` 로 집계된다.
3. **`zg_scroll_depth.percent`** 는 홈에서 열독 깊이가 아니라 **트랙 위치**다(1차 §4: sticky 가 분모의 78 %). `hero_mode` 로 나눠 보지 않으면 3D/no3d 두 분모가 섞인 평균이 된다.

---

## 7. 판정

| 체크 | 결과 |
|---|---|
| `zg-ga4.js` 무변경 | ✅ `5dd8b791` |
| F-4 발신부 명세 일치 · 수신부 결합 | ✅ 문자 일치 · `_M _E _N _S` 각 1회 · 중복 0 · no3d 0 |
| F-2 임계 8px | ✅ 첫 마크보다 126~160배 앞 → 분모 오염 0 |
| 스텁 3변수 충돌 | ✅ 0 |
| dataLayer 6/6 · `select_item` 8/8 · `item_list_name` 6종 · `hero_mode` 3값 | ✅ |
| 실행 순서 2가지 | ✅ 완전일치 |
| 중복·차단 | ✅ 0 · 0 · `preventDefault` 1(무해) |
| **L-H2** | ✅ **1,369 ms 앞당김 실측 — W-H1 닫힘** |

# **판정: PASS. 홈 계측 게이트 3회 완료, 미해소 선행조건 없음.**
**실기 잔여(배포 후):** L-H1(3D 실 `scrollHeight`) · L-H2′(폰트 CSS 차단 체감) · L-H3(실팝업) · L-H4·L-H5(오세진) · L-6(실주문 `purchase`) · L-9(주문서 3줄 후 `begin_checkout`).
**2.004 백로그:** `item_name` 정리 · `hero_mode` reduced 우선순위 · N-H4 · N-H5 · N-7.

**종료 해시:** 아래 실측(시작과 동일).
*QA(계측) 배시우 · 2026-09-08 (홈 3차)*
