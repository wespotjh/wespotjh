# QA(카페24 실환경) 확인서 — 제출 해시 `7044b38e`

**작성:** 개발팀 QA(카페24 실환경) **오세진** · 2026-09-07
**성격:** 재판정이 아니라 **해시 확인서.** 3차 PASS(`3ef0d393`) 이후 `ds/js/detail-ui.js` 만 세 번 바뀌었으므로,
내 소관(**카페24 실환경**)에 한해 현재 해시에서 다시 재현했다. 기능·계측·SEO 는 배시우·문가온·신아린 5차 PASS 소관이다.

---

# 판정: **PASS** — `7044b38e` 확인. 조건 없음.

## 1. 스냅샷 — 독립 재계산 7/7 일치

| 파일 | md5 | 바이트 | 3차 대비 |
|---|---|--:|---|
| `product/detail.html` | `868784dd` | 45,916 | 불변 |
| `moa/import/product_detail/detail.html` | `0f392522` | 2,632 | 불변 |
| `ds/css/detail.css` | `f8095b8b` | 45,551 | 불변 |
| **`ds/js/detail-ui.js`** | **`7044b38e`** | **36,541** | **변경** |
| `ds/css/price.css` | `8d6f577c` | 2,271 | 불변 |
| `moa/layout/head.html` | `2a64e772` | 3,495 | 불변 |
| `ds/js/zg-ga4.js` | `251c6804` | 41,221 | 불변 |

**저장소 ↔ 전달본 7쌍 md5 전부 일치** — `전달/2026-09-07_칼륨상세/` 재확인. **어긋남 0.**

## 2. 플랫폼 임계 항목 재검증 (내 소관만)

### 2-1. 정적

| 항목 | 결과 |
|---|---|
| `node --check detail-ui.js` | **통과** |
| `!important` (`detail.css` / `price.css`) | **0 / 0** |
| 플랫폼 임계 함수 6종 존재 | `unlazyDetailImages`(74) · `rearmLazyLoader`(108) · `captureDelivery`(131) · `visibleRows`(205) · `place`(561) · `MutationObserver`(457) **전부 생존** |
| `ec-data-src` 승격 로직 | `removeAttribute('ec-data-src')` 유지(85행) |
| `__ZG_LAZY_REARMED__` 가드 | 유지(111-112행) |

### 2-2. ★ 번들 선언 순서 — F-6 의 본체. 재확인했다

내가 페이로드 역공학으로 확정한 규칙: **① 콘텐츠 페이지 직접 선언(줄 순서) → ② 레이아웃 → ③ import.**
`product/detail.html` 실측:

```
4: <!--@css(/ds/css/detail.css)-->        오프셋 105   ← 우리
5: <!--@js(/ds/js/detail-ui.js)-->        오프셋 137   ← 우리
7: <!--@css(/moa/css/lib/ptrv_style.css)--> 오프셋 170  ← 스킨 첫 @css
8: <!--@js(/moa/js/product/detail.js)-->    오프셋 211  ← 스킨 첫 @js
```
**우리 선언이 CSS·JS 양쪽에서 스킨보다 앞 → 사용자 번들 1번 유지.**
`CAPTURED`(배송비 문자열 선점)와 이미지 승격이 `moa/js/product/detail.js`·`CAFE24.lazyload()` 보다 먼저 도는 전제가 **그대로 성립한다.**

### 2-3. 실렌더 재현 — 헤드리스 Chromium, `7044b38e` 로 다시 조립해 3상품 재측정

라이브 HTML + 동일출처 자산 실물(시스템 JS 번들 1.9 MB 포함) 오프라인 재조립, 우리 CSS/JS 를 번들 순서 그대로 주입.
**교차출처를 전부 차단했으므로 아래는 「웹폰트가 한 벌도 안 뜬 상태」의 값이다.**

| 항목 | **11 (필수옵션)** | **61 (옵션 없음)** | **62 (옵션 없음)** | 3차/4차 값과 |
|---|---|---|---|---|
| `.zg-picked-0` | true | **false** | **false** | **동일** |
| 안내문 computed `display` | **block** | **none** | **none** | **동일** |
| 하단바 금액 | `구성을 선택해 주세요` | **`15,000원`** | **`15,000원`** | **동일** |
| `#totalPrice` | `0` | `15,000원` | `15,000원` | **동일** |
| **금액-안내문 모순** | **없음** | **없음** | **없음** | **동일** |
| 상세 이미지 `src` 승격 | 14 | 16 | 15 | **동일** |
| 남은 `ec-data-src` | **0** | **0** | **0** | **동일** |
| `CAFE24.lazyload()` 호출 | **1** | **1** | **1** | **동일** |
| `__ZG_LAZY_REARMED__` | true | true | true | **동일** |
| 구성 카드 | 4 | **0** | **0** | **동일** |
| 하단바 실측 높이 | **82px** | **82px** | **82px** | **동일** |
| `.zg-fold` `max-height` | 1700px | 1700px | 1700px | **동일** |
| 안내 칩 / 더보기 / 각주 | 3 / 있음 / 2 | 3 / 있음 / 2 | 3 / 있음 / 2 | **동일** |
| `--zg-f-d` 폴백 체인 | `"Archivo","Pretendard Variable",system-ui` | 동일 | 동일 | **동일** |

**R-2(옵션 없는 상품 빈 상태) · R-5(로더 재기동) 가 새 해시에서도 그대로 성립한다. 회귀 0건.**

---

# 3. 회귀 하네스 `zengenetics/test/` — 확인했다. 빠진 것 3건

**좋다.** `Z`(제출 동결)가 내가 요구한 절차를 그대로 구현했고, 거짓 통과 방지 4겹(탐지 0매칭 FAIL · 랜드마크 선검사 ·
음성 대조군 C3N · 클릭 스텝 검증)은 내가 문서로만 말하던 것을 코드로 만든 것이다.
`baseline.json` 을 만드는 코드와 검사하는 코드가 **같은 `measure()`** 를 쓰게 한 것, `KNOWN` 을 종료코드에서 빼되
*"괜찮다가 아니라 이미 알고 있다"* 로 정의한 것, **「자동화하지 못한 것」 7건을 PASS 목록에 섞지 않고 따로 세운 것** — 전부 옳다.

## 빠진 것 (전부 정적 검사. 각 몇 줄이면 붙는다)

| # | 빠진 검사 | 왜 필요한가 | 어떻게 |
|:--:|---|---|---|
| **T-1** | **`@css`/`@js` 선언 순서** | `A4` 는 경로가 실재하는지만 본다. **누가 `product/detail.html` 의 4·5행을 7·8행 아래로 옮기면** 우리 파일이 번들 뒤로 밀려 `CAPTURED` 선점과 이미지 승격 선행이 조용히 깨진다. **F-6 반려의 본체가 지금 무방비다.** §2-2 가 그 검사다 | `A6.order`: `product/detail.html` 에서 `@css(/ds/css/detail.css)` 오프셋 < `@css(/moa/…ptrv_style.css)`, `@js(/ds/js/detail-ui.js)` < `@js(/moa/js/product/detail.js)` |
| **T-2** | **저장소 ↔ 전달본 md5 대조** | `Z` 는 **저장소 7파일이 실행 중에 안 바뀌는지**만 본다. **대표님이 붙여넣는 것은 `전달/…/*.txt` 다.** 이 둘이 갈린 사고가 **이미 두 번** 났다(내 4차 §(g), 그리고 `3b159ae5` 때). 갈린 채로 배포하면 **검수한 코드와 다른 코드가 카페24에 들어간다** | `Z2.delivery`: 7쌍 md5 동일 |
| **T-3** | **CSS `url()` 루트절대 강제** | `ds/css/*.css` 는 `optimizer_user.php` 번들로 들어가므로 상대 `url()` 은 `/ind-script/…` 로 해석돼 **404**. 근거: 사용자 CSS 번들 `url()` **29개 중 상대경로 0개**, 번들 내 `@font-face` **0개**(Pretendard 는 인라인 `<style>` 에 루트절대). **칼륨 CSS 2본에는 `url()` 이 0개라 지금은 무해**하지만, **홈·브랜드스토리는 웹폰트·이미지를 `url()` 로 부른다** | `B6.url`: `ds/css/*.css` 의 모든 `url()` 이 `/` 또는 `//`/`http`/`data:` 로 시작 |

**T-1·T-2 는 칼륨 배포 전에 붙이길 권한다.** 둘 다 이미 실제로 사고가 났던 자리다.
T-3 은 홈·브랜드스토리 착수 전이면 된다.

## 그 밖에 확인한 것
- `E2` 허용목록에 **아직 배포 전이라 정상 404 인 5개**(`detail.css`·`detail-ui.js`·`zg-ga4.js`·basket 2건)를 넣고
  **배포 후 200 이 되면 `E2.fixed` 가 알려 주는** 설계 — 내 3차 §2-2 의도를 정확히 옮겼다.
- `E3` 의 하한 **639px** 근거(라이브 전 상품 639~640, +0.16% 는 육안 식별 불가)도 타당하다.
- `E4`(주문서 계측 3줄) 를 **현재 0 → KNOWN** 으로 둔 것 옳다. 내 4차 §(a) 실측과 일치하고, 3줄을 넣고 적용하면 자동 PASS 로 뒤집힌다.
- 「자동화하지 못한 것」 7건이 내 실기 잔여 5건 + 신아린 iOS + 배시우 실주문을 정확히 포괄한다. **PASS 목록 오염 0.**

---

# 4. 남기는 말

`ds/js/detail-ui.js` 는 3차 PASS 이후 **네 번** 바뀌었다(`3ef0d393` → `22c3684f` → `3b159ae5` → `7044b38e`).
매번 전달본을 다시 뽑아야 했고, 두 번은 갈린 채로 잠깐 남아 있었다.

**이번 라운드에서 그 문제는 사람의 주의가 아니라 하네스로 옮겨졌다.** 그게 옳은 방향이다.
다만 하네스가 지키는 것이 **저장소 7파일**이므로, **대표님이 실제로 붙여넣는 전달본까지 같은 그물 안에 넣어야**(T-2)
이 사고 유형이 완전히 닫힌다.

**내 소관에서 칼륨은 이 해시로 닫는다.**

---

**QA(카페24 실환경) 오세진** · 2026-09-07

---
**추기 (2026-09-08):** 3게이트(신아린·배시우·문가온 5차) PASS 이후 재확인 — 7파일 md5 `868784dd`·`0f392522`·`f8095b8b`·**`7044b38e`**·`8d6f577c`·`2a64e772`·`251c6804` **불변**, 저장소↔전달본 7쌍 일치. 위 판정을 뒤집을 새 사실 없음. **PASS 유지 — 4게이트 전원 통과로 칼륨을 닫는다.** — QA(카페24 실환경) 오세진
