# ④-B QA(계측) 2차 판정서 — 칼륨(product_no=11) 상세 개편

**판정: 2차 변경분 PASS (신규 계측 결함 0건) · 게이트 미개방 — 1차 R-1 승인 대기로 이월**
**작성:** 개발팀 QA·계측 **배시우** · 2026-09-07 (2차)
**스코프(팀장 지시):** 1차 R-1(`ZG_VER`)은 승인 대기로 보류. **2차 변경분이 계측을 새로 깼는가만** 판정한다.
**대상:** `product/detail.html`(647→654) · `moa/import/product_detail/detail.html`(57→62) ·
`ds/css/detail.css`(1,012→1,149) · `ds/js/detail-ui.js`(558→634) · **`ds/css/price.css`(27→43, 2차 신규 대상)**
**입력:** `03B_개발_칼륨_2차_2026-09-07.md`
**검증:** 라이브 DOM(345,563B) + jsdom, **독립 하네스 6종 · 총 15회 실행**

> ⚠️ 1차 판정서에 없던 **`ds/css/price.css`** 가 2차에서 변경 대상에 추가되었다. 검수 범위에 포함해 판정했다(§6).

---

## 0. 결론 3줄

1. **`ds/js/ga4.js` 는 무변경이다.** `git diff` 빈 출력 + **md5 가 HEAD 와 동일**(`879c13d9…`). `ZG_VER` 여전히 `2.001`.
2. **2차 변경분이 새로 깬 계측은 0건이다.** dataLayer 5건/5건, 파라미터 완전 일치. 실행 순서 2가지 모두 동일. 중복 0 · 차단 0 · 오발사 0.
3. **1차 R-1 은 그대로 유효하다.** 접기 상한(`1700px`/`2200px`)이 2차에서 **변경되지 않았고**, 숨김량 **7,594px(81.7%)** 도 그대로다.
   오히려 F-1 이 접힘 문서 높이를 "예측 불가"에서 "안정 1,700px"로 바꿔 **분모가 또 한 번 이동**했다 — `ZG_VER` 분리가 더 필요해졌다.

---

## 1. `ga4.js` 무변경 재확인 (팀장 확인 항목 1 — 내가 독립으로 재검증)

```
$ git status --short                    → ds/js/ga4.js 없음 (변경 목록: price.css · 두 템플릿 · 신규 2파일)
$ git diff HEAD --stat -- ds/js/ga4.js  → (빈 출력)
$ md5sum ds/js/ga4.js                   → 879c13d92316991bac1f3affd1060e09
$ git show HEAD:./ds/js/ga4.js | md5sum → 879c13d92316991bac1f3affd1060e09   ← 동일
$ grep -n ZG_VER ds/js/ga4.js           → 22:  var ZG_VER = '2.001';
```
**무변경 확정.** 오케스트레이터 확인과 일치한다. 개발이 §0 각주에서 "한 글자도 건드리지 않았다"고 한 것도 사실이다.

---

## 2. dataLayer diff 재실행 (팀장 확인 항목 2)

### 2-1. 개편 전 vs 2차 개편 후 — **5건 / 5건, 파라미터 완전 일치**

시나리오(1차와 동일): 로드 → 2 box 선택(전=네이티브 select / 후=구성 카드 탭) → 하단바 장바구니 →
바로구매 → 옵션 레이어 → **(2차 추가)** 신규 인터랙션 4종 → 이미 담긴 카드 재탭.

| # | 이벤트 | 개편 전 | 2차 후 (ga4 선실행) | 2차 후 (detail-ui 선실행) | 파라미터 diff |
|---|---|---|---|---|---|
| 1 | `view_item` | 1 | 1 | 1 | **없음** |
| 2 | `zg_option_select` | 1 | 1 | 1 | **없음** |
| 3 | `add_to_cart` | 1 | 1 | 1 | **없음** |
| 4 | `zg_buy_click` | 1 | 1 | 1 | **없음** |
| 5 | `zg_option_layer_open` | 1 | 1 | 1 | **없음** |
| | **합계** | **5** | **5** | **5** | **소실 0 · 신규 0 · 중복 0** |

`item_variant` = `"BEST 젠제네틱스 붓기파우더 2 box (무료배송)"` — `[지금 24%▼]`·🔥·`(+33,100원)` 잔존 **0**.
`send_to`·`zg_ver` **전수 존재**. PII **0건**. `value:68000` / `value_source:"screen_total"`.

### 2-2. 2차 신규 인터랙션 — 오발사 0건

| 클릭 대상 | 발사 이벤트 |
|---|---|
| `.zg-more` (상세 더보기, `aria-expanded` 추가됨) | **0** (의도대로 무발사 — §5 N-1 로 명세) |
| `.zg-chip` (라벨 「자주 묻는 질문」으로 변경) | **0** |
| `.zg-bar__sum` / `.zg-bar__soldout` | **0** |
| **이미 담긴 카드 재탭**(토스트 경로) | **0** — `pick()` 이 `dispatchEvent` 전에 `return` 하므로 유령 `zg_option_select` 없음 ✅ |
| `.zg-opt-native-btn` | **노드 자체가 삭제됨**(무단 UI 제거) → 검사 대상 소멸 |

### 2-3. 다중 담기 · `tr.add_product` 공존 (F-4 직격 검증)

본품 옵션행 2 + **추가상품행 1**(`tr.add_product`) + `<span>` 없는 행 1 을 동시에 넣고 실행:

```
tr.option_product 2 / tr.add_product 1 / data-zg-nospan 1 / zg-picked-0 클래스: false(정상 해제)
add_to_cart → {"value":238900,"value_source":"screen_total",
               "items":[{"item_id":"11","price":238900,"quantity":1,
                         "item_variant":"BEST 젠제네틱스 붓기파우더 2 box (무료배송)"}]}
item_variant 에 "비타민B"(추가상품) 포함 여부 → 없음 ✅
```
**`ga4.js:174` 의 `tr.option_product` 한정 조회가 `anyRows()` 변경에 전혀 영향받지 않는다.**
추가상품이 매출·옵션명에 섞여 들어가는 오염 **0건**.

> `quantity:1` 인 이유는 §5 N-7 참조 — `<span>` 없는 행을 `ga4.js:182` 가 버리는 **기존 동작**이며,
> 2차 변경 때문이 아니다. 칼륨 11 의 실제 옵션행은 전부 `<span>` 을 갖는다(라이브 마크업 실측).

---

## 3. F별 계측 영향 판정 (팀장 지정 항목)

### F-1 · 상세 이미지 `ec-data-src` → `src` 승격 — **계측 영향 없음 / 문서 높이 재계산 아래**

`ga4.js` 전문 773줄에서 이미지·지연로딩 참조를 전수 검색했다:
`img` / `ec-data-src` / `lazy` / `loading=` / `IntersectionObserver` — **일치 0건.**
(`grep aria` 의 히트는 전부 `v**aria**nt` 문자열이다. 실제 ARIA 참조도 **0건**.)
→ **`ga4.js` 는 이미지에 어떤 방식으로도 의존하지 않는다.** F-1 은 계측을 건드릴 수 없다.

jsdom 실측: `#prdDetail img[ec-data-src]` **14 → 0** / `img[src]` **0 → 14**. `unlazyDetailImages()` 전체가 try/catch.

#### 문서 높이 재계산 (1차 계산의 갱신)

| 값 | 1차 | **2차** | 변화 |
|---|---|---|---|
| 상세 14장 자연 렌더 높이 @390px (25,740px × 390/1080) | 9,294px | **9,294px** | 동일 |
| `.zg-fold` 상한 (모바일 기본 / ≥1024px) | 1,700 / 2,200px | **1,700 / 2,200px** | **변경 없음**(`detail.css:893`, `:1134`) |
| 접기로 숨는 양 (모바일) | 7,594px (81.7%) | **7,594px (81.7%)** | 동일 |
| **접힘 상태에서 실제로 채워지는 높이** | **≈190px ~ 1,700px (불확정)** — IO 가 클립 조상 아래서 교차하지 않아 12장이 미로드 → 높이 0 | **1,700px (상한 도달, 안정)** — `src` 승격으로 네이티브 lazy 가 근접분을 로드 | **최대 +1,510px** |
| F-3 푸터 영역 (1차 `.zg-detail{padding-bottom}` 제거 −104px + `.footer{margin-bottom:max(0,safe-area−18px)}`) | — | 노치 **−88px** / 비노치 **−104px** | |
| **1차 대비 순변화** | — | **약 +1,406 ~ +1,422px** | |
| **개편 전(라이브) 대비 최종** | −7,594px | **−7,594px + (0~16px) ≈ −7,578 ~ −7,594px** | |

**판정:** 접기 상한이 그대로이므로 **R-1 의 근거 수치는 1차와 동일하게 유효하다.**
다만 F-1 로 접힘 문서 높이가 "최악 190px" 에서 "안정 1,700px" 로 올라갔다 — 이는 **개선**이지만
동시에 **분모의 두 번째 이동**이다. 1차와 2차가 각각 배포되는 상황이라면 같은 `2.001` 안에 **세 가지 상태**
(개편 전 / 1차 / 2차)가 섞인다. **`ZG_VER` 분리 필요성이 1차보다 커졌다.**

### F-3 · 푸터 `margin-bottom` — **계측 영향 없음**
`ga4.js` 는 `.footer` 를 참조하지 않는다. 문서 높이 기여분만 위 표에 반영했다(−88 ~ −104px).
`env(safe-area-inset-bottom)` 은 jsdom 에서 계산되지 않으므로 **실측치는 §7 L-1 로 분리**한다.

### F-4 · `#totalProducts{display:none}` 삭제 + `anyRows()` 도입 — **계측 영향 없음**

| `ga4.js` 지점 | 무엇을 보는가 | F-4 영향 |
|---|---|---|
| `:174` `selectedOptionRows()` | `document.querySelectorAll('tr.option_product')` | **없음** — `anyRows()` 는 `detail-ui.js` 전용 빈상태 판정 함수이며 `ga4.js` 와 공유 코드가 아니다 |
| `:170` 주석 | `tr.add_product` 는 **의도적으로 제외** | **없음** — §2-3 실측으로 오염 0 확인 |
| `:177` `input.quantity_opt` | 행 내부 조회 | **없음** |
| `:206` `selectedOptionQty()` | 위 결과 합산 | **없음** |
| `display:none` 규칙 삭제 | — | `querySelector` 는 CSS 가시성과 무관. 어느 쪽이든 영향 없음 |

`readRow()` 가 `data-zg-nospan="1"` 을 DOM 에 **쓴다**(읽기 함수의 부작용)는 점은 확인했으나,
`ga4.js` 가 읽는 속성이 아니고 클래스·구조를 바꾸지 않으므로 계측 무해다.

### F-6 · `@js(/ds/js/detail-ui.js)` 가 스킨 `@js` 앞으로 이동 — **계측 관점 판정: 실행 순서 무관, 안전**

**(가) 두 순서를 실제로 다 돌렸다. 결과가 완전히 같다.**

| 실행 순서 | 이벤트 | 파라미터 |
|---|---|---|
| `ga4.js` → `detail-ui.js` | 5건 | 위 §2-1 과 동일 |
| `detail-ui.js` → `ga4.js` | 5건 | **바이트 단위 동일** |

이유(코드 근거): `ga4.js` 는 `detail-ui.js` 가 만드는 노드(`.zg-opt` `.zg-sum` `.zg-bar__*` `.zg-picked-head`)를
**하나도 읽지 않는다**(§2 셀렉터 표 18개 전수). 반대로 `detail-ui.js` 는 `ga4.js` 가 읽는 노드를
**삭제·이동·재작성하지 않는다**(`#totalPrice` 는 형제 삽입 + 시각 클립뿐). 두 파일은 DOM 상 교집합이 없다.
또한 클릭은 **캡처 단계(`ga4.js` document capture)가 등록 순서와 무관하게 항상 버블 핸들러보다 먼저**이므로,
카드 핸들러와의 상대 순서도 등록 순서에 좌우되지 않는다.

**(나) 다만 F-6 이 새로 만든 위험이 하나 있다 — 확인했고, 막혀 있다.**
1차에는 `detail-ui.js` 가 생 `<script>` 로 **독립 로드**됐다. 2차에는 `@js` 로 바뀌어
**`ga4.js`(head.html `:52` 의 `@js`)와 같은 `optimizer_user.php` 번들에 concat 된다.**
→ 앞 파일이 **최상위에서 throw 하면 뒤 파일이 통째로 실행되지 않는다.**
`detail-ui.js` 가 앞이고 던지면 **GA4 전면 블랙아웃**이 된다. 그래서 훼손 DOM 9종으로 최악 순서를 강제 실행했다:

| 훼손 케이스 | `detail-ui.js` throw | `ga4.js` throw | 발사 |
|---|---|---|---|
| 정상(2차) | no | no | `view_item` |
| `#prdDetail` 제거 | no | no | `view_item` |
| `.productOption` 제거 | no | no | `view_item` |
| `#totalProducts` 제거 | no | no | `view_item` |
| `#totalPrice` 제거 | no | no | `view_item` |
| `.mobile-fix-footer` 제거 | no | no | `view_item` |
| `#freeShipGuide` 제거 | no | no | `view_item` |
| 옵션 `select` 제거 | no | no | `view_item` |
| **완전 빈 body** | no | no | (없음 — 설계대로 침묵) |

**9/9 예외 0건.** `detail-ui.js` 의 최상위 실행문은 `unlazyDetailImages()`(전체 try/catch) ·
`captureDelivery()`(전체 try/catch) · `ready(fn)` 등록뿐이고, `init()` 도 `try{init()}catch(e){}` 로 감싸여 있다.
전역 이름 충돌도 없다 — `detail-ui.js` 전역은 `ZG_OPT_MODE`/`ZG_OPT_AUTOSELECT`/`ZG_MOVE_TOTALPRODUCTS`/
`window.__ZG_DETAIL_UI__` 뿐이고 `ga4.js` 는 전역을 **하나도** 내보내지 않는다(`ZG_VER` 은 IIFE 내부).

**(다) 오세진(QA 카페24 실환경)과의 관할 — 결론이 충돌하지 않는다.**
나는 **번들 내 실제 앞뒤를 판정하지 않았다.** 계측 관점의 답은 "**어느 쪽이든 이벤트가 동일하다**"이고,
이것은 오세진이 바이트 오프셋으로 어떤 순서를 확정하든 뒤집히지 않는다.
**단 한 지점만 겹친다: (나)의 concat 실패 전파.** 나는 "앞 파일이 던지지 않음"을 실측으로 막았고,
오세진이 **실제 번들 순서**(ga4.js 가 앞인지 뒤인지)를 확정해 주면 이 항목은 완전히 닫힌다.
→ **오세진에게 넘기는 질문 1건:** *`optimizer_user.php` 안에서 `/ds/js/ga4.js` 와 `/ds/js/detail-ui.js` 중 어느 것이 먼저인가.*

### 접근성 A안 `aria-pressed` → `data-zg-on` — **계측 영향 없음**
`ga4.js` 의 ARIA/`data-zg` 참조 **0건**(전수 검색). jsdom 실측: `[aria-pressed]` **0** / `[data-zg-on]` **4**.
`ga4.js` 는 카드 상태를 보지 않고 `tr.option_product` 만 본다 → 표기 방식 변경과 무관.

---

## 4. `ga4.js` 셀렉터 × 개편 전/2차 후 매칭 (전수 재실행)

| # | 셀렉터 | 용도 | 개편 전 | 2차 후(390px) | 판정 |
|---|---|---|---|---|---|
| 1 | `.infoArea` | 전 파라미터의 뿌리 | 1 | 1 | ✅ |
| 2 | `.infoArea input[name="quantity"], input#quantity, .quantity input` | `currentQty()` | 2 | 2 | ✅ |
| 3 | `select[name^="option"]:not([name^="addproduct"])` | `currentVariant()` | 1 | 1 | ✅ |
| 4 | `tr.option_product` | 진실원장 | 1 | 1 | ✅ |
| 5 | `input.quantity_opt, input[name^="quantity_opt"]` | 행 수량 | 1 | 1 | ✅ |
| 6 | `p.product span` | 행 옵션명 | 1 | 1 | ✅ |
| 7 | **`#totalPrice .total strong, … em`** | **총액 판독** | **2** | **2** | ✅ |
| 8 | `[onclick],a,button,div,span` | 클릭 위임 1차 | 660 | **712** | ✅ 무해 (1차 714 → 「옵션 직접 선택」 삭제로 −2) |
| 9 | `.jsLayerBtn` | 옵션 레이어 | 1 | 1 | ✅ |
| 10 | `.btnKakao, [onclick*="kakaosyncLogin"], …` | `login` | 0 | 0 | ✅ (상세엔 없음이 정상) |
| 11 | `.kakao_link, .fake-sns-login` | `login` 제외 | 1 | 1 | ✅ |
| 12 | `a[href*="product_no="]` | `view_cart` | 19 | 19 | ✅ |
| 13 | `.ec-base-prdInfo, tr, li, .xans-record-` | `view_cart` 행 | 338 | 338 | ✅ |
| 14 | `[module="Order_result"], #mCafe24Order, .xans-order-result` | `purchase` | 0 | 0 | ✅ (주문완료 전용) |
| 15 | `modSel('normalresultlist')` | `purchase` 목록 | 0 | 0 | ✅ (주문완료 전용) |
| 16 | `.totalPay` | `payBoxAmount()` | 0 | 0 | ✅ (주문서/완료 전용) |
| 17 | `#orderFixItem .btnSubmit` | `checkoutTotal()` | 0 | 0 | ✅ (주문서 전용) |
| 18 | `.refer, .displaynone` | `numFromEl()` | 79 | 79 | ✅ |

**매칭이 줄어든 셀렉터 0개.** 총액 판독 규칙(`detail-ui.js:74` = `ga4.js:233`)도 2차에서 변경 없음.
실측 `#totalPrice .total` = `"68,000원 (1개)"` → `screenTotal()` = `68000`, `"238,900원 (4개)"` → `238900`.

---

## 5. 중복·차단 재검사 (신규 노드 재집계)

| 검사 | 1차 | **2차** |
|---|---|---|
| `stopPropagation` / `stopImmediatePropagation` / `preventDefault` | 0 | **0** (grep 전수) |
| 기존 노드 `remove()` / `innerHTML` 갈아엎기 | 0 | **0** |
| 개편이 만드는 신규 `zg-*` 노드 | 61 | **66** (K-10 칩 라벨·`zg-veil` 구조·상태 텍스트 추가, 네이티브 버튼 −2) |
| 그중 **6단 이내 조상에 `onclick` 보유** | 0 | **0** ✅ |
| `product_submit` 클릭 타깃 4종 + `.jsLayerBtn` 생존 | 5/5 | **5/5** ✅ |
| `pointer-events:none` 신규 | `zg-sheet-open` 버튼줄 · 토스트 | **+ `.zg-veil`**(P2-b, `.zg-more` 만 `auto`) |

`.zg-veil{pointer-events:none}` 은 **1차보다 안전해진 변경**이다. 1차에는 베일 190px 이 상세 하단을 덮어
그 아래 클릭을 삼켰다. 2차에는 통과시킨다. 그 영역엔 `onclick` 노드가 없으므로 신규 발사도 0이다.

`watchOptionSource()`(신규 MutationObserver)는 `buildOptionCards()`/`refresh()` 만 호출하고
**`pick()` 을 절대 호출하지 않는다** → 합성 `change` 를 만들 수 없다 → 유령 `zg_option_select` **불가**.
40회/15초 상한으로 자동 해제된다.

---

## 6. `ds/css/price.css`(2차 신규 검수 대상) — 계측 영향 없음
추가된 것은 `.zg-detail .ds_benefit{배경·테두리·라운드·패딩}` 과 `.ds_benefit* .price{폰트·tabular-nums}` 뿐이다.
`ga4.js` 는 `.ds_benefit` 계열을 참조하지 않고, 금액은 `#totalPrice`/`data-price` 에서만 읽는다. **무해.**

---

## 7. 라이브 확인 필요 (헤드리스로 판정 불가 — PASS 목록에 섞지 않았다)

| # | 항목 | 왜 못 했나 | 담당 |
|---|---|---|---|
| L-1 | 접힘 상태 실제 `scrollHeight` 와 `zg_scroll_depth` 퍼센트 이동폭. `env(safe-area-inset-bottom)` 실측 | jsdom 무레이아웃 | 배시우(실기) |
| L-2 | 바텀시트 열림 중 시트 안 `#fixedActionButton` 클릭 → `add_to_cart`/`zg_buy_click` 정상 발사 | 시트 전환은 스킨 번들이 해야 일어남 | 신아린 실기 + 배시우 |
| L-3 | 카페24 실 옵션 스크립트가 카드 탭 후 `change` 를 재발사하지 않는지 | 목으로만 검증 | 배시우(실기) |
| L-4 | **`optimizer_user.php` 안에서 `ga4.js` vs `detail-ui.js` 의 실제 앞뒤** | 번들은 카페24 서버가 만든다 | **오세진** |
| L-5 | `/ds/js/ga4.js` 가 v2 적용 후 **200** 인지 (현재 라이브 404, 정상) | 스킨 미배포 | 오세진 |
| L-6 | `view_cart` · `begin_checkout` · `purchase` · `login` 회귀 | 개편 범위 밖 페이지(파일 무변경) | 배시우(배포 후 실주문 1건) |
| L-7 | `G-84HNK1MRBG` / `GTM-5W5PV3CD` 내부 태그 | 추적계획 §5 미해결, 이번 개편과 무관 | — |

---

## 8. 대표님 승인 안건 (확정본) — 팀장이 그대로 올린다

### 8-1. 무엇을 고치나 — `ds/js/ga4.js` **3곳, 순증 8줄**

**① `ZG_VER` 상향 (1줄 교체) — `ds/js/ga4.js:22`**
```js
/* 변경 전 */  var ZG_VER = '2.001';
/* 변경 후 */  var ZG_VER = '2.002';   /* 2.001 = GA4 계측 도입판 / 2.002 = 상세 개편(접기·구성카드)판 */
```

**② `zg_scroll_depth` 에 접힘/펼침 구분 추가 (1줄 삽입) — `ds/js/ga4.js:759` 뒤**
```js
/* 변경 전 (755~760) */
          sendAlways('zg_scroll_depth', {
            percent: m,
            seconds: Math.round((Date.now() - start) / 1000),
            page_kind: isProduct ? 'product' : 'home',
            item_id: isProduct ? currentPrdNo() : ''
          });
/* 변경 후 — 마지막 항목 뒤에 한 줄 추가 */
          sendAlways('zg_scroll_depth', {
            percent: m,
            seconds: Math.round((Date.now() - start) / 1000),
            page_kind: isProduct ? 'product' : 'home',
            item_id: isProduct ? currentPrdNo() : '',
            detail_expanded: document.querySelector('.zg-fold.zg-open') ? 1 : 0
          });
```

**③ 「상세 정보 모두 보기」 펼침 이벤트 신설 (6줄 삽입) — `ds/js/ga4.js:733` 뒤 (§6 로그인 블록과 §7 스크롤 블록 사이)**
```js
  /* ================================================================== *
   * 6-B. 상세 접기 펼침 — 이번 개편의 핵심 가설을 재는 유일한 신호
   * ================================================================== */
  document.addEventListener('click', function (e) {
    var m = e.target && e.target.closest ? e.target.closest('.zg-more') : null;
    if (!m) return;
    send('zg_detail_expand', { item_id: currentPrdNo() });
  }, true);
```
> `send()` 를 쓰므로 페이지당 1건이다. 실패해도 기존 이벤트에 영향이 없다(다른 블록과 독립).

### 8-2. 주문서·주문완료 2페이지 — **전체 재붙여넣기는 불필요하다. 한 줄에 `?v=2002` 만 붙인다**

> **판정서 정정:** 그 두 페이지에 들어간 것은 `ga4.js` **사본이 아니라 `<script src="/ds/js/ga4.js">` 참조**다
> (`개발_수정보고_2026-09-06.md` §3 원문 확인). 따라서 스킨 파일에서 `ZG_VER` 만 올리면 **버전은 자동으로 따라간다.**
> 문제는 그 `<script>` 줄에 **버전 표시(`?v=`)가 없다**는 것 하나뿐이다.

**대표님이 읽으실 문장 (2줄):**
> 주문서·주문완료 두 페이지는 스킨을 타지 않아 `/ds/js/ga4.js` 를 직접 불러오는데, 그 줄에 버전 표시가 없어
> **이미 방문했던 손님의 브라우저는 예전 파일을 그대로 씁니다.** 그러면 같은 손님의 '상품 조회'는 새 버전(2.002),
> '결제 완료'는 옛 버전(2.001)으로 갈려 **주문서→결제완료 전환율의 분자와 분모가 어긋납니다.**
> 관리자 › 디자인 관리 › 스마트 주문서의 `orderform.html` · `order_result.html` **두 파일에서
> `<script src="/ds/js/ga4.js"></script>` 한 줄만 `<script src="/ds/js/ga4.js?v=2002"></script>` 로 바꿔 주시면 됩니다.**
> 나머지 줄(결제 뱃지·포기방지 레이어 포함)은 손대지 않습니다.

### 8-3. 신규 이벤트 — 이번 판(2.002) 필수 vs 다음 판(2.003)

| | 이벤트 | 판정 | 이유 |
|---|---|---|---|
| **N-1** | `zg_detail_expand` | **이번 판 필수** | 이번 개편의 핵심 가설이 "상세를 접으면 CTA 도달이 빨라진다"이다. **펼침률 없이는 개편의 성패를 판정할 수 없다.** (8-1 ③) |
| **N-2** | `zg_scroll_depth.detail_expanded` | **이번 판 필수** | 없으면 스크롤 퍼센트가 접힘/펼침 두 상태의 평균이라 해석 불가. R-1 과 한 묶음 (8-1 ②) |
| N-3 | `zg_guide_chip_click` | 다음 판 | 있으면 좋지만 없다고 개편 판정이 막히지 않는다 |
| N-4 | `zg_option_duplicate_tap` | 다음 판 | `multi` 확정의 부작용 크기 측정용. `detail-ui.js` 가 `CustomEvent('zg:dupe-tap')` 를 쏘고 `ga4.js` 가 듣는 방식 권장 |
| ~~N-5~~ | ~~`zg_option_native_open`~~ | **철회** | 2차에서 「옵션을 직접 선택」 버튼이 삭제됐다(무단 UI 제거). 잴 대상이 사라졌다 |
| N-6 | 추가상품 부착률 | 다음 판 | 라이브 select 가 `name="addproduct_option_name_63"` 이라 `ga4.js:348` 의 `/^option/` 에 안 걸린다. **개편 회귀가 아닌 기존 공백.** `li[data-zg-picked="1"]` 훅이 이미 있다 |
| **N-7** | **`<span>` 없는 옵션행 대응** *(2차에서 새로 발견)* | 다음 판 · **8종 전개 전에는 필수** | `ga4.js:182` 의 `if (v) out.push(...)` 가 `p.product span` 이 없는 행을 통째로 버린다 → 그 상품은 `quantity` 가 1 로 고정되고 `item_variant` 가 빈다(§2-3 실측). **칼륨 11 은 전 행이 `<span>` 을 가져 무관.** 개발이 이번에 만든 `tr[data-zg-nospan="1"]` 표식을 폴백으로 쓰면 된다 |

---

## 9. 2차 판정 요약

| 체크 | 결과 |
|---|---|
| `ga4.js` 무변경 (md5 대조) | ✅ |
| 기존 이벤트 소실 | ✅ **0건** (5/5, 파라미터 완전 일치, 실행 순서 2가지 모두) |
| 총액 판독 파손 | ✅ **없음** (`(N개)` 제거 유효, 1개/3개/4개 실측) |
| 중복 발화 | ✅ **0건** (재탭 유령 이벤트 0 · `watchOptionSource` 합성 change 불가) |
| `stopPropagation`/`preventDefault` 차단 | ✅ **0건** |
| 신규 노드 66개의 `onclick` 조상 오발사 | ✅ **0건** |
| F-1 / F-3 / F-4 / F-6 / 접근성 A안 / `price.css` | ✅ **계측 영향 0** |
| 번들 concat 예외 전파 위험 (F-6 동반) | ✅ **막힘** — 훼손 DOM 9/9 예외 0건 |
| **1차 R-1 (`ZG_VER`)** | ⏸ **승인 대기 이월. 여전히 유효하며, F-1 로 분모가 한 번 더 이동해 필요성이 커졌다** |

**2차 판정: 2차 변경분 PASS (신규 계측 결함 0건).**
**게이트는 아직 열지 않는다** — R-1 이 미해소 상태다(대표님 승인 안건, §8).
대표님 지시(*"검수 3회이상씩하고 말해줘"*)에 따라 **3차에서 델타가 아니라 전체를 다시 본다.**

### 3차 검수 예고 스코프 (델타가 아니라 전량)
1. `ga4.js` 773줄 셀렉터 **재추출**(이번 표를 신뢰하지 않고 다시 뽑는다) × 개편 전/후 재대조
2. dataLayer 전 시나리오 재실행 + **상품 9종 전체**(11/13/16/63/64/71/데이팩 3종)에서 `view_item` 발사 확인
3. `ZG_VER` 승인 결과 반영분 검증(승인 시 2.002·N-1·N-2 실발사 캡처)
4. 오세진 L-4 회신(번들 실제 순서) 반영
5. `view_cart`/`begin_checkout`/`purchase`/`login` 회귀 재확인 — 파일 무변경이어도 다시 돌린다

*QA(계측) 배시우 · 2026-09-07 (2차)*
