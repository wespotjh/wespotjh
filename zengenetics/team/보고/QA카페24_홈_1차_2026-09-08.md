# QA(카페24 실환경) — 홈(메인) 카페24 변환 **1차 판정서**

**작성:** 개발팀 QA(카페24 실환경) **오세진** · 2026-09-08
**동결 해시(독립 재계산 4/4 일치, 전달본 4쌍 일치):** `index.html` `1422550e` 15,439 B · `home.css` `19cd2e17` 16,940 B · `home-blocks.js` `c02f8b2c` 8,541 B · `home-hero.js` `42cc8313` 34,696 B · 이미지 86장 1,461,410 B (`이미지_목록.txt` md5 86/86 = 폴더 = 프로토타입 디코드 페이로드) · git `d011c08`
**칼륨 7파일:** 불변 확인(`7044b38e` 포함).
**증거:** 라이브 홈 3 UA 재취득 · 홈 전용 번들 4벌 실물 · `main_js.html` 인라인 원문 48,650 B · **헤드리스 Chromium 픽스처**(라이브 홈에서 `<main>` 본문만 우리 `index.html` 렌더로 교체, 이미지 86장·three.js 로컬 서빙, 우리 CSS 를 사용자 번들 **1번(가장 불리)** 자리에 주입) — 모바일 UA 정상/3D 차단 2회 + **변경 전 기준선 1회**. 데스크톱 1280 실행은 타임아웃(2회) → 실기 분리.
**범위:** 플랫폼 동작만. 레이아웃 수치·카피·계측·SEO 는 각 게이트 소관.

---

# 판정: **PASS (조건부)** — 코드는 플랫폼에서 돈다. 배포표 보완 2건 + 문서 정정 1건.

| 조건 | 내용 | 파일 수정 |
|:--:|---|:--:|
| **C-1** | 배포표 ⑥ 미리보기에 **「이미지가 404 로 보일 수 있다 — 적용 스킨 해석 문제이지 업로드 실패가 아니다」** 를 넣는다 (§D-3) | 문서만 |
| **C-2** | 배포표 ⑦·미리보기에 **`main_js.html` 콘솔 오류 1건은 알려진 것**이라고 적는다 (§C-1) — 대표님이 F12 에서 놀라지 않도록 | 문서만 |
| **C-3** | 개발 보고 §6 「우리 JS 가 `main_js.html` 보다 먼저」는 **거꾸로다**(§C-2). 결과에 영향 없으나 사실로 고친다 | 문서만 |

REJECT 사유(없는 경로·변수 / `@css` 순서 오류 / M-1 실패 / 배포 중 판매·홈 파손 / 되돌리기 불가)는 **전부 해당 없음.** 아래 순서대로 근거를 댄다.

---

# A. 템플릿 엔진

| 항목 | 결과 | 근거 |
|---|---|---|
| `@layout` | **`/layout/basic/main.html` 그대로** (1행) | 원본과 동일 |
| `@import` 14 → **2** | `moa/js/main_js.html`(247행) · `moa/import/board/rv_frame.html`(249행) — **둘 다 라이브에서 렌더 확인**(인라인 48,650 B · `.rv-frame` 5회) | 경로 실재 |
| **`@css(/ds/css/home.css)` 위치** | **21행 = 기존 `@css` 5개 뒤 → 사용자 CSS 번들 6번** (선검수 §e-2 요구 그대로) | `moa/css/main.css`·`ds/css/main.css` 와의 동률에서 이긴다 |
| `@js` 2개 위치 | 22·23행 = 기존 `@js` 7개 뒤 → 번들 **8·9번** (개발 보고의 「4·6번」은 오기; 순서 방향은 같다) | — |
| 새 `{$변수}` | **0개.** 정적 마크업뿐. 주석 안 `{$` **0** | 치환 사고 경로 없음 |
| 매니페스트 `window.ZG_HOME_IMG` | `base:"/ds/image/"` + 파일명 86 = `이미지_목록.txt` 86 = 폴더 86, **차집합 0·중복 0**, md5 86/86 = 프로토타입 페이로드 | 1:1 확인 |
| 본문 직접 참조 4건 | `zg-home-bg-dawn.jpg`(인라인 `<style>` 루트절대) · 스틱 v 3장(`<img src>`) — 전부 매니페스트 안 | `url()` 루트절대 ✔ (선검수 T-3) |
| `home.css` 안 `url()` / `!important` | **0 / 0** (`!important` 1건은 10행 주석) · 중괄호 142/142 | 이미지 경로가 CSS 에 없다 |
| 셀렉터 스코프 | 137블록 중 `.zg-` 접두 아닌 것 **5 = 전부 `@keyframes` 퍼센트** → 전역 유출 0 | R2 |
| `node --check` | 2/2 통과 | |
| **파일명 보존 전제** | ⚠ **미증명.** `/ds/image/ico_price2.webp` 가 그 이름인 것은 **스킨 구매본에 동봉된 파일**의 증거이지 업로더가 이름을 보존한다는 증거가 아니다(`/about.html` 200 ≠ 생성 가능, 과 같은 오류). 배포표에 「이름이 바뀌었으면 목록을 알려 달라」가 이미 있어 **절차로 흡수됨** | 실기 |

# B. 렌더 환경

| 항목 | 결과 |
|---|---|
| **UA 3벌 diff (현행 라이브)** | iPhone vs 데스크톱 **22줄** = `CAFE24.MOBILE_DEVICE` 1 + `mobile_flag`/`aLogData` 2 + **진열 카드 장바구니 아이콘 8쌍**(`btn_list_cart_mobile.png` vs `.gif`, 전부 `xans-product-listmain` 안). iPhone vs Android **0줄** |
| 진열 삭제 후 | 8쌍이 사라지므로 **환경 플래그 3줄만 남는다** — 개발 §6 주장 성립. 우리 마크업에 UA 의존 `{$변수}` 0 |
| 로그인/비로그인 | `statelogoff` 5회 전부 헤더·aside(118,867~134,585) — `<main id="contents">`(141,147) **밖**. `statelogon` 0. 우리 마크업 영향 **0** |
| `<body id="main">` | 커스텀 아님, 홈은 `id="main"` 유지 → `#main .header{height:0}` 오버레이 헤더 그대로 (설계 §2-1) |
| **3D 폴백 `zg-no3d`** | **실증.** three.js 차단 픽스처: `rootCls = zg-home zg-no3d` · `THREE undefined` · `.zg-fallback display:flex` · **트랙 높이 757 = 100vh** · `capA opacity 1` · body 오염 0 · 가로스크롤 0 |
| `prefers-reduced-motion` | 코드 경로 존재(`home-hero.js:68-69` → `zg-reduced`, CSS 2규칙). Chromium 강제 미디어 미실행 → **실기** |
| cdnjs 차단 | = 3D 폴백 경로와 동일(`defer` 스크립트 실패 → DOMContentLoaded 는 뜬다 → `typeof THREE === 'undefined'`). CSP 헤더 없음 재확인 |
| 3D 정상 | `THREE object` · 프레임 72 · 배경 5장 전부 칠해짐(`paintRest` 1.5 s) · `h1` 1 |

# C. 로딩 순서 · 번들

## C-1. ★ 신규 발견 — `main_js.html` 이 삭제된 배너를 찾다가 **TypeError 를 낸다** (P2 · 조건 C-2)

```
Uncaught TypeError: Cannot read properties of undefined (reading 'pause')
```
- **변경 전 기준선(현행 라이브 홈 픽스처)에는 없다.** 변경 후에만 난다 → **이번 변경이 만든 새 오류다.**
- 원인(원문 확인): `main_js.html` 이 `new Swiper('.jsMainSlidePC')` / `new Swiper('.jsMainSlideMobile')` 를 만드는데 배너 마크업이 삭제돼 **Swiper 4.5.1 이 `undefined` 를 돌려주고**, 이어지는 `if (chkAlone) { … runMainBannerSlide*.autoplay.pause(); }` 에서 던진다. PC 경로는 `$(function(){ MainBannerSlidePC(); })`(jQuery ready), 모바일 경로는 `window.onload` 안이다.
- **영향 범위(원문 정독):** 모바일 `window.onload` 에서 그 뒤에 남은 문장은 `subBannerSlide(); initBestProductSwiper();` **둘뿐** — 둘 다 **삭제된 섹션**(서브배너·BEST 진열)의 초기화라 건너뛰어도 잃는 것이 없다. **팝업(`chkMPopup`)은 별도 `$(function)` 이라 무관** — 픽스처에서 팝업 마크업·감시 인터벌 정상.
- **판정: 기능 손실 0, 콘솔 오류 1건/로드.** 다만 카페24 자체 오류 수집기(`js-error-tracer-api`, `collectWindowErrors:true`)에 매 방문마다 찍힌다. 개발 보고 「`pageerror` 0건」은 **사실이 아니다**(그쪽 픽스처가 배너 마크업을 남겼거나 콘솔을 안 봤을 것).
- 처리 권고(택1, 이번 판 필수 아님): ⓐ 그대로 두고 배포표에 「알려진 오류 1건」 명기(**C-2**) ⓑ `index.html` 에 `<div class="jsMainSlideMobile jsMainSlidePC" hidden><div class="swiper-slide"></div></div>` 스텁 1줄 — Swiper 가 생성되어 던지지 않는다. **스텁은 md5 를 바꾸므로 다음 판.**

## C-2. 실행 순서 — 개발 주장이 **거꾸로다** (조건 C-3, 결과 영향 0)

| 대상 | 라이브 오프셋 | 실행 시점 |
|---|--:|---|
| `main_js.html` 인라인 `<script>` (48,650 B) | **174,284** | 콘텐츠 파싱 중 — **먼저** |
| `window.ZG_HOME_IMG` 인라인 | 본문 상단 | 그 앞 ✔ |
| 사용자 JS 번들(`home-blocks.js` 8번 · `home-hero.js` 9번) | **269,775** | `</body>` 직전 — **나중** |
| three.js `defer` | — | 파싱 종료 후, `DOMContentLoaded` **직전** |

개발 §6 *"사용자 JS 번들(head) → `main_js.html`(본문). 우리가 먼저"* 는 틀렸다 — **JS 번들은 `<head>` 가 아니라 body 끝**이다(칼륨 1차에서 확정한 사실). `main_js.html` 의 `$('body').removeAttr('class')`(5회) · `window.onload =` 할당이 **우리보다 앞**에 실행된다.
**그래도 M-1·M-2 는 성립한다:** ① 우리 상태 클래스는 `.zg-home` 루트에 붙어 `body` 를 지워도 무관(실증: `bodyWipe` 후 `rootCls = zg-home` 유지) ② 우리는 `DOMContentLoaded` 만 쓴다(`home-hero.js:717`). `initHero` 가 `DOMContentLoaded` 에서 돌고 `defer` three.js 는 그 **직전**에 실행되므로 `typeof THREE` 판정도 옳은 시점이다.

## C-3. R17 — 딜레이 없는 `setInterval` : **기존 동작, 우리 회귀 0**

| | 변경 전 (현행 라이브 픽스처) | 변경 후 |
|---|---|---|
| `setInterval` 총 등록 | 6 | 6 |
| 살아남은 것 | **2 (`ms=undefined`)** | **2 (`ms=undefined`)** |
| `#scriptPC` 존재 | 없음 | 없음 |

`repeatTT = setInterval(function(){ if (document.getElementById('scriptPC')) {…clearInterval…} })` — **딜레이 인자 없음**(=최소 4 ms 마다), `#scriptPC` 가 나타날 때만 해제. **변경 전에도 같은 2개가 살아 있다** → 우리가 만든 부하가 아니다. 비용은 4 ms 마다 `getElementById` 1회(미스) — 자체는 가볍지만 메인스레드를 계속 깨운다. 386vh 히어로의 rAF 와 경쟁하는 정도는 **실기(대표님 폰)** 로만 판정된다. 우리 코드의 `setInterval` 은 **0개**.

## C-4. 조건 ④ 감시자 vs 카페24 `MutationObserver` — **충돌 없음**

- 픽스처 홈에서 생성된 MO 는 **1개 = 우리 것**. 카페24 `CAFE24.lazyload()` 의 MO 는 `img[ec-data-src]` 가 있을 때만 생성되는데 홈에는 **0개**(우리 마크업 `ec-data-src` 0, 진열 삭제). `#freeShipGuide` MO 는 상품 상세 전용.
- 설령 공존해도 MO 는 서로 간섭하지 않는다. 우리 콜백은 `body`/`html` 의 class 를 **쓰지 않고 읽기만** 한다 → 루프 없음.
- **실증:** `body.eMobilePopup` 주입 → `frozen:true`, `pageYOffset 1083 → 0`(body fixed 로 무너짐, 예상대로) → 제거 → **`pageYOffset 1083 정확 복원`**, `frozen:false`. 3D 차단 상태에서도 동일.

## C-5. sticky · 오염 (조건 ①~③)

`.zg-hero-stage` **`position:sticky`, 히어로 50 % 지점에서 top = 0** · 트랙 2,922 px = 386vh @757 · `.zg-home` `overflowX/Y visible · transform/filter/contain none` · `html/body overflow visible` · `#contents` `max-width 100% · padding 0` · **가로 스크롤 0** · 제품 블록 95 % 에서 `v0 = 525` 카운트업. 우리 CSS 를 **번들 1번(최악)** 에 넣고도 성립 → 실제 6번에서는 더 유리하다.
(픽스처 뷰포트는 기기 에뮬레이션 없이 485×757 로 잡혔다 — **390 정밀 레이아웃은 신아린 Playwright 소관**, 여기서는 구조·순서·오류만 본다.)

# D. 배포 가능성

## D-1. 순서·중단점 — 개발 `00_읽어주세요` 가 내 칼륨 4차 형식을 그대로 썼다. **큰 틀 유지, 아래 3곳만 고쳐 재작성했다**(전달 폴더에 반영).

| 순 | 대상 | 멈췄을 때 |
|:--:|---|---|
| ① 이미지 86 → `ds/image/` | **변화 0** |
| ②③④ `home.css`·`home-blocks.js`·`home-hero.js` [화면 추가] | **변화 0** (아무도 안 부른다) |
| ⑤ `index.html` | **여기서 홈이 바뀐다** |

**⑤ 를 ①~④ 보다 먼저 저장하면:** `@css/@js` 가 없는 파일을 가리킨다 → 카페24가 그 항목만 버리면 **스타일·스크립트 없는 홈**(마크업은 정적이라 텍스트·링크·3 스틱 이미지·CTA 는 보이고 **판매 링크는 전부 동작** — 빈 화면은 아니다, 히어로 캡션 5개가 세로로 다 펼쳐진 긴 페이지가 된다), 번들 생성이 실패하면 **홈의 사용자 CSS 24개·JS 13개가 통째로 사라진다**(장부 A-7 미해소 — 헤더·햄버거·팝업까지 죽는다). 이미지만 없으면 배경·프레임 빈칸(깨진 이미지 아이콘 78개). **순서로만 회피한다.**

## D-2. 이미지 86장 업로드 — 판정 불가 항목, **절차로 낮춘다**

편집기 다중선택·용량 상한은 외부에서 확인할 수 없다(칼륨·브랜드스토리와 동일). 파일당 최대 286 KB, 총 1.46 MB 라 용량은 문제될 이유가 없다. **배포표에 「20개씩 5번, 매번 목록에서 `zg-home-` 개수 세기(20/40/60/80/86)」로 바꿨다** — 86회 실패 확률을 1회 실패 시 20개 재시도로 줄인다.

## D-3. ★ 「미리보기가 `ds/` 를 서빙하는가」 — **근거 없음. 실기 항목으로 되돌린다 (조건 C-1)**

개발은 R3(적용 전 검증 불가)을 「미리보기로 대체」했다. **그 대체는 증명되지 않았다:**
- 루트 절대경로 `/ds/…` 는 **적용 스킨(skin4)** 으로 해석된다 — 증거: 대표님이 작업본에 이미 저장한 `zg-ga4.js` 가 `/ds/js/zg-ga4.js` 에서 **404**(칼륨 1차~4차 반복 확인).
- 미리보기는 서버가 skin10 으로 **HTML 을 렌더**하지만, 그 HTML 안의 `<img src="/ds/image/…">`·인라인 `url(/ds/image/…)`·`IMG.base` 는 **브라우저가 origin 기준으로 요청**한다 → 미리보기 세션 쿠키가 정적 파일 서빙에도 적용되는지는 **관측 불가**.
- 반면 `@css`/`@js` 는 `optimizer_user.php?filename=sde_design/skin10/…` 로 **스킨 경로가 박혀** 미리보기에서도 정상이다.
→ **미리보기에서 CSS·JS 는 살고 이미지 86장만 404 로 보일 가능성이 높다.** 이것은 업로드 실패가 아니다. 배포표 ⑥ 에 **「Network 에서 `home.css` 200 인데 `zg-home-bg-dawn.jpg` 404 면 정상 — 이미지는 ⑦ 적용 후에만 확인된다」** 를 넣었다. 미리보기에서 이미지가 **보이면** 그것이 곧 실기 확인이다.

## D-4. 되돌리기 — `9_index_원복.txt` 검증

`9_index_원복.txt` md5 `0ca07bd9` = `git show d011c08~1:cafe24-skin/index.html` **바이트 동일**. 내용에 `head.html`·`zg-ga4` 참조 **0** → `index.html` 만 원복하면 **홈은 원래대로, `head.html` 의 GA4·폰트는 그대로 유지 = 결제 계측이 살아 있다.** 개발 표의 문장 정확. `바디랩 반응형` 재적용(전체 롤백)은 계측도 끈다 — 표에 있음 ✔.

---

# 선검수 조건 F-1~F-13 닫힘표

| # | 조건 | 상태 | 근거 |
|:--:|---|:--:|---|
| F-1 | 새 폴더 0~1개 | **닫힘** | `ds/image/` 평면, 매니페스트 86 = 목록 = 폴더 |
| F-2 | 조건 ④ 신설 | **닫힘·실증** | `home-blocks.js:38-95`, 1083→0→1083 복원, 3초 안전장치 코드 확인 |
| F-3 | 설계서 사실오류 3건 정정 | 미확인 | 설계서 v3 를 이번에 안 읽었다. **코드에는 영향 없음** |
| F-4 | `@css`/`@js` 를 기존 선언 뒤에 | **닫힘** | 21~23행, 번들 6·8·9번 |
| F-5 | UA 3벌 diff | **닫힘** | 22줄 → 3줄(전부 환경 플래그), Android=iPhone |
| F-6 | lazyload ↔ `.zg-home` overflow | **닫힘·실증** | `overflow` 없음, `ec-data-src` 0, MO 1(우리) |
| F-7 | `index.html` 원문 백업 | **닫힘** | `9_index_원복.txt` = 변경 전 md5, 계측 무관 |
| F-8 | diss 560px ≥ 렌더폭 명시 | 부분 | 코드는 안전(프레임 `<img>` 폭 = 컨테이너), 문서 명시는 미확인 |
| F-9 | WebP 시험 업로드 | **미해소 → 실기** | ① 에서 자연 확인 |
| F-10 | 업로더 다중선택·상한 | **미해소 → 절차** | 20개×5회로 완화 |
| F-11 | 진열 8종 삭제 승인 | 닫힘 | 대표님 결정로그 #5 (개발 인용) |
| F-12 | 브랜드스토리 URL / 평점칸 / 진열 존치 | 닫힘 | pill `hidden`(132행) · 밴드 2칸(S-4) · 진열 삭제 |
| F-13 | `main_js.html` 원문 | 부분 | 편집기 원문 아님, **렌더 인라인 48,650 B**. `{$` 0 이라 실질 동일. **그 원문에서 C-1 오류를 찾았다** |

---

# 실기 확인 필요 (PASS 에 섞지 않는다)

| # | 항목 | 왜 |
|:--:|---|---|
| 1 | **미리보기에서 `/ds/image/` 가 서빙되는가** | §D-3. 30초 — Network 탭 |
| 2 | 업로더 `.webp`/`.jpg` 수용 · 다중선택 · 파일명 보존 | 편집기 UI 접근 불가 |
| 3 | 데스크톱(≥1024) 레이아웃 | 내 Chromium 1280 실행이 2회 타임아웃(외부 SSL 대기). 정적 근거(`main#contents{max-width:100%}`·동일 CSS)는 있으나 실측은 없다 → 신아린 Playwright 결과에 의존 |
| 4 | 390 정밀 레이아웃 · iOS(백드롭·홈바·`100svh`) | 기기 에뮬레이션 없음. 대표님 폰 |
| 5 | `prefers-reduced-motion` 경로 | 미디어 강제 미실행 |
| 6 | 검색창 열고 닫기 → 히어로 원위치 (조건 ④ 실물) | 픽스처는 클래스를 직접 주입했다. 실제 검색 UI 경로는 실기 |
| 7 | R17 인터벌의 실기 체감 | 기존 동작이라 회귀는 아니나 히어로와 겹치는 건 처음 |
| 8 | `main_js.html` 오류 1건의 실기 부작용 0 확인 | 원문 정독상 0 — 실물로 팝업 열림·닫힘 확인 |
| 9 | 편집기 붙여넣기 상한 | 최대 34,696 B(전례 45,551 B 이내) |

---

# 마무리

칼륨에서 반려 7건을 만든 함정 — 주석 변수·UA 분기·지연로더·경로 404·번들 순서·구체도·원본폭 — 이 홈에서는 **선검수 단계에서 전부 코드에 반영되어 들어왔다.** 실렌더에서 새로 나온 것은 `main_js.html` 의 콘솔 오류 하나이고, 그것도 삭제된 섹션의 초기화만 건너뛴다.
**코드는 통과. 배포표 3곳만 고친 뒤 대표님께 드려도 된다.** 고친 배포표는 `전달/2026-09-08_홈/00_읽어주세요.md` 에 반영했다.

**QA(카페24 실환경) 오세진** · 2026-09-08
