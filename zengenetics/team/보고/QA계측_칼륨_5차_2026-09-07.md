# ④-B QA(계측) 5차 — **델타 판정** (`detail-ui.js` 옵션명 파싱 재작성분)

**판정: PASS — 계측 회귀 0건**
**작성:** 개발팀 QA·계측 **배시우** · 2026-09-07 (5차)
전량 재검수가 아니라 델타 검수다. 3차 판정서를 대체하지 않는다.

## 해시 (판정 시작 = 종료, 판정 중 파일 변동 0)

| 파일 | 시작 md5 | 종료 md5 | 바이트 |
|---|---|---|---|
| `ds/js/detail-ui.js` | `7044b38e…` | **`7044b38e…`** | 36,541 |
| **`ds/js/zg-ga4.js`** | `251c6804…` | **`251c6804…`** | 41,221 |
| `product/detail.html` | `868784dd…` | 동일 | 45,916 |
| `moa/import/product_detail/detail.html` | `0f392522…` | 동일 | 2,632 |
| `ds/css/detail.css` | `f8095b8b…` | 동일 | 45,551 |
| `ds/css/price.css` | `8d6f577c…` | 동일 | 2,271 |
| `moa/layout/head.html` | `2a64e772…` | 동일 | 3,495 |

`node --check ds/js/detail-ui.js` 통과. **`zg-ga4.js` 무변경 확정.**

---

## 1. `item_variant` 회귀 0 — **13종 전수 통과 (0/13 상이)**

`parseOptionText()` 는 **카드 라벨 전용**이다. `select` 도, `tr.option_product` 도, 옵션 텍스트 원문도 건드리지 않는다.
`zg-ga4.js:cleanVariant()` 와 **호출 경로가 완전히 분리**돼 있음을 코드로 확인하고, 13종 실측으로 확정했다.

| 상품 | `view_item` 전→후 | `zg_option_select` 의 `item_variant` (전=후) |
|---|---|---|
| 11 칼륨 | v=34,900 → 동일 | `젠제네틱스 붓기파우더 1 box` |
| 13 비타민B | v=37,900 → 동일 | `젠제네틱스 활력파우더 1 box` |
| 16 마그네슘 | v=37,900 → 동일 | `젠제네틱스 마그네슘 1 box` |
| **34 증정이벤트(비판매)** | `zg_view_item_unreadable prd=34` → 동일 | (옵션 없음) |
| 60 칼륨 데이팩 | v=14,000 → 동일 | (옵션 없음) |
| 61 마그네슘 데이팩 | v=15,000 → 동일 | (옵션 없음) |
| 62 비타민B 데이팩 | v=15,000 → 동일 | (옵션 없음) |
| **63 붓기부스터** | v=104,000 → 동일 | **`붓기부스터 6+3 (9주 \| 무료배송)`** |
| **64 퍼포먼스부스터** | v=71,900 → 동일 | **`BEST 3+3 (60일 \| 무료배송)`** |
| 71 생애첫구매 | v=8,900 → 동일 | `BEST 생애 첫 1+1 젠제네틱스 붓기파우더 데이팩 2 box` |
| **91 네이버페이 안내(비판매)** | `zg_view_item_unreadable prd=91` → 동일 | (옵션 없음) |
| **93 멤버십 혜택(비판매)** | `zg_view_item_unreadable prd=93` → 동일 | (옵션 없음) |
| 98 칼마비 세트 | v=107,500 → 동일 | `칼+마+비 3set (무료배송)` |

`add_to_cart` 도 13종 전부 개편 전과 동일. **개편 전후가 다른 상품 0개.**
63·64·98 의 `(45%할인 \| 9주 \| 무료배송)` 형식에서 **할인율만 제거되고 수량 표기(`6+3`·`3+3`·`3set`)는 보존**된다 —
`cleanVariant()` 의 R5 규칙이 의도대로 동작함을 13종으로 재확인했다.

**`zg-ga4.js` 가 읽는 노드 영향 없음**: `tr.option_product` 1→1 · `p.product span` 1→1 · `input.quantity_opt` 1→1
(셀렉터 40개 전수 대조에서도 이 세 개 포함 전부 불변).

---

## 2. 중복·차단 재검사 — 4차와 동일, 모두 0

| 검사 | 4차 | **5차** |
|---|---|---|
| `stopPropagation` / `stopImmediatePropagation` / `preventDefault` | 0 | **0** |
| 신규 `zg-*` 노드 | 66 | **66** (증가 없음) |
| **6단 이내 `onclick` 조상 보유** | 0 | **0** ✅ |
| `product_submit` 4종 + `.jsLayerBtn` 생존 | 5/5 | **5/5** ✅ |
| 셀렉터 40개 변동 | 2건 | **동일 2건** — `[onclick],a,button,div,span` 660→712 · `strong` 21→22 (둘 다 3차에서 무해 확정) |

칩이 늘었다는 지적을 실측했다: 11번 `tag 4 / note 0 / free 3 / add 3`, 63·64번 `tag 5 / note 0 / free 3 / add 5`.
**칩은 전부 `<em>`/`<span>` 이고 `onclick` 조상이 없다.** 새 발사 경로 0.

---

## 3. `hasRealPrice()` — **계측 관점에서 안전. 오히려 계측과 1:1로 맞는다**

`hasRealPrice()` 는 `.infoArea[data-price]` 와 `#totalProducts span.quantity_price` 를 **읽기만** 한다(DOM 변경 0).
분기가 바꾸는 것은 **`.zg-bar__sum`(우리가 만든 신규 노드) 의 `style.display`** 뿐이다.

| 상품 | `#totalPrice` | `.total strong\|em` | `#totalPrice .total` 원문 | `.zg-bar__sum` | **`zg-ga4.js` 이벤트** |
|---|---|---|---|---|---|
| 34 | 존재 | **2** | `"0 (0개)"` | `display:none` | `zg_view_item_unreadable` |
| 91 | 존재 | **2** | `"0 (0개)"` | `display:none` | `zg_view_item_unreadable` |
| 93 | 존재 | **2** | `"0 (0개)"` | `display:none` | `zg_view_item_unreadable` |
| **60** | 존재 | **2** | `"0 (0개)"` | **(기본, 유지)** | **`view_item`** |
| **61** | 존재 | **2** | `"0 (0개)"` | **(기본, 유지)** | **`view_item`** |
| 11 | 존재 | **2** | `"0 (0개)"` | (기본, 유지) | `view_item` |

- **`screenTotal()` 영향 0** — `#totalPrice` 는 어느 상품에서도 삭제·이동·수정되지 않았고 `.total strong|em` 이 **6/6 모두 2개**로 유지된다.
- **판별자가 계측과 정확히 일치한다.** 금액 줄을 접는 상품(34·91·93)은 정확히 `zg-ga4.js` 가 `currentItem()` 을 못 만들어
  `zg_view_item_unreadable` 을 쏘는 상품이고, 유지하는 상품(60·61·11)은 `view_item` 을 쏘는 상품이다. **불일치 0건.**
- 개발이 1차 구현에서 60·61·62 까지 죽인 것을 자진 발견해 `hasRealPrice()` 로 좁힌 것은 **올바른 좁힘**이다.
  판매 상품의 금액 줄이 사라졌다면 계측 결함은 아니지만 **계측이 정상인데 화면만 죽는** 상태였고, 지금은 둘이 같은 편을 든다.

---

## 4. 문서 높이 — **`zg_scroll_depth` 분모의 배수급 이동 없음. `2.002` 유지로 충분**

| 항목 | 값 |
|---|---|
| `ds/css/detail.css` | **해시 불변 `f8095b8b`** → `.zg-fold{max-height:1700px}` / `@media(min-width:1024px){2200px}` **그대로** |
| DOM 노드 증가 (개편 전 → 5차) | 11번 **+72** · 63번 **+69** · 64번 **+69** · 98번 **+66** |
| 그 증가가 어디인가 | **전부 `.infoArea` 내부** (+58 / +55 / +55 / +52). **`.zg-fold` 내부는 17~31개로 개편 전과 동일** |

접힘 상한이 그대로이고 늘어난 노드가 **전부 접기 영역 밖(옵션 카드·칩·합계·하단바)** 이므로,
**접힘 상태 문서 높이의 변화는 카드 칩 몇 줄분(수십~수백 px)** 이다. 4차 실렌더 7,084px 대비 **배수급 계단이 아니다.**
1·2차(5,364) → 3·4차(7,084) 같은 **+32% 이동은 이번에 없다.**
→ **`ZG_VER` 을 2.003 으로 또 올릴 필요는 없다.** `2.002` 안에서 해석 가능하다.
(정확한 증분은 레이아웃이 필요하므로 **L-1 에 합산**한다 — 실기에서 `scrollHeight` 를 한 번 읽으면 닫힌다.)

---

## 5. N-7 — 장부 반영 확인 **완료**

`zg-ga4.js:178-184` 실사: `selectedOptionRows()` 의 `var sp = r.querySelector('p.product span');` 뒤에
**폴백이 없다.** `sp` 가 null 이면 `cleanVariant('')` = `''` → `if (v)` 에서 탈락 → **그 행이 통째로 버려진다.**
(→ `item_variant` 결측 + `selectedOptionQty()` null → `currentQty()` 폴백으로 **수량 1 고정**)

내 지적이 장부에 살아 있음을 확인했다:

| 문서 | 반영 내용 |
|---|---|
| `team/작업/다음판_필수_2026-09-07.md:42` | **B-1** 로 등재. *"`zg-ga4.js:178` 이 그 행을 버려 `item_variant` 결측·수량 고정. 후크(`tr[data-zg-nospan="1"]`)는 이미 준비돼 있다. **유지**"* |
| `team/작업/07_9종복제_선결조사_2026-09-07.md:413` | *"**`zg-ga4.js` 에 폴백이 없다는 사실은 장부에 남긴다.** 대표님이 **추가옵션 상품을 등록하는 순간 즉시 P0 로 승격**"* |
| `team/보고/팀장컨펌_칼륨_2026-09-07.md:351` | *"걸리면 `item_variant` 결측 + 수량 고정 = **매출 데이터가 조용히 틀린다**"* |

**내 지적이 문구까지 그대로 반영됐다. 이의 없음.**
현재 13종은 `#totalProducts p.product` 가 서버 응답에 0개(행은 스킨 JS 런타임 생성)라 재현·반증 둘 다 불가하다는 개발 7차 근거도 타당하다.
**폴백을 안 넣은 판단에 동의한다** — 다만 승격 조건은 명확하다: **추가옵션(add_option) 상품이 하나라도 등록되는 순간 P0.**
그때 고칠 위치는 `zg-ga4.js:178` 한 줄이며, 후크(`tr[data-zg-nospan="1"]`)는 `detail-ui.js` 에 이미 있다.

---

## 6. 5차 판정

| 체크 | 결과 |
|---|---|
| `zg-ga4.js` 무변경 | ✅ `251c6804` 시작=종료 |
| `item_variant` 회귀 | ✅ **13종 전수 0건** |
| `view_item` / `add_to_cart` / `zg_option_select` | ✅ **13종 전부 개편 전과 동일** |
| `zg-ga4.js` 가 읽는 3개 노드 | ✅ 불변 |
| 중복 발화 · `stopPropagation` · `onclick` 조상 | ✅ **전부 0** |
| `hasRealPrice()` 가 `screenTotal()` 에 미치는 영향 | ✅ **0** (`.total strong\|em` 6/6 유지) · 판별 결과가 계측과 **1:1 일치** |
| 문서 높이 | ✅ 접기 영역 불변 · 증가분 전량 `.infoArea` · **`2.002` 유지 적정** |
| N-7 장부 | ✅ **B-1 로 반영 확인** |

# **판정: PASS.** 5차 델타에서 계측 회귀 0건.
잔여는 3·4차 목록 그대로 유지(L-1·L-2·L-3·L-5·L-6·L-8·L-9). **L-1 에 "5차 DOM 증가분의 실 `scrollHeight` 증분" 을 합산**한다.

*QA(계측) 배시우 · 2026-09-07 (5차 델타)*
