# 저장본 검증 — 대표님이 편집기에서 되붙여 주신 3개 파일

방법 A(대표님이 편집기 내용을 전체 복사해 주심)로 **바이트 단위 대조**했다.
비교 대상은 우리가 드린 원본 파일. 눈으로 본 게 아니라 md5 로 대조했다.

## 결론 — 3개 전부 완전 일치 ✅

| 파일 | 저장본 md5 | 원본 md5 | 판정 |
|---|---|---|---|
| `orderform.html` | `786ca5467db2c68c4431ec2cab45117a` | 동일 | ✅ |
| `order_result.html` | `bb7735a04c5deb9e9940d098027ddd74` | 동일 | ✅ |
| `zg-ga4.js` | `7fd848da8a9e631e1800d73d03044e8d` | 동일 | ✅ |

## 검수 A — 수정 전 원문과 직접 대조

| 파일 | 추가 | 삭제 |
|---|---|---|
| orderform | 4줄 (빈 줄 1 + GA4 3줄) | **0줄** |
| order_result | 4줄 (빈 줄 1 + GA4 3줄) | **0줄** |

## 검수 B — 삽입 위치

```
orderform     17~19 GA4 3줄  /  20 </head>
order_result  15~17 GA4 3줄  /  18 </head>
```

## 검수 C — 결제 필수요소 (orderform)

`@import` 12개 · `module="Order_form"` · `$move_order_after` · `$move_basket` ·
`{$btn_payment_id}` · `{$payment_proc_id}` · `{$sPrdName}` · `progressPaybar` ·
`checkQuickTransfer` · `fillAccountHolderName` · `toggleOffRecentPayMethod` ·
`createCancelLayer` · `addQuickTransferBenefitBadge` · `quick-cancel-layer` ·
`badge-glow` · `discountRate` · `addr_paymethod` · `tcash` · `agreeMsg` ·
`orderFixItem` · `</style>` — **전부 원문과 동일**

`<script>` 만 1 → 3 으로 늘었다. 우리가 넣은 2개다. 열림/닫힘 3/3 균형.
(첫 검사에서 이걸 "불일치"로 잡았는데 **내 검사 조건이 틀렸던 것**이지 파일 문제가 아니다)

## 검수 D — zg-ga4.js

`node --check` 통과. 773줄. 원본과 md5 동일.

## 검수 E — 저장본 3종을 브라우저로 실제 실행

| | dataLayer | G-GZHFY596SS config | zg-ga4 태그 | 결제로직 | progressPaybar | JS에러 |
|---|---|---|---|---|---|---|
| orderform | js, config | ✅ | ✅ | `function` | ✅ | **0** |
| order_result | js, config | ✅ | ✅ | — (원래 없음) | — (원래 없음) | **0** |

`order_result` 에 결제로직·progressPaybar 가 없는 것은 **원래 그 페이지에 없는 것**이라 정상이다.

## 남은 것

이 검증은 **파일이 정확하다**는 것까지다. 계측이 실제로 도는 것은
작업본을 대표디자인으로 올린 뒤 실주문 1건으로 확인해야 한다
(`QA계측_3차판정_2026-09-06.md` §6).
