# QA(모바일) 홈 1차 판정서 — 홈(index.html) 카페24 변환

- 검수: 웹/모바일 개발팀 **QA 신아린** · 2026-09-08 (홈 1차 = 전수)
- 대상: `index.html`(2,977→**15,439 B**) · `ds/css/home.css`(16,940) · `ds/js/home-blocks.js`(8,541) · `ds/js/home-hero.js`(34,696) · 이미지 86장(1,461,410 B) · git `d011c08`
- 정답지: 설계서 v3 §6-4·§6-5·§7-5·§7-6·§7-9·§11 · 결정로그 #5·#6·#7 · 오세진 선검수
- 심의(광고심의) 판단은 하지 않았다.

# 판정: **PASS**

REJECT 사유 7종(보존 참조 실수 삭제 · R16 미방어 · 조건④ 복원 실패 · sticky 사망 · pageerror · 결정 위반 · `!important` 실규칙) **전부 해당 없음.**
개발이 코드 착수 중 잡았다는 결함 2건(조건④ 저장 시점 · `offsetTop` 좌표계)은 **둘 다 내가 독립 재현해 사실로 확인**했다.
잔여는 P3 2건 + 하네스 대상 추가 1건 + 실기 5건.

## 0. 해시 — 시작·종료 2회, 불변

| 파일 | 바이트 | md5 | 시작 | 종료 |
|---|--:|---|:--:|:--:|
| `index.html` | 15,439 | `1422550e` | ✅ | **동일** |
| `ds/css/home.css` | 16,940 | `19cd2e17` | ✅ | **동일** |
| `ds/js/home-blocks.js` | 8,541 | `c02f8b2c` | ✅ | **동일** |
| `ds/js/home-hero.js` | 34,696 | `42cc8313` | ✅ | **동일** |
| 칼륨 `detail-ui.js` | 36,541 | `7044b38e` | ✅ | **동일**(불변 확인) |

검수 방법: 라이브 홈(iPhone UA 291,338 B) 취득 → `<main>` 안을 새 `index.html` 본문으로 치환 → 시스템 CSS 번들(24,507 B) + 사용자 CSS **§7-5 순서 로컬 25파일, `home.css` 를 6번**에 → 이미지 86장·three.js r134 로컬 HTTP 서빙 → Chromium(SwiftShader) 실렌더. **3D 픽스처(386vh)와 3D 차단 픽스처(폴백) 2벌.**

---

## 1. 보존 diff — 실수 삭제 0건

원문 `<!--@import` **14** → 신 **2**. 12 삭제·2 유지가 결정 #5 와 §11-1 대로인지 라이브 렌더 결과로 출처를 갈랐다.

| 항목 | 원문 | 신 | 출처(라이브 렌더 위치) | 판정 |
|---|:--:|:--:|---|:--:|
| `@layout(/layout/basic/main.html)` | 1 | **1** | — | 유지 ✅ |
| 기존 `@css` 5 / `@js` 7 | 5/7 | **5/7 + 우리 1/2** | 순서·내용 그대로. `@css(home.css)` 는 기존 5줄 **뒤**(21행) = 번들 6번 | ✅ |
| `@import` **main_js.html** | 1 | **1** | 라이브 `<main>` 안 인라인 script 48,650 B(오프셋 174,284) — 팝업 구동체 | 유지 ✅ |
| `@import` **rv_frame.html** | 1 | **1** | 라이브 `<main>` 꼬리 `.rv-related…jsRvNavNext` 알파리뷰 프레임 | 유지 ✅ |
| 삭제 12 | 12 | 0 | `jsMainSlide` 13 · `sub-banner` 11 · `jsPrdSlide` 3 · `mid-banner` 12 · `timesale` 1 · `xans-product-listmain` 4 · `main-review` 4 — **전부 `<main>` 안** (= 삭제한 import 의 산출물) | **의도적** ✅ |
| `df-banner-code` 13 | | | **layout 10**(popup·top-banner·logo×2·join-point·cs-talk·footer-logo·sns×2) → **생존** / **main 안 3**(main-banner-pc·main-banner-mobile·mid-banner2) → 삭제 import 소속 | 의도적 ✅ |
| `.mpopup` 16 | | | **layout 10**(팝업 마크업) → 생존 / **main 안 6** = `main_js.html` 스크립트 내 리터럴 → import 유지로 생존 | **전부 생존** ✅ |
| 원문에 있던 참조 3개(`main.html`·`main_js.html`·`rv_frame.html`) | 3 | 3 | 로컬 저장소에는 셋 다 없음(부분 미러) — **원문 `index.html` 에 이미 있던 참조**임을 HEAD~1 로 확인 | 개발 주장 사실 ✅ |
| `<div>` 개폐 | | 104/104 | | ✅ |

→ 진열 삭제로 빠진 것은 **전부 `<main>` 안 산출물**이고, layout 소속(팝업·헤더·푸터·SNS 배너)은 **하나도 건드리지 않았다.**

## 2. R16 실증 — `body.removeAttribute('class')` 후 생존 + 3D 폴백 실렌더

three.js 를 차단한 픽스처에서 5뷰포트 전부:

| 뷰포트 | body class 전→후 | `.zg-home` class | `.zg-fallback` | `.zg-scene` | 트랙 높이 | cap-a opacity | 가로넘침 | err |
|---|---|---|---|---|---|---|---|---|
| 390·360·768·1023·1280 | `""`→`""` | **`zg-home zg-no3d` 유지** | **`display:flex`** | `none` | **= 뷰포트(100vh)** | 1 | 없음 | 0 |

**M-1 이 실제로 값을 한다.** 상태가 `.zg-home` 에 있으므로 `body` 가 통째로 지워져도 폴백 SVG 히어로가 살아 있다. 3D 픽스처에서는 `THREE=object · no3d=false · canvas.w=뷰포트폭 · fallback=none` — 정상 경로도 확인.

## 3. 조건④ 재현 — 개발의 설계 정정이 맞다

| 뷰포트 | `lastY` 저장 | `eMobilePopup` 부착 직후 `pageYOffset` | `body.position` | frozen | 뗀 뒤 `pageYOffset` | frozen | 복원 | 3.2 s 방치 |
|---|--:|--:|---|---|--:|---|:--:|---|
| 390×844 | 1207 | **0** | fixed | true | **1207** | false | ✅ | true→**false**, `body` 클래스는 붙은 채 |
| 360×740 | 1058 | 0 | fixed | true | 1058 | false | ✅ | true→false |
| 768×1024 | 1465 | 0 | fixed | true | 1465 | false | ✅ | true→false |
| 1023×800 | 1144 | 0 | fixed | true | 1144 | false | ✅ | true→false |
| 1280×900 | 1287 | 0 | fixed | true | 1287 | false | ✅ | true→false |

- **「붙는 순간 `scrollY` 저장」이 틀렸다는 개발 지적은 사실이다.** MO 콜백 시점에 이미 `pageYOffset = 0`(body fixed) — 표의 3열이 그 증거다. `lastY` 상시 기록으로 **5/5 정확 복원.**
- **3초 안전장치**: 떼지 않고 3.2 s 방치 → `frozen` 스스로 해제, 화면 멈춤 없음. `thaw()` 가 `lastP=-1` 로 전 블록을 재계산하므로 그 3초의 진행률 어긋남은 복원 시 닫힌다 — 개발 불안 ①은 실질 위험 낮음.
- (저장 Y 값이 개발 표의 1428/1544/… 와 다른 것은 "히어로 50 %" 계산식 차이일 뿐이다.)

## 4. §6-4 음성 대조군 — 검사가 살아 있다

| | overflowX | overflowY(계산값) | stageTop@50% |
|---|---|---|--:|
| 현행 | visible | visible | **0** |
| `.zg-home{overflow-x:hidden}` 주입 390px | hidden | **auto** | **−1207** ❌(=대조군 성립) |
| 〃 1280px | hidden | **auto** | **−1287** |

`overflow-y` 가 `auto` 로 계산되어 `.zg-home` 이 스크롤 컨테이너가 되고 sticky 가 죽는다 — 설계 §6-4 자기정정이 옳고, 내 sticky 검사는 "깨져야 할 때 깨지는" 검사다.
(1차 시도에서 3D 차단 픽스처로 돌려 대조군이 "실패"했는데, `.zg-no3d` 가 트랙을 100vh 로 접어 스크롤 여지 자체가 없던 **내 하네스 오류**였다. 3D 픽스처로 다시 쟀다 — 기록해 둔다.)

## 5. 5뷰포트 — 넘침 0 · sticky 0 · 스킨 오염 0 · 에러 0

| 뷰포트 | 트랙 | `scrollW/clientW` | `.zg-home` overflow | 히어로 50 % stageTop / position | 헤더·푸터·팝업·`#wrap`·`#contents` 계산 스타일(우리 시트 ON↔OFF) | pageerror | `/ds/` 4xx |
|---|---|---|---|---|---|:--:|:--:|
| 390×844 | 3258(3.86vh) | 390/390 | visible/visible | **0 / sticky** | 변화 없음 | 0 | 0 |
| 360×740 | 2856(3.86vh) | 360/360 | 〃 | 0 / sticky | 없음 | 0 | 0 |
| 768×1024 | 3953(3.86vh) | 768/768 | 〃 | 0 / sticky | 없음 | 0 | 0 |
| 1023×800 | 3088(3.86vh) | 1023/1023 | 〃 | 0 / sticky | 없음 | 0 | 0 |
| 1280×900 | 3474(3.86vh) | 1280/1280 | 〃 | 0 / sticky | 없음 | 0 | 0 |

## 6. 개발의 하네스 버그(`offsetTop`) — 내 스크립트는 같은 함정에 없다

블록 스크럽을 **`getBoundingClientRect().top + pageYOffset`(문서 좌표)** 로 쟀고, 대조를 위해 `offsetTop` 도 같이 찍었다:

| 블록 | 문서좌표 docTop | `offsetTop` | idx @0/0.5/0.95 | 끝 상태 |
|---|--:|--:|---|---|
| pot | 4116 | **264** ← 다름 | [0, 11, 22] 단조↑ | v0=**525** v1=20 band .97 cap「하루의 나트륨 밸런스를 RESET」 |
| mag | 7117 | 283 | [0, 12, 22] | v0=**400** v1=20 |
| vitb | 10137 | 302 | [0, 12, 22] | v0=**4** v1=20 |

`offsetTop` 은 `.zg-pblock`(position:relative) 기준이라 문서 좌표와 4~10 k px 다르다 — 그걸로 스크롤했으면 `[0,0,0]` 이 나온다. 개발 자인이 정확하다.

## 7. CSS 176→84 — 살아 있는 규칙이 빠진 것은 없다 (독립 감사)

프로토타입 `site/home.html`(md5 `b72553be`) `<style>` 3개 **289 규칙** 을 마크업·JS 토큰과 대조:
- 죽은 토큰 포함 **97 규칙** — `card`·`pouch`·`closing`·`sc-*`·`rev-*`·`trust`·`showcase` … 이전 라운드 삭제 섹션 잔재 → 안 옮긴 것이 맞다.
- 살아 있는 토큰만 쓰는 **192 규칙** 중 선언이 `home.css` 에 없는 것 **38** — 전부 **설계상 의도적 미이식**으로 귀속됨: `#bg2/#bg3/#bgM/#bg4` 배경 `url()`(S-5, JS 주입) · `.gnb*`·`.zgb/.zgm/.zgsm`·`.ft*`·`footer`(S-6 스킨 헤더/햄버거/푸터) · `:root` 변수(→`.zg-home{--zg-…}`) · `.pill.ghost`(hidden) · `from/to`(키프레임 본체는 있음, @keyframes 2). `body.no3d #bw0`·`#bggrade` 는 값 표기 차이(`.1`)로 걸린 오탐 — `home.css:167,170` 존재.
- **`.zg-home` 접두 없는 전역 셀렉터 0건.** `*`·`a`·`h2`·`section` 전부 스코프됨(`.zg-home,.zg-home *` / `.zg-home a` / `.zg-home h2` / `.zg-store{padding-block:72px}`).
- (개발의 "176/92" 는 히어로 `<style>` 하나만 센 수치로 보인다. 총계는 다르지만 결론은 같다.)

## 8. 확정 카피 대장(§11-3) — **38/38 일치** · 결정 #6·#7 반영

- `<h1>` **1개** = 「물 없이도, OK」 / `.zg-cap-a` 의 `h1` **0**(→`p.zg-h1`) ✅ S-3
- 「아침부터 밤까지」 **1회**(제목만) · 설명문 「하루에도 필요한 순간은 다르니까. 시간에 맞춰 챙기는 건강 루틴.」 ✅ #6
- `레몬라임` **0** · `레몬` 0 · `라임` **3** · 「상큼한 **라임**향으로」 ✅ #7
- 금칙: `claude.ai` 0 · `평점` 0 · `26,961` 0 · `100vw`(코드) 0 · `data:image` 0 · `ec-data-src` 0 · `asyncImage` 0 · `scroll-effect` 0
- 링크 9개 전부 §11-4 확정표와 일치(11/16/13 ×2 · 71 · `/category/store/42/`), 브랜드 스토리 pill `hidden` 1

## 9. 3D 경로 · 지연 주입 · 바 · 점프

| | 390 | 1280 |
|---|---|---|
| THREE / no3d / canvas / fallback | object / false / 390 / none | object / false / 1280 / none |
| 배경 요청 **스크롤 전 1장**(dawn) → 스크롤 후 5장 (S-5) | ✅ | ✅ |
| `.zg-ctabar` 상단 off → 0.7vh 에서 `zg-on` | ✅ | ✅ |
| `[data-zg-jump]` → `.zg-store` | top=0 도달 | 1.2 s 시점 y=7881 진행 중(스무스 스크롤 미완, 페이지 13 k px) — **실기 항목** |
| pageerror | 0 | 0 |

## 10. `home.css:10` `!important 0개` 문구 (지시 ⑨)

- 하네스 B1 은 `strip_comments()` **뒤에** 센다 → `home.css` 계산값 **0**. 규칙 위반 아님.
- 그러나 **`home.css` 는 하네스 대상이 아니다**(`b_static.py CSS = [detail.css, price.css]`). 개발 §7-1 요청대로 추가되기 전까지 이 검사는 홈에 안 걸린다.
- `grep -c` 가 1 을 세므로 오케스트레이터 독립 확인이 매번 걸린다. **`detail.css` 전례("important-플래그")대로 문구를 바꿀 것을 권고**(P3). 같은 이유로 7행 `100vw 금지` 도 `100vw` 토큰이 걸린다 — 함께 바꾸면 깨끗하다. 코드 동작 변경 0.

## 11. 정답지 대조 (구현/미구현/오구현)

| 근거 | 항목 | 판정 |
|---|---|---|
| §6-4 | `.zg-home` overflow 0 · 전역 셀렉터 0 · 새 `!important` 0 · `body` 클래스 0 · M-1 이관 | **구현** |
| §6-5 | MO 감시(body+html) · 동결 · 복원 · 3초 안전장치 | **구현**(설계 문장 정정 포함) |
| §7-5 | `@css` 6번 · `@js` blocks→hero 순 · 기존 12줄 무변경 | **구현** |
| §7-6 | `ec-data-src` 0 · `.zg-home` overflow 0 | **구현** |
| §7-9 M-1~M-5 | body 클래스 0 · `window.onload` 0 · `asyncImage`/`ec-data-src`/`scroll-effect` 0(코드) | **구현** |
| §11-1 | 골격 ①~⑪ · 삭제 12/유지 2 | **구현** |
| §11-2 | 구조(정적 3블록·`.zg-fr` 72·썸네일 3·h1 1·ctabar) | **구현** |
| §11-3 | 카피 38/38 · #6 · #7 | **구현** |
| §11-4 | 링크 9 · pill hidden | **구현** |
| **계** | **9/9 구현 · 미구현 0 · 오구현 0** | |

## 12. 잔여

**P3 (코드 동작 무관)**
- `home.css:7,10` 주석 토큰(`100vw`·`!important`) 문구 정리 — §10
- 하네스 A/B 대상에 `index.html`·`home.css`·`home-*.js` 추가(개발 §7-1 요청) — 반영 전까지 홈은 회귀 방어망 밖

**실기(미리보기) 5건 — 대표님**
1. 히어로 중간(≈50 %)에서 **헤더 검색창 탭 → 닫기** → 히어로가 원위치로 돌아오는가 (조건④ 실경로. 내 재현은 클래스 직접 주입)
2. **팝업(`.mpopup`) 열기 → 닫기** → 스크롤 정상·히어로 진행률 안 튐 (`main_js.html` 은 jQuery 의존이라 내 픽스처에서 실행 못 함)
3. 「루틴 구경하기」·「제품 보러 가기」 탭 → **첫 구매 EVENT 섹션 상단**에 멈추는가 (데스크톱 긴 페이지 스무스 스크롤)
4. 대표님 폰에서 히어로 3D **체감 프레임**(R17 스킨 `setInterval` 은 수정 금지 파일)
5. 폰트·아이콘 등 스킨 자산과의 **최종 외관**(픽스처는 일부 자산 404)

*QA(모바일) 신아린 · 2026-09-08 (홈 1차)*
