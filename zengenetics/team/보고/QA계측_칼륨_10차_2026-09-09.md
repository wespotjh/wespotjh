# ④-B QA(계측) 칼륨 10차 — **델타 판정** (구성 카드 재탭 해제)

**판정: PASS — 계측 회귀 0건. 해제는 계측을 부풀리지 않는다**
**작성:** 개발팀 QA·계측 **배시우** · 2026-09-09
**시작 해시:** git `720ae1c` · `ds/js/detail-ui.js` **`f62a2ce9`** 39,376 · **`zg-ga4.js` `5dd8b791` 42,698 불변** · `node --check` 통과
**차단 호출:** `stopPropagation` 0 · `stopImmediatePropagation` 0 · **`preventDefault` 0** — 5차의 1건(무단 UI 삭제 때 함께 사라진 jump 버튼)도 없어 **상세 파일에는 차단 호출이 하나도 없다.**

---

## 1. `zg_option_select` 오발사 — **0건. 구조적으로 나갈 수 없다**

`pick()` 이 `already` 면 **`unpick(key)` 후 `return`** 한다 — `optSel.dispatchEvent(new Event('change'))` 에 **도달하지 않는다**(`:479-484` vs `:486-488`).
`unpick()` 은 `del.click()` 만 부르고, 그 클릭은 카페24 위임 핸들러(`#totalProducts a.delete` → `.option_box_del`)로 가지 `<select>` 를 건드리지 않는다.

**실측 — 담기 3 → 해제 3:**

| 조작 | `tr.option_product` | `#totalPrice .total` | 발사 |
|---|---|---|---|
| 담기 5box | 1 | `149,000원 (1개)` | `zg_option_select` |
| 담기 3box | 2 | `246,000원 (2개)` | `zg_option_select` |
| 담기 2box | 3 | `314,000원 (3개)` | `zg_option_select` |
| **해제 5box** | 2 | `165,000원 (2개)` | **—** |
| **해제 3box** | 1 | `68,000원 (1개)` | **—** |
| **해제 2box** | 0 | `0원 (0개)` | **—** |

**해제 3회 → `zg_option_select` 0건.** GA4 선택 수 부풀림 없음. **명세 불필요**(§7 참고 1건만 기록).

## 2. `add_to_cart` 의 `items` — 유령 0

5box 담기 → 2box 담기 → **5box 해제** → 장바구니:
```json
{"value":68000,"value_source":"screen_total",
 "items":[{"item_id":"11","price":68000,"quantity":1,
           "item_variant":"BEST 젠제네틱스 붓기파우더 2 box (무료배송)"}]}
```
**해제한 `5 box` 문자열 잔존 0** ✅ · `quantity` 1(남은 행 1) ✅. `zg-ga4.js:174` 가 매번 `document.querySelectorAll('tr.option_product')` 로 **다시 읽기** 때문에 캐시 유령이 생길 구조가 아니다.

## 3. `screenTotal()` — 감소 경로 전부 정상, 오류 이벤트 0

353,000 급 → 149,000 → 0원 감소 구간에서 `#totalPrice .total strong|em` 판독이 위 표대로 매 단계 정확하다.
**행 0 · 총액 `0원 (0개)` 상태에서 오류 이벤트 0건** — `zg_cart_unreadable` 는 `/order/basket\.html` 경로 전용이라 상세페이지에서 애초에 발화하지 않는다. `zg_view_item_unreadable` 도 `.infoArea` 판독은 그대로라 안 나간다.

### 경계 — 전부 해제한 뒤 장바구니를 누르면
`screenTotal()` 이 `0원` → `v > 0 ? v : null` 로 **null** → 설계된 폴백:
```json
{"value":34900,"value_source":"base_price","items":[{"price":34900,"quantity":1}]}
```
**한 번도 고르지 않은 최초 상태의 페이로드와 바이트 단위로 동일하다** → GA4 에 **새로운 상태가 생기지 않는다.** 기존 동작 그대로이고, `value_source:'base_price'` 가 "옵션 미선택"을 이미 표시한다. (실제로는 필수옵션 상품이라 카페24가 제출 자체를 막는다.)

## 4. 행 재그리기 중간 상태 — 값이 깨지지 않는다

행은 있는데 `p.product span` 이 아직 없는 순간을 강제로 만들어 담기:
```json
{"value":68000,"value_source":"screen_total","items":[{"price":68000,"quantity":1}]}
```
**`value` 는 화면 총액과 일치**(68,000) ✅ · `item_variant` 는 **키 자체를 넣지 않는다**(N1 설계 — 빈 문자열을 보내 "옵션 없는 상품"과 섞지 않는다) ✅.
→ 중간 상태에서 **틀린 값이 아니라 없는 값**이 된다. 설계 원칙 2 대로다. (이는 장부 **B-1(N-7)** 과 같은 지점이며, 재탭 해제가 그 구간을 **더 자주 지나가게 만든다** — §7)

## 5. 해제↔담기 반복 5회 — 중복·소실 0

2 box 담기↔해제 **5왕복**(조작 10회) → 최종 행 0 · 총액 `0원 (0개)` ·
이벤트 `{"view_item":1,"zg_option_select":5}` — **담기 5회에 정확히 5건**, 해제 5회는 0건. ✅

## 6. 13종 전수 dataLayer diff — **상이 0**

직전 커밋본 `detail-ui.js` `7044b38e` 와 10차 `f62a2ce9` 를 같은 픽스처로 각각 돌렸다.

| 상품 | 직전 | 10차 | diff |
|---|---|---|---|
| 11·13·16·63·64·98 | 10 | 10 | 없음 |
| 60·61·62 | 9 | 9 | 없음 |
| 71 | 8 | 8 | 없음 |
| 34·91·93 | 7 | 7 | 없음 |
| **합계** | **116** | **116** | **상이 상품 0개** |

throw 0. `zg_ver` `2.003` 전수 · `send_to` 전수 · PII 0.

---

## 7. 기록 (반려 아님 · 명세 불필요)

1. **해제는 GA4 에 흔적을 남기지 않는다.** `zg_option_select` 는 담기만 세므로 **"고른 뒤 마음을 바꾼 사람"이 보이지 않는다.** 지금은 `zg_option_select` 대비 `add_to_cart` 비율로 간접 추정할 수 있다. 실제로 얼마나 일어나는지 궁금해지면 **2.004 백로그 N-8 `zg_option_unpick`**(`{item_id, item_variant, remaining}`)으로 올린다. **이번 판에는 필요 없다** — 없다고 기존 지표가 틀어지지 않는다.
2. **장부 B-1(N-7) 노출 빈도가 올라간다.** `p.product span` 이 없는 행을 `zg-ga4.js:178` 이 통째로 버리는 문제는 그대로인데, 재탭 해제가 그 재그리기 구간을 더 자주 지나간다. **현재 13종은 발생 불가**(추가옵션 0개)라 등급은 그대로 두되, 장부에 *"10차 재탭 해제로 노출 빈도 상승"* 한 줄만 덧붙이길 요청한다.

## 8. 판정

| 체크 | 결과 |
|---|---|
| `zg-ga4.js` 무변경 | ✅ `5dd8b791` |
| 해제 시 `zg_option_select` | ✅ **0건**(구조적으로 도달 불가) |
| `add_to_cart` 유령 항목 | ✅ 0 |
| `screenTotal()` 감소 경로·0원 | ✅ 정상 · 오류 이벤트 0 · 최초 상태와 구별 불가 |
| 재그리기 중간 상태 | ✅ 틀린 값 대신 없는 값 |
| 5왕복 중복·소실 | ✅ 담기 5 = `zg_option_select` 5 |
| 13종 diff | ✅ 116/116 상이 0 |
| 차단 호출 | ✅ **0 / 0 / 0** |

# **판정: PASS.** 10차 델타에서 계측 회귀 0건.
DOM 직접 조작 없이 **카페24 자기 삭제 컨트롤을 클릭**하는 방식이라, `zg-ga4.js` 는 고객이 아래 목록에서 직접 지운 것과 **완전히 같은 DOM 변화**를 본다 — 계측이 알아야 할 새 경로가 없다. 이 선택이 옳았다.
잔여·백로그 변동: **N-8 신규(2.004, 선택)** · B-1 노트 1줄 요청. 그 외 기존 목록 그대로.

**종료 해시:** 아래 실측(시작과 동일).
*QA(계측) 배시우 · 2026-09-09 (칼륨 10차 델타)*
