# QA(SEO·접근성) 판정서 — 홈 3차 · 전량

**판정: ✅ PASS** — 2차 잔여 2건(P1-E · P2-h) 해소 확인. 신규 REJECT 사유 없음.
**검수:** 개발팀 QA·SEO **문가온** · 2026-09-08 (홈 3차)
**해시:** 시작 08:32:23Z / 종료 10:34:01Z **동일** — `index.html` `4cb10827` 17,281B · `home.css` `c88028de` 17,294B · `home-blocks.js` `b22664e2` 8,800B · `home-hero.js` `071f78c2` 36,509B · `head.html` `2a64e772`(불변) · git `c663592`
**방법:** `git diff 9557546` 로 2차→3차 변경 전량 정독 + `renderBlock` 수치 재현 + 캐스케이드 순서 실사. **심의 판단 없음.**

---

## 1. 2차 잔여 — 둘 다 해소 ✅

### P1-E `np > 0` 가드 — ✅ **해소.** Googlebot 긴 뷰포트에서 `525` 유지

`home-blocks.js:147-152` 가 내가 지정한 대로 정확히 들어갔다.
```js
if (np > 0) { if (b.v0) b.v0.textContent = …; if (b.v1) b.v1.textContent = …; }
```

실소스 로직을 그대로 재현해 수치 검증했다. `np = clamp01((p-0.62)/0.34)` 이므로 **p ≤ 0.62 인 모든 상태에서 숫자를 건드리지 않는다.**

| 시나리오 | `:126` 게이트 | p | np | 결과 |
|---|---|---|---|---|
| **Googlebot 긴 뷰포트 — 2번째 블록** | **OPEN** (`r.top > vh*2` 열림) | 0.000 | 0 | **미접촉 = 마크업 `525`** ✅ |
| **Googlebot 긴 뷰포트 — 3번째 블록** | **OPEN** | 0.000 | 0 | **미접촉 = `525`** ✅ |
| 일반 — 블록이 화면 아래 | OPEN | 0.000 | 0 | `525` ✅ |
| 일반 — 블록 상단이 화면 top | OPEN | 0 | 0 | `525` ✅ |
| 일반 — 스크럽 62% | OPEN | 0.620 | 0 | `525` ✅ |
| 일반 — 스크럽 100% | OPEN | 1.000 | 1.000 | `525` (카운트업 종료값 동일) ✅ |

**핵심:** 2차에서 우려한 "게이트가 열리면 `0` 으로 되돌아간다" 경로가 **np 가드로 완전히 막혔다.** 게이트가 열려도 p=0 이면 숫자는 마크업 값이다. 카운트업 연출은 np>0 구간에서 그대로 돈다(진입 순간 `round(525·e)`≈0 부터 상승). **크롤러가 보는 DOM = 「칼륨 525 mg · 하루 2포 기준」 「20 포 · 한 상자」** 확정.

### P2-h `.zg-no3d .zg-bg0{display:block}` — ✅ **해소.** 캐스케이드 순서까지 정확

```
:170  .zg-home.zg-no3d .zg-bg {display:none}    (0,3,0)
:172  .zg-home.zg-no3d .zg-bg0{display:block}   (0,3,0) ← 동일 구체도, 뒤에 선언되어 이긴다
```
cdnjs 가 카페24 2MB 번들보다 늦어 `boot()` 가 `.zg-no3d` 를 붙이는 경로에서도 **preload 한 286KB 첫 배경이 그려진다.** `bgm/bg2/bg3/bg4` 는 계속 숨김 — 권고대로 첫 장만 살렸다. ✅

### 접근자 스텁 3개 — ✅ 색인·접근성 무관

```js
['runMainBannerSlidePC','runMainBannerSlideMobile','runSubBannerSlide'].forEach(function(k){
  var stub = {autoplay:{pause:function(){}},destroy:function(){}};
  Object.defineProperty(window,k,{get:…,set:function(){},configurable:true});
});
```
`window` 전역 프로퍼티 정의일 뿐 **DOM 노드·텍스트·ARIA·이미지에 접촉 0**. 렌더 결과가 바뀌지 않으므로 색인·접근성 **무관** ✅.
2차의 리터럴 대입에서 접근자로 바꾼 이유(폭 768 초과 시 `destroy()` 뒤 `undefined` 재대입 → 회전·리사이즈로 되돌아오면 다시 던짐, setter 를 비워 영구 유지)는 `main_js.html` 원문 인용과 정합한다. `runSubBannerSlide` 추가도 서브 배너 `@import` 삭제와 짝이 맞다.

---

## 2. 전량 재검수

### 2-1. h 계층 — `<h1>` 정확히 1개 ✅

| 순 | 줄 | 태그 | 텍스트 |
|---|---|---|---|
| **1** | :116 | **h1** | **아침부터 밤까지, 젠제네틱스** — DOM 첫 제목 · 브랜드명 포함 · 첫 뷰포트 가시(P1-D) |
| 2–4 | :122/130/135 | h2 | 아침은, 비타민B / 저녁은, 칼륨 / 밤은, 마그네슘 |
| 5 | :170 | h2 | 물 없이도, OK |
| 6–8 | :185/205/225 | h2 | 칼륨 525mg, 하루 두 포 / 마그네슘 400mg, 자기 전 한 포 / 비타민B 4종, 아침 한 포 |
| 9 | :242 | h2 | *처음이라면, 첫 구매 EVENT! |

`h1` **1** · `h2` **8** · `h3` **0** · 건너뜀 **없음** · 라이브 홈(0/0) 대비 개선 유지. 장부 F-2 해소 확정 ✅

### 2-2. preload 문법·위치 ✅
`:68 <link rel="preload" as="image" href="/ds/image/zg-home-bg-dawn.jpg" fetchpriority="high">` — 인라인 `<style>`(`:69`) 바로 앞. 동일 출처라 `crossorigin` 불필요, `.zg-bg0` 이 즉시 소비하므로 "미사용 preload" 경고 없음. 미리보기 404 시 부작용은 콘솔 경고 1건뿐(2차 판정 유지).

### 2-3. `alt` 표 (86장)

| 구분 | 장수 | 삽입 | `alt` | `loading`/`decoding` | 판정 |
|---|---|---|---|---|---|
| 히어로 배경 jpg | 5 | CSS 배경(1장 인라인 + 4장 JS) | 해당 없음 | preload high(1장) | ✅ |
| three.js 텍스처 | 6 | WebGL | 해당 없음 | — | ✅ |
| 스틱 프레임 | 72 | JS `:113-119` | `alt=""` 장식 | 블록당 3 eager / 21 lazy · async | ✅ |
| 트리오 `:175-177` | 3 | 정적 | **`젠제네틱스 칼륨/비타민B 컴플렉스/마그네슘 분말 스틱`** | lazy · async | ✅ 창작 0 |
| 썸네일 `:246-248` | 3 | 정적(링크 안) | `alt=""` | lazy · async | ✅ 링크 텍스트가 이름 제공 (P2-b 이월 유지) |
| **계 86** | | | **`alt` 속성 누락 0** | | ✅ |

### 2-4. LCP · CLS — **F-2 임계 8px 승격 재측정** ✅

`home-hero.js:727` `if ((window.pageYOffset || 0) > 8) return;` (2차 `innerHeight*0.5` → **8px**).

| 상태 | 3D 승격 | 높이 변화 | CLS |
|---|---|---|---|
| `scrollY = 0` (첫 화면 정지) | 승격 | 100vh→386vh, 스테이지 `sticky top:0` 라 **화면 내용 이동 없음** | **≈0** (개발 실측 0.006) |
| `scrollY 1~8px` (iOS 바운스 여유) | 승격 | 동일 | ≈0 |
| `scrollY > 8px` | **승격 안 함**(폴백 100vh 유지) | 변화 없음 | **0** |

2차의 `0.5 × innerHeight`(422px) 임계에서는 이미 내려간 상태에서 트랙이 386vh 로 늘며 아래 내용이 밀렸다 — 개발 실측 0.4~0.8. **8px 승격으로 그 구간이 사라졌다.** 임계를 낮춘 대가는 "느린 망에서 3D 를 못 보는 사용자가 늘어나는 것"뿐이고, 그때 폴백에는 P2-h 로 첫 배경 사진 + h1 + 캡션이 남는다 — **SEO·접근성 손실 0.**
높이 고정(386vh/100vh/300vh/100svh/`aspect-ratio:1/1`/`min-height:2.9em`)은 1차와 동일 ✅.

### 2-5. 크롤 가능성 ✅
`home.css` 의 `display:none` **5건 전부** 3D 폴백 전환용(`.zg-fallback` 기본 숨김 · `.zg-no3d` 의 `.zg-scene`/`.zg-skywrap`/`.zg-callouts`+`.zg-dashes`/`.zg-bg`). **텍스트 콘텐츠를 숨기는 `display:none` 0건.**
히어로 캡션 5·제품 3블록 제목/설명/법정각주·스토어·법정 고지는 전부 정적 마크업, 숨김은 `opacity`/`transform`. 폴백 경로에서 `.zg-cap-a{opacity:1}` 로 h1·설명·CTA 잔존 ✅. 카운터는 §1 대로 `525/400/4/20` ✅.
스크롤 캡션 1~3(`data-zg-cap1~3`, 9문장)은 속성이라 미색인 — P2-d 이월 유지.

### 2-6. 접근성 ✅
`prefers-reduced-motion` → `home-hero.js:734-735` `boot()` 에서 `.zg-reduced` 부착(THREE 유무 무관) → `:43-44` 트랙 100vh + `:71` `reduced` 로 스크럽 정지, 정적 프레임 + 텍스트 잔존 ✅.
`:274 .zg-home a:focus-visible,.zg-home button:focus-visible{outline:2px solid var(--zg-acc);outline-offset:2px}` — 흰 배경 3.63:1 / 남색 4.92:1, 비텍스트 3:1 충족 ✅. 링크·버튼 전부 네이티브 ✅.
대비: 히어로 캡션 17.85 · eyebrow 13.19 · 칩 16.56 · CTA 18.08 · ink-2 6.09 · note 5.06 ✅ / `--zg-ink-3` **2.99** · `.zg-hint` **1.81** ⚠ — 칼륨과 동일 토큰, **P2-e 이월**(디자인 소관, 반려 아님).

### 2-7. 금칙어·주석·보존 ✅
`레몬라임`·`평점`·`리뷰수`·`26,961`·`2박스`·`무료배송`·`claude.ai`·판정용어(PASS/REJECT/검수/판정)·TODO/FIXME — **4파일 전부 0건**.
주석 26개 안 `{$` **0** · 파일 전체 `{$` **0** · `index.html` 안 `<title>/<meta>/canonical/ld+json/itemscope` **0건**(head 소관 무침범) ✅.
`head.html` md5 `2a64e772` **불변** → `facebook-domain-verification` 1개 · canonical 1개 자기참조 · Organization JSON-LD/`itemscope` 전부 무영향 ✅.

---

## 3. 🔴 대표님 조치 (P0) — 홈 `<title>` 최종 확정본

**코드로 못 고친다.** 라이브 홈은 지금 `<title>젠제네틱스 공식몰</title>` 1개뿐이고 브랜드명 외 키워드가 0개다. 개편은 `<title>` 에 손대지 않으므로 그대로 남는다.

### 어디서 고치나
**카페24 관리자 › 쇼핑몰 설정 › 검색엔진 최적화(SEO) › 주요 페이지 SEO 태그 › 「메인 페이지」 탭**
상품 제목이 `[PRODUCT_NAME] - [MALL_NAME]` 로 나오는 바로 그 화면입니다. 홈 한 페이지에만 적용되고 `<title>` 개수는 1을 유지합니다.

> ⚠ **「고급설정 › 코드 직접입력」에는 절대 넣지 마십시오.** 전 페이지 `<head>` 에 실려 상품페이지 제목까지 덮습니다 — 2026-09-06 사고가 난 바로 그 자리입니다.
> ⚠ `index.html`(스킨)에 `<title>` 을 넣는 것도 금지입니다. 본문 슬롯이라 `<body>` 안에 두 번째 `<title>` 이 생겨 같은 사고가 납니다.

### 붙여넣기 문안 (확정 카피의 단어만 사용 — 창작 0)

| 필드 | 값 |
|---|---|
| **타이틀** | `젠제네틱스 공식몰 \| 아침부터 밤까지, 칼륨 · 마그네슘 · 비타민B 분말 스틱` |
| **메타 설명** | `물도 컵도 필요 없는 분말 스틱. 아침은 비타민B, 저녁은 칼륨, 밤은 마그네슘 — 시간에 맞춰 챙기는 건강 루틴. 첫 구매 데이팩 8,900원.` |
| **og:title** | 타이틀과 동일 |
| **og:description** | 메타 설명과 동일 |

### 반영 후 확인 (30초)
```bash
curl -s https://zengenetics.co.kr/ | grep -c '<title'        # 반드시 1
curl -s https://zengenetics.co.kr/ | grep -o '<title>[^<]*'  # 새 제목이 나와야 한다
curl -s https://zengenetics.co.kr/ | grep -c 'canonical'     # 반드시 1
```
**2가 나오면 즉시 되돌린다.**

---

## 4. 이월 (반려 아님)

| # | 내용 | 소관 |
|---|---|---|
| P2-b | 스토어 썸네일 3장 `alt` (이미지 검색용) | 개발 · 다음 판 |
| P2-d | 스크롤 캡션 1~3 (9문장)을 DOM 에 (`<span hidden>`) | 개발 |
| P2-e | `--zg-ink-3` #93959D 2.99:1 · `.zg-hint` #BFC0C6 1.81:1 → 대비 상향 | 디자인 |
| P2-a·c·f·g | 프레임 eager 축소 · 폴백 시프트 · 투명 캡션 내 버튼 포커스 · 장식 `aria-hidden` | 개발 |
| **P0** | **홈 `<title>`·description·og — §3** | **대표님** |

---

**최종: ✅ PASS — 홈 3회 검수 완료. P1 5건 전량 반영, P2-h 해소, 신규 회귀 0.**

**QA 문가온** · 2026-09-08 (홈 3차 · 전량)
