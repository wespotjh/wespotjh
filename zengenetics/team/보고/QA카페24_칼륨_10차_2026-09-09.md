# **PASS** — 삭제 경로가 본품/추가상품으로 갈린다는 근거를 번들 원문에서 확인했다. 확인 팝업 없음. 배포는 ③ 1파일 재붙여넣기이고, **적용 뒤 저장이라 「라이브가 즉시 바뀐다」는 사실을 배포표에 넣었다**(중간 깨짐 없음).

# QA(카페24 실환경) — 칼륨 상세 **10차 판정서** (R-2 재탭 해제)

**작성** QA(카페24 실환경) 오세진 · 2026-09-09 · 대상 git `720ae1c`
**변경 1파일** `ds/js/detail-ui.js` `f62a2ce9bf4c3792c3d8c9af04c73192` / 39,376 B (전달본 `3_` md5 일치)
**불변 확인** `detail.css` `e8e10b3f` · `product/detail.html` `50f2c989` · `zg-ga4.js` `5dd8b791` · `price.css` · `head.html` · `import/detail.html`
**범위** 플랫폼 경로만. 기능·계측·접근성은 앞 3게이트 PASS 그대로 인용, 재확인하지 않았다.

---

## 1. 삭제 컨트롤 경로 — 두 갈래가 맞다 (번들 원문, 미리보기 `optimizer.php` 1,964,325 B)

| 대상 | 위임 핸들러 | 실제 삭제 |
|---|---|---|
| **본품** `tr.option_product` | `EC$(document).on('click', '#totalProducts a.delete', …)` **@1,156,911** → `EC$(this).find('.option_box_del').click()` | `EC$(document).on('click', '.option_box_del', …)` **@1,157,053** |
| **추가상품** `tr.add_product` | `EC$(document).on('click', '#totalProducts .add_products a.delete', …)` **@1,591,936** → `.option_add_box_del` 클릭 | `EC$(document).on('click', '.option_add_box_del', …)` **@1,592,097** |

- 아이콘 클래스도 갈린다: 본품은 `buildDeleteIcon()` (@1,206,848) 이 **`class="option_box_del"`** 고정, 추가상품은 행 빌더가 **`sOptionBoxCode + '_del'`** = `option_add_box_del` 을 붙인다(@1,623,704 · @1,625,867 · @1,627,378 · @1,628,786).
- 우리 쪽 진입점은 `pickedRows()` = `$$('tr.option_product')` (`detail-ui.js:192`) 하나뿐이고, `unpick()` 은 그 행 안(`delControl(r.el)`)만 뒤진다. **추가상품 행은 선택 자체가 되지 않는다** — 신아린 P0(불간섭)의 구조적 근거가 원문과 일치한다.
- 한 가지만 기록해 둔다: 만약 우리 선택자가 언젠가 추가상품 행에 닿으면 `a.delete` 는 **두 위임 핸들러에 동시에** 걸린다(하나는 `.option_box_del` 을 못 찾아 무해, 다른 하나가 실제 삭제). 그래서 `tr.option_product` 스코프가 이 설계의 안전핀이다. 11차 정리 때도 이 스코프는 건드리지 말 것.

## 2. UA 분기 — 삭제 컨트롤에는 8차의 함정이 없다

| 빌더 | 삭제 마크업 |
|---|---|
| `buildMobileSoldInCells` (본품·모바일) | `<td class="center"><a href="#none" class="delete">` + `<img class="option_box_del">` |
| `buildPcSoldInCells` / `buildPcSoldOutCells` (본품·PC) | `<a href="#none" class="delete">` + `<img class="option_box_del">` |
| 모바일 **품절(sold-out)** 셀 @1,212,267 | **`<a href="#none">` — `class="delete"` 가 없다.** `<img class="option_box_del">` 는 동일 |
| 추가상품(모바일·PC) | `<a href="#none" class="delete">` + `<img class="option_add_box_del">` |

- 8차의 `.up/.down`(=`<a>` vs `<img>`)처럼 **클래스가 태그를 옮겨 다니지는 않는다.** `.option_box_del` 은 항상 `<img>` 에, `.delete` 는 `<a>` 에(모바일 품절만 예외로 빠짐).
- 우리 `delControl()` 은 `'a.delete, .option_box_del, a[class*="delete"], a[class*="Del"]'` 라 **네 형태 모두 잡힌다** — 모바일 품절 행에서도 `.option_box_del`(img)로 걸린다. `querySelector` 는 문서 순서 우선이라 `a.delete` 가 있으면 그것을(부모) 먼저 잡고, 없으면 img 를 잡는다. **어느 쪽을 눌러도 최종 경로는 `.option_box_del` 핸들러 하나**다.

## 3. 확인 대화상자 — **없다**

`.option_box_del` 핸들러 본문(@1,157,053~약 4KB) 전수: `confirm(` **0** · `alert(` **0**. 하는 일은 `is_selected` 해제 → 행 `remove()` → 파일옵션 정리 → (직접구매 주문서면) `deleteBasketProduct` → `TotalAddSale` 재계산이다. 번들 전체의 `confirm(` 9곳은 전부 다른 흐름(중복 장바구니·단독구매·게시판)이라 이 경로와 무관.
⇒ **재탭이 팝업을 부르지 않는다.** (`CAFE24.checkChannelUI()` 분기로 `closest('.option_product').remove()` 냐 `parents('tr,li').last().remove()` 냐가 갈리지만, 이는 고객이 직접 삭제 아이콘을 눌렀을 때와 **완전히 같은 경로**라 새 위험이 아니다.)

## 4. 배포 — ③ 1파일. 단, **적용 뒤 저장이라 전제가 바뀐다**

- 바뀐 파일은 `ds/js/detail-ui.js` 하나. `product/detail.html`(불변)의 `<!--@js(/ds/js/detail-ui.js)-->` 5행 선언·파일명·번들 순서 그대로 → **번들 레코드 목록 불변, 내용만 교체**. 다른 파일 재붙여넣기 불필요.
- **중간 상태 없음**: 파일 1개짜리 교체라 "절반만 적용된 조합"이 존재하지 않는다. 저장 전에 페이지를 연 손님은 캐시된 9차 번들을 그대로 쓰고(정상 동작), 저장 후 요청부터 새 번들을 받는다. 번들 URL 의 `t=` 가 저장 시각으로 갱신되어 **옛 코드가 캐시에 남지 않는다**(홈 4차에서 실측한 것과 같은 메커니즘).
- ★ **배포표의 「저장해도 쇼핑몰은 1초도 영향받지 않는다」는 적용 후에는 거짓이다.** 대표님이 지금 적용을 진행 중이므로, 배포표 맨 앞에 **③ 1파일 안내 + 「적용 뒤에는 저장 즉시 라이브가 바뀐다 · 중간 깨짐은 없다 · 손님 적은 시간 권장 · 되돌리려면 9차 `7044b38e`(36,541 B) 재붙여넣기」** 를 넣었다 (`00_읽어주세요.md` md5 `6d3721612afff39b2d309725e5da41aa`). 10차 확인 4항목(재탭 해제 · 수량3에서도 전체 해제 · 추가상품 불간섭 · 팝업 없음)도 같이 넣었다.
- 9차 파일을 되돌리기용으로 `3b_` 로 넣을지는 대표님 요청 시에 하기로 적어 두었다(파일 수를 늘리면 혼동 위험).

## 실기 확인 필요
| # | 항목 |
|:--:|---|
| 1 | 재탭 시 팝업 없음 — 원문상 확정이나 실기 1회 확인 |
| 2 | 모바일 **품절** 구성에서의 재탭 해제(그 행만 `a.delete` 클래스가 없다 — 선택자는 통과하나 실물 미확인) |
| 3 | 적용된 라이브에서 저장 직후 새 번들 반영 — 저장 뒤 내가 curl 로 확인한다 |

## 11차 이월 (판정 무관, 기록만)
- `detail-ui.js:392` 주석 "multi 는 재탭해도 해제하지 않는다" → **사실과 어긋남**(신아린 P2).
- `[검수 근거]` 토큰 17개(`detail-ui.js` 5 · `detail.css` 10 · `zg-ga4.js` 2) 일괄 정리.

**QA(카페24 실환경) 오세진** · 2026-09-09

---

# 추기 — **11차 R-7 (`detail.css` `492bbe8e` / 49,186 B) 배포 판정: PASS**

**작성** QA(카페24 실환경) 오세진 · 2026-09-09 (10차 판정서에 추기)
변경 1파일 `ds/css/detail.css` `492bbe8ecf4ec2bffb2f2a09dec1a440` (전달본 `2_` md5 일치) · 다른 6파일 불변. 기하·기능은 신아린 PASS 인용.

## 1. 배포 절차 — **② `2_ds-css-detail.css.txt` 1파일뿐** ✔

- 변경 내용은 CSS 규칙 **1블록 삭제**(`.zg-detail #totalProducts > table tbody tr.add_product:not(#totalProductsOption){background;box-shadow}`). `@css` 선언·파일명·번들 순서·다른 파일 모두 불변 → **재붙여넣기 1개**.
- **★ 10차 `detail-ui.js` 는 이미 라이브에 반영돼 있다** — 라이브 사용자 JS 번들에서 `function unpick` 1건·`구성을 뺐어요` 1건 확인. 이번에 다시 올리실 필요 없다.
- **중간 상태 없음**: CSS 파일 1개 교체이고, 번들은 요청 단위로 통째 생성된다 — "절반만 반영된 CSS"가 존재할 수 없다. 저장 전 접속자는 캐시된 옛 번들(회색 배경)을 그대로 쓰고, 저장 후 요청부터 새 번들을 받는다. **HTML·JS 를 건드리지 않으므로 마크업/동작 불일치도 불가능**하다.
- **삭제 후 회색이 남지 않는가 — 확인함.** 라이브 CSS 번들 전수에서 `tr.add_product` 를 다루는 규칙은 21곳이지만 **배경·그림자를 칠하는 것은 우리 1블록뿐**이고, 스킨 규칙은 `padding-right`·`margin`·`border-top`·`display` 만 만진다. 우리 블록이 빠지면 그 행은 **본품 행과 같은 기본 흰 배경**이 된다. 여백을 건드리지 않았으므로 8차에서 잡은 가격 칸 절대배치도 그대로다.
- **되돌리기**: 같은 자리에 9차본(`e8e10b3f`, 49,280 B) 재붙여넣기 1개.

## 2. 번들 캐시 — `t=` 가 저장 시각으로 갱신된다 ✔ (실측 근거)

지금 라이브(적용 상태) 상세 11의 사용자 번들:

| 번들 | `t=` | UTC 환산 | 뜻 |
|---|--:|---|---|
| CSS | `1788854935` | 2026-09-08 08:08:55Z | **9차 CSS 저장 시각** — 아직 11차 전 |
| JS | `1788929230` | 2026-09-09 04:47:10Z | **10차 `detail-ui.js` 저장 시각** |

두 값이 서로 다르게, 각자의 저장 시각을 따라간다 = **파일을 저장하면 그 타입의 번들 URL 이 바뀐다.** URL 이 바뀌므로 브라우저·CDN 캐시가 옛 회색 배경을 계속 쓸 수 없다.
확인용 문자열: 지금 라이브 CSS 번들에는 `tr.add_product:not(#totalProductsOption){background: var(--zg-bg-2);box-shadow: inset 3px 0 0 0 var(--zg-hair-2);}` 가 **1건 있다.** 저장 뒤 이 문자열이 **0건**이 되면 반영 완료다(내가 curl 로 확인한다).

**배포표 갱신 완료** — 「이번엔 ② 1파일 / 10차 JS 는 이미 반영됨 / 적용 상태라 저장 즉시 라이브지만 CSS 1개라 중간 깨짐 없음 / 되돌리기 9차본 / 11차 확인 3항목」. (`00_읽어주세요.md` md5 `0bab2c1e4860bf9230225a5c1dd0cacc`)

## 3. (예고) R-3 B안 확정 시 내가 볼 실기 항목

시트가 **닫힌 상태**에서 하단바 버튼이 카페24 위임 핸들러에 실제로 도달하는지가 선행 조건이다. 확정되면 다음을 본다:
① 하단바 버튼이 `#totalProducts`/구매 폼 **바깥**으로 옮겨져도 카페24 위임(`EC$(document).on(...)`)이 document 레벨이라 도달하는가 — 원문상 도달하지만 `closest()`·`parents()` 로 컨텍스트를 찾는 핸들러는 **행 안에서만** 성립한다(예: `.option_box_del` 은 `parents('tr,li')`) → 하단바에서 부를 수 있는 것과 없는 것을 목록으로 가른다.
② 시트가 `display:none`/`visibility:hidden` 일 때 그 안의 `select`·`input` 값 읽기·`click()` 이 유효한가(숨은 요소 클릭은 되지만 포커스·스크롤 의존 코드는 다르다).
③ `body.eMobilePopup`·`html.scroll-disabled` 상태에서 시트 열닫이 히어로/스크롤 위치를 흔들지 않는가(홈 조건 ④와 같은 계열).

**QA(카페24 실환경) 오세진** · 2026-09-09
