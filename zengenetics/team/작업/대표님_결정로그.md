
---
# ✅ 대표님 결정 확정 — 2026-09-07 (2차)

| # | 항목 | 결정 | 담당 |
|---|---|---|---|
| 5 | 홈 상품 진열 8종(BEST 5 + NEW 3) | **삭제 승인 — 새 홈만** | 홈 설계 |
| 6 | 히어로 「아침부터 밤까지」 중복 | **설명문에서만 뺀다** (제목은 그대로) | 홈 설계 |
| 7 | 칼륨 맛 표기 | **전부 「라임」으로 되돌림** | 홈·상세 전역 |
| 8 | 브랜드스토리 본문 수치 | **전부 뺀다** | 브랜드스토리 설계 |

## #5 홈 진열 8종 삭제 — 승인
- 프로토타입 홈 그대로 간다: 히어로 → 제품 3블록 → `#store` 썸네일 → 푸터
- 진열 8종과 배너매니저 슬롯은 **홈에서만** 빠진다. **상품 자체는 그대로 판매한다.**
- 부수 효과(오세진 F-5 실측): UA diff 38줄 중 장바구니 아이콘 8곳이 전부 진열 8종 안이므로,
  진열을 지우면 **UA 차이가 1줄(`CAFE24.MOBILE_DEVICE`)로 준다.** 칼륨 F-7 유형 재발 경로가 사라진다.

## #6 히어로 중복 — 설명문에서만 제거
- 제목 「아침부터 밤까지, 젠제네틱스」 **유지** (대표님 문장, 한 글자도 안 건드림)
- 설명문 → 「하루에도 필요한 순간은 다르니까. 시간에 맞춰 챙기는 건강 루틴.」
- 제목에서 지우지 않는다. 한 화면에 두 번 나오는 것만 없앤다.

## #7 맛 표기 — 「라임」으로 통일 (되돌림)
- **HOME-03 의 「레몬라임향」 → 「라임향」 으로 되돌린다.**
- 나머지 27건은 이미 「라임」이므로 **손대지 않는다.**
- 근거: 실물 패키지 인쇄가 `Lime` 이다. 화면과 손님이 받는 제품이 같아야 한다.
- ⚠️ **되돌리는 방향이다.** 27건을 고치는 게 아니라 1건을 원복하는 것이다. 반대로 하지 마라.

## #8 브랜드스토리 수치 — 전부 제거
아래 전부 본문에서 뺀다. 자리를 비워 두고 나중에 확정되면 넣는다.
- 리뷰 26,961건 · 평점 4.9
- 재구매율 92%
- 수출 14개국
- 올리브영 랭킹 1위 · 쿠팡 랭킹 1위
- 그 밖에 출처가 확인되지 않은 모든 수치

**숫자를 지운 자리를 "많은 분들이" 같은 모호한 표현으로 메우지 마라.** 문장 구조를 다시 짜서
수치 없이 성립하는 글로 만든다. 수치가 빠져 문단이 빈약해지면 문단을 합치거나 없앤다.
예외: **배송 정책 문구(3,500원 / 5만원 이상 무료)는 관리자 실측값이므로 그대로 쓴다.**

---
# ✅ 대표님 결정 확정 — 2026-09-08 (3차)

| # | 항목 | 결정 |
|---|---|---|
| 9 | `zg-ga4.js` 수정 (2.002 → **2.003** + 홈 이벤트) | **승인** |

## #9 상세 — 배시우 판정서 `QA계측_홈_2차_2026-09-08.md` §5-1 이 정답지
- `zg-ga4.js:22` `ZG_VER = '2.003'` (주석: 2.003 = 홈 개편판)
- **N-H1 `select_item`** — `.zg-home a[href]` 중 `product_no` 또는 `/category/` 링크 클릭.
  `item_list_name: 'home_'+{ctabar|pblock_pot|pblock_mag|pblock_vitb|thumb|store_all}`, `items:[{item_id,item_name}]`, `sendAlways`
- **N-H2 `zg_hero_stage`** — `document.addEventListener('zg:hero-stage', …)` → 구간당 1회.
  (`home-hero.js` 캡션 전환 시 `CustomEvent('zg:hero-stage')` 발신 1줄은 **홈 3차**에서 — 지금은 홈 4파일 제출 동결 중)
- **N-H3 `hero_mode`** — `zg_scroll_depth` 페이로드에 `isHome` 일 때 `'3d'|'no3d'|'reduced'`
- **N-H6** `detail_expanded` 는 `isProduct` 일 때만 (홈에서 항상 0 붙는 노이즈 제거)
- 주문서 3줄 마지막 줄 → `<script src="/ds/js/zg-ga4.js?v=2003">` (캐시버스터, 아직 라이브에 없으므로 재작업 0)

## 대표님 후속 작업 (수정본 나오면)
1. `ds/js/zg-ga4.js` 1개 파일 다시 붙여넣기 (편집기, 기존 내용 전부 지우고)
2. 주문서 2페이지 3줄은 **새 버전(`?v=2003`)으로** 처음 삽입 — 이전에 드린 3줄은 폐기

## 근거
- 라이브에 `/ds/` 리소스 호출 0회 → `zg-ga4.js` 는 아직 한 번도 발사된 적 없음 → **오염 데이터 0건**
- 안 고치면 새 홈에서 제품 클릭 10종이 GA4에 **한 건도 안 남는다**(배시우 1차 실측 +0 ×10)

---
# 팀장 결정 — 2026-09-08

| # | 항목 | 결정 | 근거 |
|---|---|---|---|
| T-1 | `#kakaoKey` 실제 값을 저장소 `product/detail.html` 에 넣는다 | **넣는다** | 카카오 **JavaScript 키는 공개값**이다(모든 페이지 소스에 노출, 보호 대상은 REST 키). 자리표시가 전달본까지 흘러가 작업본 공유가 불능이 되는 회귀가 실제로 났다. 저장소에 넣으면 장부 E-7 반복 작업이 소멸한다. 오세진 판정 ⓐ·ⓑ 근거. |
| T-2 | 대표님 지금 조치 | **622행 1줄만 수정** (45KB 재붙여넣기 아님) | 오세진 ⓒ. 편집기 Ctrl+F `kakaoKey` → 괄호 문구를 32자리 키로 교체 → 저장 |
| T-3 | 하네스 `a_preserve.py:32` `'#kakaoKey'` 토큰 결함 | **수정** — `id="kakaoKey"` 로 + 값 형식(32-hex) 검사 + 미리보기 curl 라이브 동일성 검사 | 지금 토큰은 `zg-ga4.js:725` 주석만 맞고 `detail.html` 0건이라 **div 가 사라져도 PASS** 한다. 오세진 ⓓ |

---
# 📌 대표님 요청 — 2026-09-09 (시안 먼저, 작업은 나중)

## R-1. 가루 컵 구간(diss 블록) 레이아웃 재설계 — **시안 3안 이상 먼저 제출**

**대표님 원문:**
> "이 가루 컵으로 드랍되는 구간 너무 쓸모없는 이미지 낭비와 페이지 낭비가 심한 거 같은데
> 내용도 없이. 이 부분 컵이랑 이미지 부분 좌측, 그리고 내용(텍스트 및 숫자 카운트)되는 부분을
> 우측으로 넘기는 거 어떨까? 이거 바로 작업하지 말고 시안들 만들어서 먼저 전달줘.
> 시안들 보고 한번 작업 진행해보자."

**지적의 핵심 (수치로 뒷받침됨):**
- 블록 1개가 `height:300vh` 를 먹는데(`home.css:210`) 담긴 내용은 **문장 1줄 + 숫자 2개**뿐이다
- 제품 3종 × 300vh = **900vh**. 홈 전체 문서의 큰 몫을 이 구간이 차지한다
- 이미지도 블록당 24장(pot 192KB / mag 165KB / vitb 162KB, 합계 **519KB**)을 쓴다
- 배시우 3차 측정: **sticky 구간이 스크롤 깊이 분모의 78%** — 즉 GA4 열독 지표도 이 구간이 삼킨다
- 대표님 표현: "쓸모없는 이미지 낭비와 페이지 낭비"

**요청 방향:** 세로로 길게 늘어뜨리는 대신 **좌: 컵/이미지 · 우: 텍스트+숫자카운트** 로 나란히.

**⚠️ 처리 규칙**
1. **코드 먼저 짜지 마라.** 시안(디자인) → 대표님 선택 → 그다음 개발.
2. 시안은 **최소 3안**. 각각 모바일(390) / 데스크톱(1280) 둘 다 보여준다.
   좌우 분할은 데스크톱에선 자연스럽지만 **모바일 390px 에서 좌우로 쪼개면 둘 다 좁아진다** —
   모바일 대안을 안마다 명시해야 한다.
3. **삭제해도 되는 것/안 되는 것**을 안마다 표로 밝힌다. 숫자(525mg·20포)와 문장은 확정 카피다.
4. 시안 단계에서 **프레임 24장을 몇 장까지 줄일 수 있는지** 안별로 제시(용량·체감 절충).
5. 시안은 **PNG 렌더 + 열어볼 수 있는 단일 HTML** 두 형태로 낸다. 아티팩트 링크는 팀원이 못 여니 파일로 준다.

**선행:** 홈 4차(실기 반려 성능 수정) QA 4게이트 종료 후 착수. 단 **시안 제작은 병행 가능**(코드 미변경).

---
# 📌 대표님 요청 — 2026-09-09 (2)

## R-2. 구성 카드 **다시 누르면 선택 해제** — 칼륨 상세

**대표님 원문:**
> "몇박스로 하시겠어요? 부분 클릭하면 선택되는 거 맞아. 근데 **선택한 거 다시 선택하면 선택 해제**되어야 할 것 같아. 그것도 추가작업 필요해 보여"

**현재 동작 (`detail-ui.js:476-479`):** 이미 담긴 카드를 다시 누르면
`toast('이미 담겨 있어요. 수량은 아래에서 조절하세요.')` 만 뜨고 **아무 일도 안 일어난다.**
지우려면 아래 「선택한 구성」 목록에서 지워야 한다.

**요청 동작:** 담긴 카드를 다시 누르면 **그 구성이 빠진다.**

**결정 #1(여러 구성 담기 = `multi`)과 충돌하지 않는다.** 복수 담기는 그대로 두고
「같은 카드 재탭 = 그 카드만 해제」를 더하는 것이다.

**구현 힌트 (오케스트레이터 확인):** `detail-ui.js:510` 에 이미 행 삭제 컨트롤 탐색기가 있다 —
`$('a.delete, .option_box_del, a[class*="delete"], a[class*="Del"]', r.el)`.
카페24 **자기 삭제 버튼을 프로그램으로 누르는 방식**이라, 기획 유나래가 1차에 경고한
"옵션 스크립트 내부 상태 조작"에 해당하지 않는다. 그 경로를 재사용한다.

**⚠️ 적용을 막지 않는다.** 지금 동작도 기능상 정상이고(아래 목록에서 삭제 가능),
이 건은 **다음 판**으로 간다. 대표님 「적용」은 예정대로 진행.

---
# 📌 대표님 실기 지적 — 2026-09-09 (적용 후 첫 실기)

## R-3. 결제 버튼 중복 — 인라인 「장바구니·구매하기」 정리
**원문:** *"결제 버튼이 너무 많아. 장바구니랑 구매하기 하단에 계속 따라다니는 거 있으니,
총 구매금액 아래 장바구니랑 구매하기 버튼은 없애도 될 듯"*

**실물 구조(라이브 실측):**
```
#fixedActionButton.productAction
  └ .buy-btn-wrap
      ├ button.actionCart      onclick="product_submit(2,…)"   장바구니
      └ div.btnSubmit          onclick="product_submit(1,…)"   구매하기
          └ span.benefit-bubble  df-banner-code="detail-bubble"  ← 말풍선이 이 안에 있다
.app-pay-wrap
  ├ #NaverChk_Button      네이버페이
  └ #appPaymentButtonBox  카카오페이
(그 아래) .mobile-fix-footer + 우리 K-12 하단 고정바
```

**⚠️ 주의**
- `onclick="product_submit(…)"` 는 **보존 대상**이다. **HTML 에서 지우지 마라.**
- 말풍선이 구매하기 버튼 **안에** 있다 → 인라인 버튼을 숨기면 말풍선도 같이 사라진다. R-4 와 얽힌다.
- `#fixedActionButton` 은 카페24 자체 스크립트가 참조한다(`.mobile-layer.fixed` 시트 스크롤).
- 네이버페이·카카오페이는 **별도 블록**이라 같이 사라지지 않는다.

## R-4. 카카오 말풍선 텍스트가 잘린다
**원문:** *"카카오 말풍선 텍스트 오류나는거 확인해봐"*
실기 화면: 노란 말풍선의 「카카오로 구매하고 평생 무료배송 받기」가 **아랫줄이 잘려** 반만 보인다.

**실물:** `span.benefit-bubble[df-banner-code="detail-bubble"]` — HTML 에는 `hidden` 이고
**배너매니저 앱이 런타임에 `{#title}`·`style="{#html}"` 를 채운다.** 그래서 서버 HTML 에 문구가 없다.
→ **런타임에서 봐야 한다.** 글자 수·줄바꿈·앱이 넣는 인라인 style 과 우리 `.zg-detail` CSS 의 상호작용을 봐라.

## R-5. 재탭 해제 — **이미 고쳐져 있다. 대표님이 아직 안 붙여넣으셨다.**
대표님 실기 지적 *"상품 선택하고 다시 선택한다고 삭제되는 게 아니네"* 는 **9차본(현재 라이브)** 동작이다.
10차 `f62a2ce9` 가 그것을 고쳤고 QA 4게이트 전원 PASS 했다. 전달 완료(`3_ds-js-detail-ui.js.txt`).
**대표님께 「이미 드린 파일을 넣으시면 된다」고 안내할 것.**

## R-7. 추가상품 행 배경을 본품과 동일하게 (흰색). 「추가」 칩은 유지
**원문:** *"추가 구성 상품 있잖아 저거 색깔이 다른 구성처럼 보여서 부자연스러운데,
기본 상품처럼 흰 배경 해주고 위에 [추가] tag는 그대로 남겨줘"*

**해당 규칙 (`ds/css/detail.css:516-519`)**
```css
.zg-detail #totalProducts > table tbody tr.add_product:not(#totalProductsOption) {
  background: var(--zg-bg-2);                      /* ← 이 회색 톤을 없앤다 */
  box-shadow: inset 3px 0 0 0 var(--zg-hair-2);    /* ← 좌측 세로 레일 */
}
```
「추가」 칩은 `:520-528` `tr.add_product td .product::before { content:"추가" }` — **별도 규칙이라 그대로 둔다.**

**판단 필요:** 좌측 레일(`box-shadow`)도 같이 뺄지. 대표님이 「기본 상품처럼」이라 하셨으므로
**배경·레일 둘 다 빼고 칩만 남기는 것**으로 읽는다. 개발이 전/후 캡처로 확인받는다.

**배경:** 8차에 좌측 레일+배경 톤을 쓴 이유는 `padding-left` 로 구분하면 모바일에서
**가격 칸이 행 padding 기준 절대배치라 어긋나기 때문**이었다(`:512-515`).
배경·레일을 빼는 것은 **레이아웃을 건드리지 않으므로 그 제약과 무관**하다 — 안전하다.
