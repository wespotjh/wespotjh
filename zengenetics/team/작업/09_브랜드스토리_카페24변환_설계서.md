# 브랜드 스토리 → 카페24 변환 설계서 (1차 = 설계서. 코드 없음)

**작성:** 웹/모바일 개발팀 **개발** · 2026-09-07
**대상 원본:** `site/brand-story.html` (2,320,052 B · 오늘 햄버거·웰니스 메뉴 반영 최신본)
**대상 스킨:** `젠제네틱스 v2 작업본` (skin_no=16 / skin_code=skin10) — **라이브 `바디랩 반응형`은 열지 않는다**
**성격:** 조사·판정 문서. **파일은 한 개도 만들지 않았고 스킨도 한 글자도 건드리지 않았다.**

---

# 0. 요약 (먼저 읽을 3줄)

1. **판정 = 「스킨에 새 템플릿 파일」.** 라이브 실측으로 *카페24가 스킨에 실재하는 임의 경로의 `.html` 을 그대로 서빙한다*는 것을 확인했다. 게시판·상품상세·이벤트 경로는 **전부 이미지가 `ec-data-src` 로 바뀌는 경로**라 이 페이지(`.cover{overflow:hidden}` 15장)와 정면충돌한다.
2. **라이브에 이미 「브랜드 소개」 페이지가 있다 — `/about.html`.** 다만 내용이 **배너매니저 앱으로만 채워지는 빈 껍데기**다(현재 등록 배너 0건 → 본문 없음). 우리 페이지의 목적지로는 부적합하고, **푸터 「회사소개」 링크의 행선지**로만 남긴다.
3. **가장 큰 함정은 이미지가 아니라 웹폰트다.** 프로토타입이 부르는 Google Fonts 한글 2종이 이 페이지에서 실제로 내려받는 양을 실측했더니 **100 요청 / 1.23 MB** 였다. 이미지 전체(1.63 MB)에 맞먹는다. **본문 IBM Plex Sans KR 은 버리고 스킨의 Pretendard 를 쓰고, 제목 Noto Serif KR 은 서브셋 자체호스팅(2 파일 47 KB)으로 돌린다 → 1.23 MB → 47 KB.**

---

# 1. ★ 판정 — 페이지를 어디에 어떻게 만드는가

## 1-1. 결론

> **스킨 `젠제네틱스 v2 작업본` 의 최상위에 새 화면 `brand-story.html` 을 추가한다.**
> **공개 URL = `https://zengenetics.co.kr/brand-story.html`**
> 첫 줄은 `<!--@layout(/layout/basic/layout.html)-->` 이다.
> (차선: `ds/html/brand-story.html` — §1-5)

## 1-2. 근거 — 라이브에서 직접 확인한 사실

카페24가 어떤 경로를 서빙하는지, **추측하지 않고 실제로 받아 봤다.**

| 요청 경로 | 결과 | 이 경로가 증명하는 것 |
|---|---|---|
| `/about.html` | **200 · 191,511 B** | 카페24 표준 화면이 아닌 **최상위 커스텀 `.html` 이 서빙된다** |
| `/ds/html/price.html` | **200 · 72,370 B** | `@import` 전용 조각 파일도 **경로만 맞으면 그대로 서빙된다** |
| `/moa/layout/head.html` | **200 · 138,001 B** | 같음 (레이아웃 조각도 서빙됨) |
| `/moa/import/product_detail/detail.html` | **200 · 73,851 B** | **임의 깊이의 하위 폴더**도 서빙된다 |
| `/layout/basic/layout.html` | 200 · 200,034 B | 같음 |
| `/brand.html` · `/story.html` · `/brand/story.html` · `/html/brand-story.html` | **전부 404 (3,415 B 동일)** | 404 는 「경로가 막혀서」가 아니라 **「그 파일이 스킨에 없어서」**다 |
| `/ds/css/detail.css` · `/ds/js/zg-ga4.js` | **404** | 아직 작업본에만 있고 **적용 전**이라 그렇다 (QA 4차 §(a) 와 일치) |
| `/ds/css/price.css` · `/ds/image/ico_price1.png` · `/moa/fonts/pretendard/*.woff2` | 200 | 스킨의 **css·이미지·폰트 파일도 경로대로 서빙**된다 |

**⇒ 카페24 = 「URL 경로 → 스킨 파일」 단순 매핑.** 관리자에서 URL 을 따로 등록하는 절차가 없다. 파일을 만들면 그 경로가 곧 URL 이다.

**서빙 방식도 확인했다:** `/ds/html/price.html` 은 `@layout` 지시자가 없어서 **레이아웃 없이 알맹이만** 내려온다(`<html>` 0개, 헤더·푸터 0개). 즉 **`<!--@layout(...)-->` 한 줄이 헤더/푸터/GA4 를 붙이는 스위치**다. 우리 페이지는 반드시 이 줄을 첫 줄에 둔다.

## 1-3. 나머지 3안을 버린 이유 (전부 실측 근거)

### ❌ 안2. 게시판(`/board/...`)
- 실물: 「복용법」 = `/board/gallery/list.html?board_no=8`, 「고객센터」 = `/board/free/list.html?board_no=1`.
- **치명적:** 게시판 본문은 관리자 에디터가 입력하고, 카페24는 그 안의 `<img>` 를 **`ec-data-src` 로 바꿔 지연로딩**한다(이벤트 페이지 실측: `<img ec-data-src="/web/upload/NNEditor/...">` 6장).
  이 페이지는 `.cover{overflow:hidden}` 인 전면 커버가 **5장**이다. **칼륨 브리프가 경고한 바로 그 함정** — 잘린 영역 안에서는 IntersectionObserver 가 교차하지 않아 영영 안 뜬다.
- URL 이 `?board_no=8&no=NN` 형태라 브랜드 페이지로 쓸 주소가 못 된다.
- 게시글 본문에 `<style>`/`<script>` 를 넣는 것은 관리자 에디터의 필터링에 의존한다 — 우리가 통제할 수 없다.
- 게시판 껍데기(제목·작성자·목록/이전글 버튼·댓글)를 지울 수 없다.

### ❌ 안3. 상품 상세설명(관리자 콘텐츠)
- 더미 상품을 만들어야 한다 → 분류·가격·장바구니·구매하기 버튼·재고가 딸려온다. 실수로 노출되면 **판매 사고**다.
- 상세설명(NNEditor)도 `ec-data-src` 경로다(위와 동일한 함정).
- 칼륨 브리프 §3 「상세설명(NNEditor) 내용 수정 금지」와 충돌한다.

### ❌ 안4. 「추가 페이지」 기능
- 카페24 스마트디자인에 *디자인 관리의 별도 「추가 페이지」 메뉴* 는 없다. **「추가 페이지」에 해당하는 실체는 두 가지뿐**이고 둘 다 확인했다:
  1. **디자인 편집기의 [화면 추가]** — 결국 **안1과 같은 것**이다(스킨에 파일을 만드는 행위). 대표님이 이미 `ds/css/detail.css`·`ds/js/detail-ui.js` 를 이 방식으로 만들기로 되어 있다(QA 4차 체크리스트 ②③).
  2. **이벤트(기획전)** — 실물 확인: 「멤버십 혜택」이 `/event/detail.html?product_no=93&cate_no=61` 이다. 관리자에서 내용을 넣지만 **본문이 이미지 나열 + `ec-data-src`** 이고, 하단에 「이벤트 진행 상품」 슬라이드가 강제로 붙는다. 우리 페이지는 타이포그래피·다크모드·카운트업이 본체라 **전부 그림으로 눕히는 순간 페이지의 이유가 사라진다.**

### ✅ 안1을 고른 결정적 이유 3가지
1. **이미지가 `ec-data-src` 를 타지 않는다.** 스킨 템플릿 안의 `<img src>` 는 카페24가 건드리지 않는다(라이브 스킨 이미지 `/moa/img/default/flag_kr.svg` 가 평범한 `src` 로 나온다). `.cover{overflow:hidden}` 문제가 **원천적으로 발생하지 않는다.**
2. **버전관리·되돌리기가 칼륨과 같은 규칙 위에 놓인다.** 저장소 git ↔ 전달본 `.txt` ↔ md5 대조 체계를 그대로 쓴다. 관리자 콘텐츠는 이 체계 밖이라 되돌릴 수 없다.
3. **URL 이 사람이 읽을 수 있다** (`/brand-story.html`). 메뉴·광고·인스타 프로필에 그대로 쓴다.

## 1-4. 배포 시점 — 반드시 알아야 할 것

**작업본 스킨은 지금 적용되어 있지 않다.** 따라서 이 파일을 저장해도 라이브는 변하지 않고, **칼륨 상세 배포의 「⑨ 적용」 한 번에 브랜드 스토리도 같이 공개된다.**
→ 칼륨 배포와 **같은 판에 실을지, 다음 판으로 뺄지**는 팀장·대표님 결정 사항이다(§7 A-1).

## 1-5. 경로 선택 — 최상위 vs `ds/html/`

| | `/brand-story.html` (권장) | `/ds/html/brand-story.html` (차선) |
|---|---|---|
| URL 품질 | 좋음 | 나쁨 (내부 폴더가 노출) |
| 폴더 신설 | **없음** (최상위엔 `index.html`·`about.html` 이 이미 있다) | **없음** (`ds/html/` 존재) |
| [화면 추가] 확실성 | 최상위 폴더 선택 가능 여부를 **편집기에서 눈으로 봐야 함** | `ds/` 계열은 이미 같은 방식으로 파일을 만들기로 확정 |
| 이름 충돌 | `/brand-story.html` **현재 404** = 비어 있음(확인) | `/ds/html/brand-story.html` **현재 404** = 비어 있음(확인) |

> **폴더는 새로 만들지 않는다.** 지난 `ga4.js` 폴더 사고의 규칙(QA 체크리스트 ①)을 그대로 지킨다.
> 편집기에서 최상위에 [화면 추가]가 안 되면 **그 자리에서 멈추고 알려 주시면** 차선으로 전환한다(파일 안의 코드는 한 글자도 안 바뀐다. 이미지 경로만 그대로).

---

# 2. 라이브 조사 결과

## 2-1. 기존에 브랜드스토리 비슷한 페이지가 있는가 → **있다. `/about.html`**

```
GET /about.html  →  200 · 191,511 B
현재 위치: 홈 > 브랜드 소개
<div class="hero" df-banner-code="about-top" hidden> … </div>
<div class="about"   df-banner-code="about"     hidden> … </div>
```

- **모아스튜디오 스킨이 제공하는 「브랜드 소개」 페이지**다.
- 본문이 **전부 배너매니저 앱(`df-banner-code`) 슬롯**이고, 두 슬롯 다 `hidden` = **등록된 배너가 0건이라 화면에 아무것도 없다.**
- 진입점: **푸터 「회사소개」** (`policy__item display-m` — 모바일에서만 보임). 사이트맵에는 없다.

**판단:** 이 파일을 우리 페이지로 덮어쓰지 않는다.
① 덮으면 모아 배너매니저 연동이 사라져 되돌리기가 어렵다(원본 소스를 우리가 갖고 있지 않다).
② 「회사소개」(법인 정보 성격)와 「브랜드 스토리」(서사)는 성격이 다르다.
③ 우리 페이지는 새 URL 로 두고, 나중에 대표님이 원하시면 `/about.html` 을 `/brand-story.html` 로 리다이렉트/링크만 바꾸면 된다.

## 2-2. 사이트맵 — 정적 페이지가 무엇인지

`/sitemap.xml` 200 · 3,318 B · **URL 23개**뿐:
- 홈 1 · `shopinfo/company.html` · `member/agreement.html` · `member/privacy.html` · 게시판 1 · 분류 7 · 상품 12
- **`/about.html` 은 사이트맵에 없다.** → 카페24 사이트맵은 **관리자에 등록된 것(상품·분류·게시판·약관)만** 넣는다. **스킨 커스텀 페이지는 자동으로 안 들어간다.**
- **⇒ `/brand-story.html` 도 사이트맵에 자동 등록되지 않는다.** §7 E-3 에 대표님 작업으로 올린다.

## 2-3. 메뉴 구조 — 어디를 고쳐야 「웰니스 ▸ 브랜드 스토리」가 붙는가

메뉴는 **관리자 분류가 아니라 스킨 마크업**이다. 라이브 HTML 에 편집 안내 주석이 그대로 남아 있어 위치가 명확하다.

| 화면 | 스킨 파일 | 현재 마크업 | 하위메뉴 패턴 |
|---|---|---|---|
| 데스크톱 가로 nav | `moa/layout/header.html` | `<li class="category__item">` + `<!-- 🔽 리뷰 메뉴 -->` 주석쌍 | 「이벤트」가 `<div class="sub-category"><ul class="sub-category__list">` 로 **이미 드롭다운을 쓰고 있다** |
| 모바일 햄버거 패널 | `moa/layout/aside.html` (`xans-layout-slidepackage`) | `<li class="aside-category__item">` + 같은 주석쌍 | 「이벤트」가 `<a class="aside-category__toggle cate">` + `<ul class="aside-child">` 로 **이미 아코디언을 쓰고 있다** |

**⇒ 04 문서가 프로토타입에 새로 만든 드롭다운/아코디언을 라이브로 옮길 필요가 없다. 스킨에 이미 있는 「이벤트」 블록을 복제해 「웰니스」로 바꾸고, 「리뷰」 블록을 지우면 끝이다.**

### 04 문서 §6 「경로 확인 필요」 2건 — **여기서 해소됐다**

라이브 `aside` 마크업에서 실제 값을 읽었다:

| 메뉴 | 04 문서 | **라이브 실제 값 (확정)** |
|---|---|---|
| 멤버십 혜택 | `#` | `https://zengenetics.co.kr/event/detail.html?product_no=93&cate_no=61&display_group=1` |
| 고객센터 | `#` | `/board/free/list.html?board_no=1` |

프로토타입 `home.html` · `brand-story.html` 의 드로어 `href="#"` 2곳씩(총 4곳)을 이 값으로 바꾸면 04 문서 §6 이 닫힌다. (이번 과제 범위 밖 — §7 B-4 로 올린다.)

### 프로토타입이 가리키는 「브랜드 스토리」 링크가 틀려 있다

`site/brand-story.html` · `site/home.html` 의 드로어·nav 에서 「브랜드 스토리」가
`https://claude.ai/code/artifact/1fd7a5ca-973c-4731-b9db-af263ab98343` (아티팩트 미리보기)를 가리킨다.
**배포 후 `https://zengenetics.co.kr/brand-story.html` 로 바꿔야 한다.** (§7 B-3)

## 2-4. `/board/` 계열 URL 실측

| 메뉴 | URL | board_no |
|---|---|---|
| 리뷰 (메뉴에서 제거 예정) | `/board/review/list_photo.html?board_no=4` | 4 |
| 복용법 | `/board/gallery/list.html?board_no=8` | 8 |
| 고객센터(1:1) | `/board/free/list.html?board_no=1` | 1 |
| 상품후기 | `/board/product/list.html?board_no=6` | 6 |
| 자유/기타 | `/board/free/list.html?board_no=3` | 3 |
| 공지사항 | `/board/공지사항/1/` (사이트맵) | 1 |

---

# 3. 프로토타입 → 카페24 매핑표

프로토타입 본문은 **18블록**이다. 헤더/푸터/드로어는 **스킨이 이미 갖고 있으므로 전부 버린다.**

| # | 프로토타입 블록 | 카페24 목적지 | 작업 성격 |
|---|---|---|---|
| **B-00** | `<head>` · `<title>하루를 고르게</title>` · Google Fonts `<link>` 3줄 | **삭제.** head 는 `moa/layout/head.html` 이 전 페이지에 이미 싣는다 | **삭제** (칼륨에서 배운 것: 폰트·GA4 재삽입 금지) |
| **B-01** | `.top` 상단바(로고 + nav) | **삭제.** 스킨 `moa/layout/header.html` | 삭제 |
| **B-02** | `.zgb` 햄버거 + `#zgm` 드로어 (마크업 1,160 B + CSS 2,602 B + JS ~1.3 KB) | **삭제.** 스킨 `.jsHamburger` + `moa/layout/aside.html` 이 이미 동작한다 | **삭제** (중복 메뉴 2벌 방지) |
| **B-03** | `<header class="cover first">` 첫 커버 + `<h1>숨을 고르는 하루</h1>` | 페이지 본문 최상단 | 마크업 + CSS. **`<img src>` 로 즉시 로드(LCP)** |
| **B-04** | `.open` 리드 문단 | 본문 | 마크업 + CSS |
| **B-05~B-16** | 커버 4 · `.sec` 8 · `.sec.after` 3 · `.breath` 1 (01~10 넘버링 섹션) | 본문 | 마크업 + CSS |
| **B-17** | `.nums` 숫자 카운트업 4개 (26,961 / 4.9 / 92% / 14) | 본문 | 마크업 + CSS + **JS(카운트업)** — **§8 R-5 사실성 확인 필요** |
| **B-18** | `.mapbox` 세계지도 + `.pin` 8개 + `.flags` | 본문 | 마크업 + CSS (지도는 `<img src>`) |
| **B-19** | `.close` 마감 + 제품 3장(칼륨·비타민B·마그네슘) + 「제품 보러 가기 →」 | 본문 하단 | 마크업 + CSS. **제품 3장에 실제 상품 링크(11·13·16) 부여 권장** |
| **B-20** | `<footer>` 회사정보 4줄 | **삭제.** 스킨 푸터가 **똑같은 정보를 이미 출력한다**(상호/대표/주소/사업자번호/통신판매업/개인정보책임자/E-mail) | **삭제** (정보 2벌 = 어긋남의 원인) |
| **B-21** | `window.__MAP__` / `__PHOTO__` 데이터URI 15장 + 주입 JS | **삭제.** 실제 파일 15장 + `<img src="/ds/image/…">` | **삭제 + 대체** |
| **B-22** | 카운트업·IntersectionObserver 스크립트 | `ds/js/brand-story.js` | JS 이관 |
| **B-23** | 인라인 `<style>` 16,794 B | `ds/css/brand-story.css` | CSS 이관 + **네임스페이스** |
| **NEW-1** | — | `<div class="path">` 빵부스러기 「홈 > 브랜드 스토리」 | **신규** (`/about.html` 과 같은 형태로 통일) |
| **NEW-2** | — | `document.title` · `meta description` 설정 | **신규** (§8 R-4) |

## 3-1. 마크업을 감싸는 껍데기 (카페24 레이아웃 안에서의 위치)

```
<body>                          ← 카페24 (커스텀 페이지는 id·class 없음. 실측)
 └ #wrap → #container → main#contents   ← layout/basic/layout.html 의 <!--@contents-->
      └ ★ 여기부터가 우리 파일의 내용
         <div class="path">…</div>            ← NEW-1
         <div class="zgbs"> … 본문 18블록 … </div>   ← 모든 스타일의 스코프 루트
```

`main#contents` 에 스킨이 이미 거는 규칙(실측):

| 뷰포트 | `#contents` 규칙 | 출처 |
|---|---|---|
| ≥1025px | `max-width:1280px; min-height:800px; padding:0 50px 200px; margin:0 auto` | `layout/basic/css/layout.css:94` |
| ≤1440px | `padding-left/right:50px` | `moa/css/layout.css:2953` |
| ≤1024px | `padding-left/right:16px` | `moa/css/layout.css:2980` |
| ≤768px | **`padding-left/right:0`** | `moa/css/layout.css:3593` |
| ≤1024px(전역) | `padding-bottom:100px` | `layout/basic/css/layout.css:57` |

**⇒ 모바일(≤768px)은 좌우 패딩이 0이라 전면 커버가 그대로 붙는다. 데스크톱만 최대 1280px 안에 갇힌다.**
전면 출혈(full-bleed)이 필요하면 `.zgbs .cover{margin-left:calc(50% - 50vw);width:100vw}` 형태로 처리한다. **`#contents` 자체의 규칙은 건드리지 않는다** — 전 페이지에 영향이 간다.

---

# 4. CSS / JS 충돌 분석 — 무엇을 어떻게 네임스페이스하는가

## 4-1. 실측한 충돌

- 전 페이지에 실리는 스킨 전역 CSS **17개 파일 · 188,912 B · `!important` 98개**
- 프로토타입이 쓰는 클래스 **75개**를 전수 대조:

| 충돌 클래스 | 스킨 셀렉터 수 | `!important` 포함 |
|---|--:|--:|
| `.spec` | 66 | **11** |
| `.wrap` | 11 | 0 |
| `.close` | 5 | 0 |
| `.aside` | 5 | 0 |
| `.top` | 4 | 0 |
| `.grid` | 1 | 0 |
| 나머지 69개 | 0 | 0 |

- **더 위험한 것은 요소 셀렉터다.** 스킨 전역 CSS 의 맨몸 셀렉터: `li` **71**, `img` **42**, `h2` **15**, `h3` **13**, `ul` **13**, `p` **10**, `table` **5**. 프로토타입은 `<h2> <p> <ul> <li> <img> <table>` 을 그대로 쓴다.
- **CSS 변수는 충돌 0.** `--paper --panel --ink --rule --deep --accent --serif --sans --mono` 전부 스킨에 0회 (`--m` 만 7회 — **이름을 `--zg-m` 으로 바꾼다**).

## 4-2. 방침 — 칼륨과 같은 규칙

1. **래퍼 `.zgbs` 1개**로 전부 감싸고 **모든 셀렉터에 `.zgbs` 를 접두**한다 → 구체도 +1 로 스킨을 이긴다.
   `.spec`(66개·`!important` 11개)도 `.zgbs .spec` 이면 `!important` 를 제외하고는 전부 이긴다. **`.spec` 는 아예 `.zg-spec` 으로 개명해 회피한다.**
2. **새 `!important` 0개.** (칼륨 정정 5 규칙 승계)
3. `body{}` · `*{}` · `:root{}` **전역 규칙 금지.** 프로토타입의 `body{margin:0;background:…;font-family:…;overflow-x:hidden}` 을 그대로 옮기면 **쇼핑몰 전 페이지가 바뀐다.** → `.zgbs{…}` 로 내린다. 변수는 `.zgbs{--zg-paper:…}` 에 선언한다(변수도 `--zg-` 접두).
4. 단문자 클래스(`.c .d .m .n .r .s .t .v .h`)는 **전부 `.zg-` 접두**로 개명한다. 지금은 충돌 0이지만 스킨이 갱신되면 즉시 깨진다.
5. `zgm-*` `zgb` `zgs*` (드로어) 클래스는 **파일에서 통째로 삭제**한다(B-02).
6. `viewport` 는 스킨이 `width=device-width, user-scalable=no` 로 고정 — 프로토타입의 `initial-scale=1` 은 **넣지 않는다**(head 는 우리 파일이 아니다).

## 4-3. 다크모드 — 결정 필요

- **스킨에 `prefers-color-scheme` 이 0회**다. 헤더·푸터·햄버거 패널은 **항상 라이트**다.
- 프로토타입은 완전한 다크 팔레트를 갖고 있다. 그대로 켜면 **본문만 검고 헤더·푸터는 흰** 화면이 된다.
- **권장(설계 기본값): 다크 팔레트를 삭제하지 않고 `.zgbs` 안에 그대로 두되 기본 비활성**으로 둔다. 한 줄(`@media (prefers-color-scheme:dark)` 블록 주석 해제)로 켤 수 있게 남긴다. → **대표님 결정 항목** (§7 A-2)

---

# 5. 이미지 처리 — 정확한 수치와 업로드 방법

## 5-1. 실측 (base64 를 전부 디코딩해 헤더의 실제 치수를 읽었다)

**총 15장 · 원본 합계 1,708,934 B (1.63 MB)** · 파일 안에서는 base64 라 **2,278,596 B (2.17 MB)** 를 차지한다 = **파일 2,320,052 B 의 98.2%**.

| # | 문서 순서 | 요소 id | 원본 형식 | 치수 | 원본 바이트 | 권장 파일명 (`/ds/image/`) | 배포 바이트 |
|--:|---|---|---|---|--:|---|--:|
| 1 | 커버 1 (첫 화면·LCP) | `ph-c0` | WebP | 1400×1628 | 235,380 | `bs-cover-1.webp` | 235,380 |
| 2 | 커버 2 | `ph-ch1` | WebP | 1400×933 | 36,290 | `bs-cover-2.webp` | 36,290 |
| 3 | 02 초원의 나무 | `ph-nature` | WebP | 1100×619 | 136,974 | `bs-nature.webp` | 136,974 |
| 4 | 03 아침 테이블의 잔 | `ph-cup` | WebP | 1100×619 | 15,120 | `bs-cup.webp` | 15,120 |
| 5 | 커버 3 | `ph-ch3` | WebP | 1400×932 | 64,876 | `bs-cover-3.webp` | 64,876 |
| 6 | 04 아침 | `ph-am` | WebP | 760×570 | 81,926 | `bs-am.webp` | 81,926 |
| 7 | 04 한낮 | `ph-noon` | WebP | 760×570 | 92,346 | `bs-noon.webp` | 92,346 |
| 8 | 04 밤 | `ph-pm` | WebP | 760×570 | 19,190 | `bs-pm.webp` | 19,190 |
| 9 | 커버 4 | `ph-ch6` | WebP | 1400×934 | 270,902 | `bs-cover-4.webp` | 270,902 |
| 10 | 08 호수의 해 | `ph-lake` | WebP | 1100×619 | 28,844 | `bs-lake.webp` | 28,844 |
| 11 | **09 수출 지도** | `#worldmap` | **PNG (RGBA)** | 1920×720 | **353,920** | `bs-map.webp` | **198,392** ★ |
| 12 | 09 도시 1 | `ph-c1` | WebP | 760×570 | 42,154 | `bs-city-1.webp` | 42,154 |
| 13 | 09 도시 2 | `ph-c2` | WebP | 760×570 | 87,012 | `bs-city-2.webp` | 87,012 |
| 14 | 09 도시 3 | `ph-c3` | WebP | 760×570 | 60,502 | `bs-city-3.webp` | 60,502 |
| 15 | 커버 5 (마지막) | `ph-end` | WebP | 1133×1700 | 183,498 | `bs-cover-5.webp` | 183,498 |
| | | | | | **1,708,934** | **15개 파일** | **1,553,406** |

## 5-2. 방침

1. **재압축하지 않는다 (14장).** WebP 14장을 quality 82 로 다시 인코딩해 봤더니 **오히려 커졌다**(1,355,014 → 1,425,576 B). 이미 최적이다. **그대로 뽑아 올린다.**
2. **지도 1장만 PNG → 무손실 WebP.** 353,920 → **198,392 B (−44%)**. 무손실이라 화질 손실 0이고 **알파 채널이 보존**된다(다크모드에서 배경이 비친다). *(손실 q82 는 91,770 B 지만 지도의 국경선·글자가 뭉개져서 쓰지 않는다.)*
3. **총 전송량 = 1,553,406 B ≈ 1.48 MB / 15 요청.** 단, 첫 화면에는 **커버 1(235,380 B) 1장만** 필요하다 — 나머지 14장은 `loading="lazy"` 로 내린다.
4. 커버 이미지에는 **`loading="lazy"` 를 붙이지 않는다**(첫 화면 LCP). 나머지 전부에 붙인다.
5. **`ec-data-src` 를 쓰지 않는다.** 우리가 쓰는 평범한 `<img src>` 는 카페24가 건드리지 않으므로 `.cover{overflow:hidden}` 함정이 발생하지 않는다.
6. 모든 `<img>` 에 **`width`/`height` 속성**을 위 실측 치수로 넣어 CLS 를 0으로 만든다. *(칼륨 자기정정의 교훈 — 치수 상수는 **실제로 내려받는 파일의 헤더**에서 읽는다. 위 표가 그 값이다.)*

## 5-3. ★ 대표님 업로드 방법 (정확히 어디에 어떻게)

**개발이 준비해 드리는 것:** `전달/2026-09-XX_브랜드스토리/img/` 폴더에 **위 표의 파일명 그대로 15개 파일**.

**올리시는 곳: 카페24 관리자 → 디자인(PC/모바일) → 디자인 보관함 → `젠제네틱스 v2 작업본` → [디자인 편집]**
(편집기 상단 이름이 **`젠제네틱스 v2 작업본`** 인지 눈으로 확인. **`바디랩 반응형`(라이브)은 열지 않습니다.**)

1. 왼쪽 파일 목록에서 **`ds` → `image` 폴더를 클릭해 엽니다.** (이 폴더는 이미 있습니다 — `ico_price1.png`, `ico_price2.webp` 가 들어 있습니다.)
2. **`ds/image` 폴더를 연 상태에서 [파일 업로드]** 를 누릅니다. **❌ [폴더 추가]가 아닙니다. 새 폴더는 하나도 만들지 않습니다.**
3. 15개 파일을 **한 번에 선택**해 올립니다. (한 번에 안 되면 5개씩 3번 — **순서는 상관없습니다.**)
4. 올린 뒤 목록에 **`bs-` 로 시작하는 파일이 15개** 보이는지 세어 주십시오. 15개가 아니면 빠진 것만 다시 올리면 됩니다.
5. **파일명을 바꾸지 말아 주십시오.** 페이지가 이 이름으로 이미지를 부릅니다.

**확인 방법(적용 후):** 브라우저에서 `https://zengenetics.co.kr/ds/image/bs-cover-1.webp` 를 열어 사진이 보이면 성공입니다.

**만약 편집기가 `.webp` 업로드를 막으면** — 그 자리에서 멈추고 알려 주십시오. `.png` 판(용량 약 3배)으로 다시 뽑아 드립니다. *(현재 스킨 `ds/image/ico_price2.webp` 가 이미 라이브에서 200으로 서빙되고 있어 webp 자체는 문제없습니다. 막힐 수 있는 것은 업로드 단계뿐입니다.)*

**차선(편집기 업로드가 아예 불가할 때):** 관리자 에디터(게시판 글쓰기 등)로 올려 `/web/upload/NNEditor/…` 경로를 받는다. **단, NNEditor 는 모바일용 639~640px 축소본을 따로 만든다**(칼륨 자기정정에서 101장 전수 확인). 이 경우 **PC 원본 경로가 실제로 200 인지 개발이 먼저 확인한 뒤에** 템플릿에 박는다.

---

# 6. ★ 웹폰트 — 이 변환의 가장 큰 함정 (실측)

프로토타입 `<head>` 는 Google Fonts 를 3종 부른다: **Noto Serif KR(400/600/700) + IBM Plex Sans KR(300/400/500/600) + IBM Plex Mono**.

**이 페이지의 실제 글자 475자를 Google Fonts 의 unicode-range 124개 서브셋에 매핑해 실제 다운로드 바이트를 측정했다:**

| 서체 | 용도 | 실제 요청 수 | 실제 바이트 |
|---|---|--:|--:|
| **IBM Plex Sans KR** | 본문 전체 | **84** | **649,880 B (0.62 MB)** |
| **Noto Serif KR** | 제목·리드 (고유 글자 134자) | **16** | **644,668 B (0.61 MB)** |
| IBM Plex Mono | 라틴 라벨 | (0) | **0 — `moa/layout/head.html` 이 이미 싣고 있다** |
| | | **100 요청** | **1,294,548 B (1.23 MB)** |

**이미지 전체(1.48 MB)에 맞먹는 양이 폰트로 나간다.** 그대로 옮기면 안 된다.

## 6-1. 처방

| 서체 | 처방 | 결과 |
|---|---|---|
| **IBM Plex Sans KR (본문)** | **로드하지 않는다.** `--zg-sans` = 스킨이 이미 자체호스팅 중인 **`'Pretendard Variable'`** (`/moa/fonts/pretendard/*.woff2`, unicode-range 분할, 라이브 200 확인) | **0.62 MB → 0 B, 84 요청 → 0** (칼륨 정정 6 규칙과 동일) |
| **IBM Plex Mono (라벨)** | 그대로 쓴다. `head.html` 이 `Archivo + IBM Plex Mono` 를 이미 비차단으로 싣는다 | **0 B 추가** |
| **Noto Serif KR (제목)** | **서브셋 자체호스팅.** 이 페이지에 실제로 쓰이는 **134자**만 남겨 woff2 로 뽑았다 (fontTools 실측): **400 = 23,868 B · 600 = 23,380 B** | **0.61 MB / 16요청 → 47,248 B / 2요청 (−93%)** |

- Noto Serif KR 은 **SIL Open Font License** — 서브셋·자체호스팅이 허용된다(라이선스 파일 동봉).
- 서브셋의 한계: **나중에 문구를 고치면 없는 글자가 폴백으로 떨어진다.** → 문구 변경 시 개발에 알려 주시면 폰트를 다시 뽑는다. 운영 장부에 규칙으로 올린다(§7 B-5).
- 폰트 파일 업로드가 편집기에서 막히면 **차선: Google Fonts `<link>` 1줄(0.61 MB / 16요청)로 되돌린다.** 코드 한 줄 차이다.
- 세 처방 모두 **`moa/layout/head.html` 을 고치지 않는다.** `@font-face` 는 우리 CSS(`ds/css/brand-story.css`) 안에 둔다 → 브랜드 스토리 페이지에서만 로드된다.

**최종 페이지 무게(예상):** 첫 화면 = HTML ~18 KB + CSS ~16 KB + JS ~2 KB + 커버1 235 KB + 서브셋 폰트 47 KB ≈ **318 KB**. 나머지 이미지 1.32 MB 는 스크롤하며 지연 로드.
*(프로토타입 그대로였다면 첫 화면에만 2.32 MB HTML + 1.23 MB 폰트.)*

---

# 7. 대표님이 관리자에서 하셔야 할 일

> **아래를 다 하셔도 「적용」 전에는 쇼핑몰이 1초도 바뀌지 않습니다.** 바뀌는 순간은 「적용」 한 번뿐입니다.

## A. 먼저 결정해 주셔야 개발이 코드를 씁니다 (2건)

| # | 항목 | 선택지 |
|---|---|---|
| **A-1** | **배포 시점** | (가) 칼륨 상세와 **같은 「적용」에 함께** 공개 / (나) 칼륨을 먼저 안정화한 뒤 **다음 판**에 공개 |
| **A-2** | **다크모드** | (가) **끈다** — 헤더·푸터와 톤을 맞춘다(스킨은 다크모드가 없습니다) / (나) **켠다** — 본문만 어두워지고 헤더·푸터는 흽니다 / (다) 페이지 안에 해/달 토글 버튼을 단다 |

## B. 편집기에서 하실 일 (「적용」 전, 순서대로)

| 순 | 무엇을 | 어디서 | 비고 |
|:--:|---|---|---|
| **①** | **이미지 15개 업로드** | 편집기 → `ds/image` 폴더 → [파일 업로드] | §5-3 |
| **②** | **폰트 2개 업로드** | 편집기 → `ds/image` 폴더 (또는 편집기가 허용하는 폴더) → [파일 업로드] | `bs-serif-400.woff2` 23,868 B · `bs-serif-600.woff2` 23,380 B. **막히면 멈추고 알려 주십시오** |
| **③** | **`ds/css/brand-story.css` 신규** | `ds/css` 폴더를 연 채 **[화면 추가]** (❌ 폴더 추가 아님) | 약 16 KB |
| **④** | **`ds/js/brand-story.js` 신규** | `ds/js` 폴더를 연 채 **[화면 추가]** | 약 2 KB |
| **⑤** | **`brand-story.html` 신규** | **최상위 폴더를 연 채 [화면 추가]** → 이름 `brand-story.html` | 약 18 KB. ③④ 다음이어야 합니다 (순서를 어기면 없는 파일을 부릅니다) |
| **⑥** | **`moa/layout/header.html` 수정** — 「리뷰」 `<li>` 삭제, 그 자리에 「웰니스 ▸ 브랜드 스토리」 드롭다운 | 데스크톱 가로 메뉴 | **개발이 이 파일의 원문을 아직 갖고 있지 않습니다 → §8 R-1** |
| **⑦** | **`moa/layout/aside.html` 수정** — 같은 교체 | 모바일 햄버거 패널 | 같음 |
| **⑧** | **미리보기**로 확인 | | §7-D |
| **⑨** | **적용** | 여기서 처음 라이브가 바뀝니다 | |

**중간에 멈춰도 되는 지점:** ①②③④ 까지는 **화면 변화 0** 입니다(아무도 그 파일들을 부르지 않습니다). ⑤ 를 저장하면 그때 `/brand-story.html` 이 생기지만, **메뉴에 링크가 없으므로 주소를 아는 사람만 볼 수 있습니다.** ⑥⑦ 이 메뉴에 노출시키는 단계입니다.

## C. 관리자(편집기 밖)에서 하실 일

| # | 항목 | 위치 | 비고 |
|---|---|---|---|
| **C-1** | **사이트맵 등록** | 카페24 사이트맵(`/sitemap.xml`)은 **상품·분류·게시판·약관만** 자동 등록합니다. `/brand-story.html` 은 **자동으로 들어가지 않습니다.** | Google Search Console 에 URL 직접 제출 / robots·sitemap 정합은 이미 장부 E-3 |
| **C-2** | **푸터 「회사소개」(`/about.html`) 처리** | 현재 **본문이 빈 페이지**입니다(배너 0건). 방치 / 브랜드 스토리로 링크 변경 / 배너매니저로 채우기 중 택1 | 이번 범위 밖. 결정만 주시면 됩니다 |
| **C-3** | **인스타그램 프로필·광고 랜딩** | 브랜드 스토리 URL 확정 후 | `https://zengenetics.co.kr/brand-story.html` |

## D. 「적용」 직후 30초 확인

- [ ] `https://zengenetics.co.kr/brand-story.html` 이 열린다 (404 가 아니다)
- [ ] **첫 화면 사진이 보인다** · 스크롤하면 나머지 사진 14장이 순서대로 뜬다 (**빈칸이 하나도 없어야 합니다**)
- [ ] 「지금까지」 섹션의 숫자 4개가 **0 에서 올라간다**
- [ ] 헤더·푸터가 다른 페이지와 **같은 모습**이다 (메뉴가 두 벌 보이면 안 됩니다)
- [ ] 햄버거를 열면 **「웰니스」 아래 「브랜드 스토리」**가 있고, **「리뷰」는 없다**
- [ ] F12 → Network → `brand-story.css` / `brand-story.js` **200**
- [ ] **다른 페이지(홈·상품·장바구니)가 그대로다** ← 전역 CSS 유출이 없었다는 뜻

---

# 8. 위험 목록

| # | 위험 | 근거 / 영향 | 대응 |
|---|---|---|---|
| **R-1** | **`moa/layout/header.html` · `aside.html` 원문이 저장소에 없다** | 저장소는 "완전한 백업이 아니다"(`cafe24-skin/README.md`). 라이브에서 받을 수 있는 것은 **렌더된 결과**뿐이라 `{$변수}` 원문이 아니다. 메뉴 교체 코드를 **정확히** 만들 수 없다 | **선결.** 대표님이 편집기에서 두 파일 전문을 복사해 주시면 저장소에 보존 후 교체본을 만든다. **그 전에는 메뉴 수정 코드를 쓰지 않는다.** *(다행히 라이브 렌더 결과에 `<!-- 🔽 리뷰 메뉴 -->` 편집 주석이 그대로 남아 있어 삽입 지점 자체는 확정돼 있다)* |
| **R-2** | **전역 CSS 유출로 쇼핑몰 전체가 깨진다** | 프로토타입 `<style>` 에 `body{…}` `*{box-sizing}` `:root{--…}` `::selection` 이 있다. 그대로 옮기면 **홈·상품·장바구니 전 페이지**에 적용된다. 스킨 전역 CSS 는 188,912 B / `!important` 98개 | `.zgbs` 래퍼 스코프 강제, 전역 셀렉터 0개, 새 `!important` 0개, 변수 `--zg-` 접두. 검수에서 **`brand-story.css` 안의 최상위 `body`/`html`/`*`/`:root` 셀렉터 0개**를 기계적으로 확인 |
| **R-3** | **웹폰트 1.23 MB / 100 요청** | §6 실측. 모바일 LCP 붕괴 | IBM Plex Sans KR 제거(→Pretendard) + Noto Serif KR 서브셋(47 KB/2요청). **폰트 업로드가 막히면 Google Fonts 1줄로 폴백** |
| **R-4** | **`<title>` 을 못 정한다** | `moa/layout/head.html` 이 `<title>기본 레이아웃</title>` 이고, 커스텀 페이지는 카페24가 몰 이름으로 덮는다(`/about.html` 실측 → `젠제네틱스 공식몰`). 페이지 본문은 `<main>` 안이라 `<title>` 을 넣을 자리가 없다 | 페이지 최상단 인라인 `<script>` 에서 `document.title` · `meta[name=description]` · OG 태그를 파싱 중에 설정한다. **`head.html` 은 고치지 않는다**(전 페이지 영향). 근본 해결은 장부 **E-2(홈 `<title>` P0)** 와 같은 건이므로 함께 올린다 |
| **R-5** | **숫자·문구의 사실성** | 본문에 **리뷰 26,961건 / 평점 4.9 / 재구매율 92% / 수출 14개국**, 「올리브영 실시간 랭킹 1위(2026.03.22 09~10시)」, 「쿠팡 실시간 랭킹 1위(2026.07.27~08.25)」가 하드코딩돼 있다. 칼륨 브리프 §3 은 상세페이지에서 **「4.9 / 26,961건」 하드코딩 이식을 금지**했다(알파리뷰가 진실원장) | **대표님 확인 항목.** 대표님이 확정·제공하시는 수치는 회사 공식 데이터로 즉시 사용한다(CLAUDE.md). **확인 전에는 배포하지 않는다.** 리뷰수·평점은 알파리뷰 위젯으로 대체할 수 있는지 별도 검토 |
| **R-6** | **최상위 [화면 추가]가 안 될 수 있다** | 편집기 UI 를 직접 못 봤다. `/about.html` 이 200 인 것은 **파일이 이미 있다**는 증거일 뿐, **새로 만들 수 있다**는 증거는 아니다 | 차선 경로 `ds/html/brand-story.html` 을 미리 준비. **코드는 동일**하고 URL 만 바뀐다 |
| **R-7** | **데스크톱에서 커버가 1280px 에 갇힌다** | `#contents{max-width:1280px;padding:0 50px 200px}` (≥1025px) | `.zgbs .cover{margin-left:calc(50% - 50vw);width:100vw}` 로 출혈 처리. **`#contents` 규칙 자체는 건드리지 않는다** (전 페이지 영향) |
| **R-8** | **메뉴가 두 벌 보인다** | 프로토타입에 자체 헤더(`.top`)·햄버거(`.zgb`)·드로어(`#zgm`)가 들어 있다. 스킨에도 `.jsHamburger` + `aside` 가 있다 | B-01·B-02·B-20 을 **파일에서 통째로 삭제**. 검수에서 `zgm`·`zgb`·`<footer`·`class="top"` 문자열 **0회** 확인 |
| **R-9** | **다크모드 반쪽 화면** | 스킨 `prefers-color-scheme` 0회 (실측) | A-2 결정 전까지 **다크 블록 비활성**이 기본값 |
| **R-10** | **적용 후 되돌리면 브랜드 스토리도 같이 사라진다** | 되돌리기 = `바디랩 반응형` 재적용 = 작업본의 모든 것이 꺼짐 | 칼륨 체크리스트 ⑧과 동일. **되돌리기 표에 「브랜드 스토리 페이지도 함께 404 가 됩니다」 한 줄을 추가**한다 |
| **R-11** | **붙여넣기 잘림** | 최대 파일이 `brand-story.html` 약 18 KB — 검증된 상한(`detail.css` 45,551 B)의 40% | 각 파일 「맨 끝 줄」 색인 제공 + md5 대조. 위험 낮음 |
| **R-12** | **프로토타입의 브랜드 스토리 링크가 아티팩트 URL** | `site/home.html` · `site/brand-story.html` 의 드로어·nav 가 `claude.ai/code/artifact/1fd7a5ca…` 를 가리킨다 | 배포 후 실제 URL 로 교체 (프로토타입 2파일 · 라이브 메뉴 2파일) |

---

# 9. 파일 계획

## 9-1. 새로 만드는 것 (5 + 17)

| 순 | 저장소 경로 | 카페24 위치 | 예상 바이트 | 성격 |
|:--:|---|---|--:|---|
| ① | `cafe24-skin/ds/image/bs-*.webp` (15개) | `ds/image/` | **1,553,406** | 이미지 (업로드) |
| ② | `cafe24-skin/ds/image/bs-serif-400.woff2` · `-600.woff2` | `ds/image/` | **47,248** | 서브셋 폰트 (업로드) |
| ③ | `cafe24-skin/ds/css/brand-story.css` | `ds/css/` **[화면 추가]** | 약 **16,000** | 신규 CSS (프로토타입 14,192 B + 스코프·출혈 규칙) |
| ④ | `cafe24-skin/ds/js/brand-story.js` | `ds/js/` **[화면 추가]** | 약 **2,000** | 신규 JS (카운트업 + reveal + title/meta) |
| ⑤ | `cafe24-skin/brand-story.html` | **최상위 [화면 추가]** | 약 **18,000** | 신규 페이지 (`@layout` + `@css` + `@js` + 본문 18블록) |

## 9-2. 고치는 것 (2) — **R-1 선결 후**

| 파일 | 변경 |
|---|---|
| `cafe24-skin/moa/layout/header.html` | 「리뷰」 `<li class="category__item">` 블록 → 「웰니스」 + `.sub-category` 안에 「브랜드 스토리」 |
| `cafe24-skin/moa/layout/aside.html` | 「리뷰」 `<li class="aside-category__item">` 블록 → 「웰니스」 + `.aside-child` 안에 「브랜드 스토리」 |

**두 파일 모두 스킨에 이미 있는 「이벤트」 블록과 똑같은 구조를 복제한다. 새 CSS·JS 0줄.**

## 9-3. 손대지 않는 것

`moa/layout/head.html` (폰트·GA4 재삽입 금지) · `layout/basic/layout.html` · `product/detail.html` 등 **칼륨 배포 7파일 전부** · `about.html` · 관리자 콘텐츠 전부 · 라이브 `바디랩 반응형`.

## 9-4. 전달 폴더 (QA 4차 §(d) 규칙 = **한 폴더 = 한 배포**)

```
전달/2026-09-XX_브랜드스토리/
  00_읽어주세요.md      ← 순번 ↔ 파일 ↔ 카페24 위치 ↔ 바이트 ↔ md5 ↔ 맨 끝 줄
  img/  bs-*.webp (15) + bs-serif-*.woff2 (2)   ← 그대로 업로드
  3_ds-css-brand-story.css.txt
  4_ds-js-brand-story.js.txt
  5_brand-story.html.txt
  6_moa-layout-header.html.txt        ← R-1 해소 후
  7_moa-layout-aside.html.txt         ← R-1 해소 후
```
파일 앞 번호 = §7-B 순번. **저장소 ↔ 전달본 md5 전쌍 일치**를 배포 전에 확인한다.

---

# 10. 선결 항목 (이게 닫혀야 코드를 씁니다)

| # | 항목 | 누구 |
|---|---|---|
| **P-1** | `moa/layout/header.html` · `moa/layout/aside.html` **원문 확보** (R-1) | 대표님 → 편집기에서 복사 |
| **P-2** | **A-1 배포 시점** (칼륨과 함께 / 다음 판) | 대표님 |
| **P-3** | **A-2 다크모드** (끈다 / 켠다 / 토글) | 대표님 |
| **P-4** | **R-5 숫자·랭킹 문구 사실성 확인** (26,961 / 4.9 / 92% / 14개국 / 올리브영·쿠팡 랭킹) | 대표님 |
| **P-5** | 최상위 [화면 추가] 가능 여부 (R-6) — **①②③④ 를 하시면서 확인만 해 주시면 됩니다** | 대표님 |

**P-1·P-4 는 코드 작성 전에 반드시 닫혀야 한다.** P-2·P-3·P-5 는 코드를 쓰면서 병행 가능하다.

---

**개발 (웹/모바일 개발팀) · 2026-09-07 · 1차 설계서**
